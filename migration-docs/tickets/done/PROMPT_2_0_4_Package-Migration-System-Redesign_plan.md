## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.0.4
- **Scope:** `app/modules/migration/src/MigrationService.php`, `app/system/index.php`, `app/system/src/Controller/MigrationController.php`, `app/console/src/Commands/MigrationCommand.php`, `app/installer/src/Package/PackageManager.php`, `app/modules/auth/src/Handler/DatabaseHandler.php`, `packages/pagekit/blog/src/Migrations/2025/Version001_CreateBlogTables.php`, `tests/unit/Migration/MigrationServiceTest.php`
- **Deferred:**
  - Step 5.6 (Marketplace & Extensions) — full marketplace API, ZIP upload modernization, marketplace API integration
  - Step 2.5 (Extension Safety) — sandboxed module loading, fault isolation, auto-disable
  - Step 2.0.5 (Composer Hygiene) — autoload cleanup, dependency removals
  - Step 2.1.7 (QueryBuilder API) — `json_array` → `json` type normalization (blog migration already uses `json`; `DatabaseHandler` uses `json_array` but is being deleted in this step)
- **Bridges:** None. This is a clean internal unification per Rules 1, 2, 4 (No Compatibility Layers, No Adapters, Delete Over Wrap).
- **Breaking Change:** `PackageManager::enable()` now requires `migration` service in container for auto-migration. Extensions with `src/Migrations/` directory will have migrations auto-executed on enable and auto-rolled-back on uninstall.
- **Checklist:**
  1. **Harden MigrationService return values & delete dead code** — `app/modules/migration/src/MigrationService.php`: (a) In `migrate()` (~line 143-154), remove the `is_array($result)` branch and the TODO comment — `$migrator->migrate()` returns `array<string, MigrationResult>` (Doctrine Migrations 3.x); a non-empty return means executed migrations, exception means failure. Replace with: count `$result` directly, extract execution info from `MigrationResult` objects. (b) Apply identical fix in `rollback()` (~line 252-259), `migrateExtension()` (~line 530-537), `rollbackExtension()` (~line 656-662). (c) Delete `getConfigPath()` (~line 80-83) — unused private method. Files: `app/modules/migration/src/MigrationService.php`.
  2. **Unify login check — check Doctrine Migrations before version bump** — `app/system/index.php` (~line 123-138): In the `auth.login` event handler, before the `else` branch that silently bumps version, also check `MigrationService::status()['has_pending']` via `$app->get('migration')`. If pending Doctrine Migrations exist OR `scripts->hasUpdates()`, redirect to migration wizard. Never bump version without both checks passing. Remove the existing TODO comment. Files: `app/system/index.php`.
  3. **Unify update wizard — run Doctrine Migrations before scripts** — `app/system/src/Controller/MigrationController.php`: (a) In `migrateAction()`, call `$this->app->get('migration')->migrate()` BEFORE `scripts->update()`. On failure, throw `\RuntimeException`. (b) In `indexAction()`, also check `MigrationService::status()['has_pending']` — if pending migrations exist, show update page even if `scripts->hasUpdates()` returns false. (c) Type constructor parameters properly: replace `mixed $system` with the actual types (`\Pagekit\Module\Module`, config callable, etc.) or at minimum use more specific types where determinable. (d) Add `use Pagekit\Migration\MigrationService` import. Files: `app/system/src/Controller/MigrationController.php`.
  4. **Unify CLI migrate command — Doctrine Migrations + scripts** — `app/console/src/Commands/MigrationCommand.php`: (a) Add `declare(strict_types=1)`. (b) In `execute()`, run `MigrationService::migrate()` first via `$this->container->get('migration')`. Report count of executed migrations. On failure, output error and return `Command::FAILURE`. (c) Then run `PackageScripts::update()` as before. (d) Bump version only after both succeed. (e) Replace `return 0` with `return Command::SUCCESS`. (f) Remove the existing TODO comment. Files: `app/console/src/Commands/MigrationCommand.php`.
  5. **Extension lifecycle — auto-migrate on enable** — `app/installer/src/Package/PackageManager.php`: In `enable()`, after `$scripts->update()` succeeds but BEFORE setting the version in config, check if extension has a `src/Migrations/` directory (relative to package path). If it does and `$this->app->has('migration')`, call `$this->app->get('migration')->migrateExtension($namespace . '\\Migrations', $path . '/src/Migrations')`. On failure, throw `\RuntimeException` (the existing `try/catch(\Throwable)` in `enable()` will handle rollback). Derive namespace from the package's `autoload` config or module namespace. Files: `app/installer/src/Package/PackageManager.php`.
  6. **Extension lifecycle — auto-rollback on uninstall** — `app/installer/src/Package/PackageManager.php`: In `uninstall()`, before `$this->composer->uninstall()` or file deletion, check if extension has `src/Migrations/` directory. If it does and `$this->app->has('migration')`, call `$this->app->get('migration')->rollbackExtension($namespace . '\\Migrations', $path . '/src/Migrations', '0')` to rollback all extension migrations. Wrap in try/catch — log warning on failure but do not block uninstall. Files: `app/installer/src/Package/PackageManager.php`.
  7. **Remove deprecated DatabaseHandler::createTable()** — `app/modules/auth/src/Handler/DatabaseHandler.php`: (a) Delete the `createTable()` method (~line 130-146). (b) Remove the `$this->createTable()` call from `write()` (~line 86). (c) Verify the core migration `Version20251023061532` creates `@system_auth` with matching schema: `id` string(255), `user_id` integer unsigned default 0, `access` datetime nullable, `status` smallint, `data` json nullable, PK(id). Confirmed: migration uses `json` (modern) vs deprecated `json_array` — this is correct, no alignment needed. Files: `app/modules/auth/src/Handler/DatabaseHandler.php`.
  8. **Rename Blog migration to timestamp format** — (a) Rename `packages/pagekit/blog/src/Migrations/2025/Version001_CreateBlogTables.php` to `packages/pagekit/blog/src/Migrations/2025/Version20251023070000_CreateBlogTables.php` (use a timestamp consistent with the core migration date). (b) Update the class name inside the file from `Version001_CreateBlogTables` to `Version20251023070000_CreateBlogTables`. (c) Note: since this migration class name is tracked in the `migration_versions` DB table, existing installations that have already run the migration would need a version table update. Add a TODO comment: `// TODO: AUDIT FIX Step 2.0.5 — Existing installations may need migration_versions table updated from Version001_CreateBlogTables to this class name`. Files: `packages/pagekit/blog/src/Migrations/2025/Version001_CreateBlogTables.php` (rename).
  9. **Type PackageManager public API** — `app/installer/src/Package/PackageManager.php`: (a) `uninstall($uninstall)` → `uninstall(string|array $uninstall)`. (b) `enable($packages, $previousPackageConfigs = [])` → `enable(object|array $packages, object|array $previousPackageConfigs = [])`. (c) `disable($packages)` → `disable(object|array $packages)`. (d) `getScripts($package, $current = null)` → `getScripts(object $package, ?string $current = null)`. (e) `doInstall($package)` → `doInstall(object $package)`. (f) `getVersion($package)` → `getVersion(object $package)`. (g) `rollbackEnable($package, array $originalState)` → `rollbackEnable(object $package, array $originalState)`. (h) Remove outdated `@param` PHPDoc blocks that conflict with the new type declarations. Files: `app/installer/src/Package/PackageManager.php`.
  10. **Write real MigrationServiceTest tests** — `tests/unit/Migration/MigrationServiceTest.php`: Replace all `markTestSkipped()` tests with real implementations using SQLite in-memory connection. Tests: (a) `testMigrateRunsPendingMigrations()` — create a temp migration class, run `migrate()`, verify `success=true` and `executed > 0`. (b) `testRollbackRevertsLastMigration()` — migrate up, then rollback, verify table changes reverted. (c) `testStatusReturnsPendingCount()` — register migrations, call `status()`, verify `has_pending` is true and available count matches. (d) `testMigrateExtensionRunsExtensionMigrations()` — create temp extension migration dir, run `migrateExtension()`, verify success. (e) `testRollbackExtensionOnlyAffectsExtensionNamespace()` — run core + extension migrations, rollback extension only, verify core migrations untouched. (f) `testMigrateWithNoMigrationsReturnsZeroExecuted()` — call `migrate()` when already up-to-date. Use `setUp()` to create in-memory SQLite connection and configure MigrationService. Use `tearDown()` to clean up temp migration files. Files: `tests/unit/Migration/MigrationServiceTest.php`.
  11. **Verify zero silent version bumps & final cleanup** — (a) `rg` for any remaining `->set('version',` calls that bypass migration check — confirm only the wizard and CLI (which now run migrations first) perform version bumps. (b) Verify no remaining TODO comments referencing "Step 2.0.4" that should have been resolved (resolved TODOs should be deleted, not left behind). (c) Verify `app/modules/migration/index.php` registers the `migration` service correctly for all code paths to consume.

## TESTING STRATEGY
- **Per step:** PHPUnit + PHPStan (mandatory after every checklist step)
- **Final run (after all steps):** PHPUnit + PHPStan + `php pagekit setup` + `php pagekit list` + Playwright E2E (installation, login, dashboard)
