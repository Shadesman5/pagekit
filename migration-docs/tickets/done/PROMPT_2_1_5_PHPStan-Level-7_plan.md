## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.1.5 (PHPStan Level 6→7 — Null Safety). GitHub Issue: #152. Predecessor Step 2.1.4 already landed (PR #203). No Phase 1 audit closures here — 1.11 closes later via 2.0.8 + 2.1.6 + 2.1.10/2.1.11 per ROADMAP.
- **Scope:**
  - `phpstan.neon`: bump `level: 6` → `level: 7` (final checklist step only; intermediate steps run a **scoped Level-7 check** under the still-committed `level: 6` config).
  - `phpstan-baseline.neon`: surgical removals only (entries made unmatched by source fixes); **never** run `--generate-baseline`; **never** add a new entry; **never** increment `count: N → N+1`.
  - **Property type sweep (small, well-bounded surface)** — `grep` for `(private|protected|public) \$[a-zA-Z_]` (i.e. untyped properties remaining after Step 2.1.4) returns:
    - `app/modules/` — 11 files: `kernel/src/Event/ExceptionListener.php`, `kernel/src/Event/ControllerEvent.php`, `filesystem/src/StreamWrapper.php`, `feed/src/Feed.php`, `feed/src/Feed/RSS1.php`, `feed/src/Feed/RSS2.php`, `feed/src/Feed/Atom.php`, `debug/src/Event/WrappedListener.php`, `application/src/Module/Loader/CallableLoader.php`, `application/src/Application/Console/Command.php`, `application/src/Tests/FtpTestCase.php`.
    - `app/console/src/Commands/` — 16 command files (each with 1–2 untyped properties): `Translation/{Fetch,...}Command.php`, `Migration{,/{StatusCommand,RollbackCommand,GenerateCommand,MigrateRunCommand}}.php`, `Update`/`Start`/`Selfupdate`/`Setup`/`Uninstall`/`Install`/`Build`/`Archive`/`ExtensionTranslate`/`ClearCache`Command.php.
    - `app/system/`, `app/installer/`, `packages/pagekit/` source — **zero** matches (verified: `Grep` `(private|protected|public) \$[a-zA-Z_]` on `app/system/**/*.php` and `app/installer/**/*.php` returns no files; only `packages/composer/ClassLoader.php` matches, which is excluded from PHPStan via the `excludePaths` added in Step 2.1.4 Checklist Step 1).
  - **Null-safety sweep** — under L7, PHPStan reports primarily `nullsafe.neverNull`, `method.nonObject` ("Call to method on null"), `property.nonObject` ("Access to property on null"), `argument.type` (`X|null` passed where `X` expected), `return.type` (returning `X|null` from a `: X` signature), and `instanceof.alwaysTrue`/`alwaysFalse` follow-on inferences. Per the prompt's §3.1 decision tree and the ROADMAP "Agent Note": **prefer fixing the type declaration over adding a null guard**. Null guards are a code smell when the value is in fact always set — a wrong type declaration is the real bug. Verifier scrutinizes every `!== null` / `?->` introduction.
  - **Cross-module spillover discipline** — same precedent as Step 2.1.4 amendments #1/#2/#3: if narrowing a property type or a method signature in one module makes baseline entries unmatched in another module's files, surgically remove those entries in the same Checklist Step commit. No baseline regeneration. No `@phpstan-ignore`. No `// TODO`.
- **Deferred:**
  - **Step 2.1.6 (PHPStan Level 7→8 — Strict Typing)** — `mixed` is acceptable in this step where genuinely unknown (`__call`, `__invoke`, container/registry payloads). Do **not** sweep `mixed` here. The Bugbot-flagged `MailerInterface` split, `Mailer::beforeSend()`/`afterSend()` typing, `ModelServiceLocator` / `IntlServiceLocator` / `EntityManager` singleton typing, ORM `Metadata` / `Relation` / `PropertyTrait` typing gaps, `#[AllowDynamicProperties]` on `Node`/`Widget`, `app/modules/database/src/Logging/DebugStack.php` deletion, `app/modules/routing/src/Event/AliasListener.php` dead query-string parser deletion, naming-collision renames (`UrlGeneratorInterface`, `GetResponseEvent`) — all explicitly Step 2.1.6 work.
  - **Step 2.1.7 (QueryBuilder API standardization)** — touches `app/modules/database/`; do not refactor query-builder call sites beyond null-safety fixes.
  - **Step 2.1.8 / 2.1.9 (Infection / Coverage)** — out of scope.
  - **Step 2.1.10 (Entity Presentation Layer — `ModelServiceLocator` → DTO/presenter)** — architectural removal; this step only narrows types where strictly required to satisfy L7.
  - **Step 2.1.11 (EntityManager DI)** — architectural removal of the static singleton; out of scope.
  - **PSR-11 `Container::get()` / `ContainerInterface::get()`** — keep returning `mixed` per PSR-11 contract (Step 2.0.1); do NOT narrow.
  - **`packages/composer/*`** (Composer-generated autoload scaffolding) — already excluded from PHPStan in 2.1.4 Step 1; no fixes, no baseline entries, no `// TODO` markers.
  - **PHPStan baseline regeneration** — explicitly forbidden. The only allowed baseline edits are surgical removals of entries that become unmatched.
- **Bridges:** None. Null safety is forward-only. Where a child override breaks parent compatibility (e.g. a parent declares `?string` and the child currently has `string`), fix the child to match the parent — never wrap, never alias. Where a property genuinely can be uninitialized until a setter is called, declare it as `?Type` with `= null` initializer (no `unset()` games). Where the Refactorer is tempted to add `if ($x !== null) { $x->method(); }` purely to silence PHPStan, the Verifier rejects the change and the Refactorer fixes the **type** instead (the parent property/parameter declaration was wrong).
- **Checklist:**
  1. **Discovery snapshot + baseline pre-clean.** Run `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M --level=7 --error-format=raw 2>&1 | tee tmp/phpstan_l7_discovery.txt` (gitignored under `tmp/`, never committed). Categorize the snapshot by identifier (`missingType.property`, `method.nonObject`, `property.nonObject`, `argument.type`, `return.type`, `nullsafe.neverNull`, `instanceof.alwaysTrue/False`) and by directory (`app/modules/{kernel,application,config,log,session,auth,migration,database,view,markdown,filter,filesystem,routing,feed,debug}`, `app/system/modules/{mail,user,site,intl,widget,finder,captcha,dashboard,content,cache,view,settings,comment,info}`, `app/system/src`, `app/installer`, `app/console`, `packages/pagekit/{blog,theme-one}`). Save the per-directory breakdown into the same `tmp/phpstan_l7_discovery.txt` for downstream steps to consume. Do **not** bump the level yet. Do **not** edit any source file. Do **not** add baseline entries. Pre-clean any obviously stale baseline entries that are already unmatched on `develop` HEAD (run `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` at the configured Level 6 and surgically remove any entries flagged `Ignored error pattern ... was not matched in reported errors`). Commit only the baseline diff (and only if non-empty): `chore(phpstan): pre-clean stale baseline entries before L7 sweep`. **If the L6 run is already clean (no unmatched entries) this Checklist Step ships an empty diff and the Orchestrator skips the commit — Refactorer emits `Step 1 done. Files: [].` in that case.**
  2. **Property types — `app/modules/` (11 files).** Add typed properties (and `readonly` where applicable) to every untyped property in:
     - `app/modules/kernel/src/Event/ExceptionListener.php` — type the listener's logger/handler properties from their constructor injections.
     - `app/modules/kernel/src/Event/ControllerEvent.php` — type the event-payload properties (controller callable, arguments).
     - `app/modules/filesystem/src/StreamWrapper.php` — PHP stream-wrapper contract properties; consult the stream-wrapper class contract (`$context` is `?resource`, internal cursor properties are `int`).
     - `app/modules/feed/src/Feed.php`, `feed/src/Feed/{RSS1,RSS2,Atom}.php` — type the feed channel/item/builder properties.
     - `app/modules/debug/src/Event/WrappedListener.php` — type the wrapped-callable + name properties.
     - `app/modules/application/src/Module/Loader/CallableLoader.php` — type the `callable` loader property.
     - `app/modules/application/src/Application/Console/Command.php` — type the `$container` / `$config` properties consistent with Symfony Console's `Command`.
     - `app/modules/application/src/Tests/FtpTestCase.php` — type the FTP harness properties.
     Where a constructor parameter sets a property 1:1, prefer **constructor property promotion** (`public readonly Foo $foo`). Where a property is genuinely uninitialized until a setter is called, declare `?Type` with `= null` initializer. Do **not** add `// TODO`. Per-step Tester runs scoped L7 on `app/modules/` and asserts the property-typing errors in these files drop to 0. Commit: `refactor(types): add property types to app/modules core files`.
  3. **Property types — `app/console/` commands.** Add typed properties to every untyped property in the 16 command files under `app/console/src/Commands/` (including the `Migration/` subdirectory). Each command currently has `$container` / `$config` / `$options` / `$arguments` style properties; type them according to their constructor injection (typically `Pagekit\Application` for legacy `$container`, `array<string, mixed>` for `$options`/`$arguments` config). For commands that pull services off `$this->container` via `$this->getContainer()->get('foo')`, this is **out of scope** for property typing — Step 2.1.6 narrows those access patterns. This step only types the properties themselves. Do **not** add `// TODO`. Per-step Tester runs scoped L7 on `app/console` and asserts property-typing errors in this directory drop to 0. Commit: `refactor(types): add property types to console commands`.
  4. **Null safety — `app/modules/` Group A (kernel + application + config + log + session + auth + migration).** For every L7 error reported by the discovery snapshot under `app/modules/{kernel,application,config,log,session,auth,migration}/` of identifier `method.nonObject` / `property.nonObject` / `argument.type` / `return.type` / `nullsafe.neverNull` / `instanceof.alwaysTrue|alwaysFalse`, apply the §3.1 decision tree from the prompt:
     1. **Can the value actually be `null` at runtime?** → If yes, add a null guard at the call site (early-return or branch).
     2. **Is the type declaration wrong?** → If the value is always initialized (e.g. set in constructor and never reassigned to `null`), narrow the type from `?Foo` → `Foo` (remove the `?`). This is the preferred fix and the one the Verifier looks for.
     3. **Is it a logic bug?** → Fix the bug; if the bug is out of scope (e.g. requires a refactor flagged in a future step), narrow the type with an `instanceof` / `assert(false === null)` runtime guard that throws on the impossible path (`throw new \LogicException(...)`) — never with `@phpstan-ignore`, never with `/** @var */`.
     **Audit hot-spot:** the `Pagekit\Auth\Auth::login()` / `logout()` returns were narrowed in Step 2.1.4 amendment #3 to `LoginEvent` / `LogoutEvent`. The matching event listeners in `app/modules/auth/src/Event/` may now report `argument.type` errors against the narrower types — fix them by accepting the concrete event subclass in the listener signature. **Verifier check:** `git diff` for this step must NOT show any new `if (\$x !== null)` blocks added purely to silence `method.nonObject` — every such guard must be justifiable as a real runtime possibility. Per-step Tester runs scoped L7 on `app/modules/{kernel,application,config,log,session,auth,migration}` and asserts zero errors. Commit: `refactor(types): null safety for kernel, application, config, log, session, auth, migration`.
  5. **Null safety — `app/modules/database/` (Group B, ORM).** Same §3.1 decision tree across `app/modules/database/`. **Critical scope guard:** Step 2.1.7 (QueryBuilder API standardization) covers query-builder call-site refactors; **do not** refactor `QueryBuilder` usage here beyond null-safety fixes. The `ORM\ModelTrait::find(): ?static` / `create(): static` `instanceof static` guards were added in Step 2.1.4 amendment #3; do not re-touch them. The `EntityManager` singleton typing stays untouched (deferred to Step 2.1.11). The `ModelServiceLocator` / `IntlServiceLocator` static service locators stay untouched (deferred to Step 2.1.6 type-narrow + Step 2.1.10 architectural removal). Focus on: `Connection`/`Statement`/`Result` null-result handling (e.g. `Connection::fetchAssociative()` returns `array|false` — fix call sites that treat the return as always-array), `Schema\Comparator` / `Schema\Schema` null-table edge cases, `ORM\Relation\*` null relation-target handling, `ORM\Metadata` reflection lookups. **`trait.unused` scoping exemption** for `ModelTrait` and `PropertyTrait` carries over from Step 2.1.4 — the scoped Tester gate excludes those two specific errors as cross-module scoping artifacts (option (d), standing convention). Verifier check: no new `@phpstan-ignore`, no widening of return types to `mixed` to silence errors. Per-step Tester runs scoped L7 on `app/modules/database` and asserts non-`trait.unused` errors drop to zero. Commit: `refactor(types): null safety for app/modules/database (ORM, DBAL wrappers)`.
  6. **Null safety — `app/modules/` Group C (view + twig submodule + markdown + filter + filesystem).** Same §3.1 decision tree across `app/modules/{view,markdown,filter,filesystem}` and the `view/modules/twig/` Twig integration. Hot-spots include: `View::render()` returning `?string` when template is missing (decide: throw vs. return `null` — match the contract on the consumer side), `Filter\FilterManager::filter()` chain (each step can refine `mixed`; null-safety affects only the entry points), `Filesystem\StreamWrapper` (`fopen()`/`stat()` `false` returns), `Twig\TwigLoader::findTemplate(string $name, bool $throw = true): ?string` (the Step 2.1.4 narrowing now propagates as `?string` to all callers). Per-step Tester runs scoped L7 on `app/modules/{view,markdown,filter,filesystem}` and asserts zero errors. Commit: `refactor(types): null safety for view, twig, markdown, filter, filesystem`.
  7. **Null safety — `app/modules/` Group D (routing + feed + debug).** Same §3.1 decision tree across `app/modules/{routing,feed,debug}`. Hot-spots: `Routing\Router::generate()` (route may be unknown — `?string` return), `Routing\RouteCollection::get()` (collection lookup may miss), `Feed\Feed::load()` (file/URL may be unreachable), `Debug\DataCollector\AuthDataCollector::collect()` (already narrowed in 2.1.4 §4.1; verify no regression). **Out of scope:** the dead `Routing\Event\AliasListener.php` query-string parser block (lines 50–57) is tagged `// TODO: Must be refactored in Step 2.1.6` and is deleted in Step 2.1.6 — leave the `// TODO` in place. Per-step Tester runs scoped L7 on `app/modules/{routing,feed,debug}` and asserts zero errors. Commit: `refactor(types): null safety for routing, feed, debug`.
  8. **Null safety — `app/modules/` final sweep.** Catch-all for any remaining L7 errors in `app/modules/` files not yet covered (`Container.php`, top-level utility classes, cross-cutting `Module/` / `Event/` files). After this step the scoped L7 check `./app/vendor/bin/phpstan analyse --level=7 --no-progress --memory-limit=512M app/modules` must report 0 errors (modulo the 2 `trait.unused` scoping artifacts on `ModelTrait`/`PropertyTrait` — same standing exemption as Step 5). Commit: `refactor(types): finalize app/modules null-safety sweep`.
  9. **Null safety — `app/system/modules/{mail,user}`.** Same §3.1 decision tree. Hot-spots:
     - `mail/Mailer::send(Email $message): bool` — Step 2.1.4 already declared `bool`. Verify call sites do not treat the boolean as a `Result` object.
     - `mail/Message::send(?array &$errors = null): int` / `queue()` — verify `$errors` out-parameter semantics under L7 (null-safety on the by-reference reassignment).
     - `user/Controller/ResetPasswordController` + `RegistrationController` mailer call chains — these now use the concrete `\Pagekit\Mail\Mailer` (post-2.1.4); confirm no `?Mailer` slipped in.
     - `user/Auth/Auth::login()`/`logout()` — already narrowed to `LoginEvent`/`LogoutEvent` (2.1.4 amendment #3); L7 may surface event-listener consumers that still expect `?EventInterface`. Fix listeners to the concrete event types.
     - `user/Model/User::permissions` annotated `array<int, string>|null` in 2.1.4; consumers iterating without a null guard → fix them.
     - **`AuthorizationListener` / `AccessListener`** — null-safety on `getUser()` returns. Verifier scrutinizes every `if ($user !== null)` guard.
     Per-step Tester runs scoped L7 on `app/system/modules/{mail,user}` and asserts zero errors. Commit: `refactor(types): null safety for mail and user modules`.
  10. **Null safety — `app/system/modules/{site,intl}`.** Same §3.1 decision tree. Hot-spots:
      - `site/Controller/MenuApiController::indexAction()` — the local `$trashCount` accumulator pattern committed in Step 2.1.4 Step 7 amendment #2 is type-correct under L7. Verify no regression.
      - `site/Model/Node` `BelongsTo($user)` / `HasMany($comments)` style relations now typed as `?User` / `?array` (post-2.1.4); fix consumers that dereference without guarding.
      - `intl/Controller/IntlController` + `IntlApiController` — locale lookup chains may return null; narrow vs. guard.
      - `IntlServiceLocator` static usage stays untouched (Step 2.1.6/2.1.10 territory). Only narrow its `mixed` returns at consumer sites if L7 fires there; **do not** modify the locator itself.
      Per-step Tester runs scoped L7 on `app/system/modules/{site,intl}` and asserts zero errors. Commit: `refactor(types): null safety for site and intl modules`.
  11. **Null safety — `app/system/modules/` tail (widget, finder, captcha, dashboard, content, cache, view, settings, comment, info).** Same §3.1 decision tree across all ten modules. These modules carry less business logic than `user`/`site`, and most L7 errors here should resolve to type narrowings rather than guards. Per-step Tester runs scoped L7 on these directories and asserts zero errors. Commit: `refactor(types): null safety for system tail modules`.
  12. **Null safety — `app/system/src/` + `app/installer/` + `app/console/`.** Same §3.1 decision tree. `app/system/src/Controller/{AdminController,ExceptionController}` already have concrete constructor types (2.1.4); L7 may surface `Request::getSession()` / `$request->getUser()` null returns — narrow or guard correctly. `app/installer/Controller/*` similarly. `app/console/Commands/*` — null-safety on `InputInterface::getOption()` / `getArgument()` return values (typed `mixed` by Symfony; if the command declares the option/argument as required, the value is non-null and a cast is safe — Verifier confirms the option/argument is declared required in `configure()`). Per-step Tester runs scoped L7 on these three trees and asserts zero errors. Commit: `refactor(types): null safety for app/system/src, app/installer, app/console`.
  13. **Null safety — `packages/pagekit/{blog,theme-one}`.** Same §3.1 decision tree. `Blog\Model\Post::$user` / `$comments` were typed `?User` / `?array` in 2.1.4 §14; L7 will surface consumers that miss a null check. `Blog\Controller\*` action methods returning `array|Response` — narrow where possible. `theme-one/functions.php` `attrs()`/`image()`/`bgImage()` — these already received parameter types in 2.1.4 §15; L7 may fire `argument.type` on internal `htmlspecialchars(null)` style calls — narrow the parameter to `string` (not `?string`) if the helper does not accept null. Per-step Tester runs scoped L7 on these two trees and asserts zero errors. Commit: `refactor(types): null safety for blog and theme-one packages`.
  14. **Bump PHPStan to Level 7 + final baseline cleanup + null-guard sanity audit.**
      - Edit `phpstan.neon`: `level: 6` → `level: 7`. No other config edits.
      - Run `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M`. For every `Ignored error pattern ... was not matched in reported errors` warning, surgically remove that single entry from `phpstan-baseline.neon` (the `-` block including `message`/`identifier`/`count`/`path` keys). Do **not** run `--generate-baseline`. Final state: `git diff phpstan-baseline.neon` shows removals only (or zero changes); `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` exits 0 at Level 7.
      - **Null-guard sanity audit (Verifier-led).** Run `git diff --diff-filter=AM HEAD~13..HEAD --unified=0 -- '*.php' | rg -nU '^\+.*\b!==\s*null\b'` and review every newly introduced `!== null` guard against the corresponding property/parameter declaration. For each match, the Verifier confirms:
        - the property/parameter declaration is `?Type` (genuinely nullable), **not** a non-nullable type where the guard is dead-code defensive programming;
        - if the declaration is `?Type`, runtime data flow can plausibly hit the null branch (i.e. the guard isn't load-bearing only to silence PHPStan).
        Reject any guard that fails either check and force the Refactorer to fix the type instead. Same audit applies to `?->` nullsafe operator additions and `if ($x === null) { return ...; }` early-returns.
      - **Final sanity check:** `grep -rn '\(private\|protected\|public\) \$[a-zA-Z_]' app/ packages/ | grep -v 'app/vendor' | grep -v 'packages/composer' | wc -l` returns `0` (no untyped properties remain in analyzed code).
      Commit: `chore(phpstan): raise level to 7 (null safety) + final baseline cleanup`.

## TESTING STRATEGY
- **Per step (Tester subagent):** PHPUnit + PHPStan (mandatory after every Checklist Step).
  - **Steps 1–13** run PHPStan at the **config-defined level (6)** — exit 0 required, plus `git diff --quiet phpstan-baseline.neon` (no baseline additions and no `count: N → N+1` increments). Surgical removals are permitted on Step 1 (stale entries on `develop` HEAD), and on Steps 2–13 for any entries that become unmatched as a direct consequence of the step's source fixes (same surgical-removal precedent as Step 2.1.4 amendments #1/#2/#3).
  - **Steps 2–13** additionally run a **scoped Level-7 check**: `./app/vendor/bin/phpstan analyse --level=7 --no-progress --memory-limit=512M <step's directory(s)>` and assert `0 errors` (or, where the standing `trait.unused` exemption applies — `app/modules/database/src/ORM/ModelTrait.php` and `PropertyTrait.php` in Step 5 — that the only remaining errors are exactly those 2 `trait.unused` scoping artifacts). The Tester must NOT run a global L7 sweep on intermediate steps — that fails until Step 14. Document the scoped-paths command for each step in the Tester's per-step run log.
  - **Step 14** runs the **global Level-7 check**: `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` (now under `level: 7`) and asserts exit 0. Tester also runs the `grep -rn '\(private\|protected\|public\) \$[a-zA-Z_]'` sanity check and asserts count `0`.
  - **Null-guard audit (Verifier-led, Step 14):** as described in Checklist Step 14 — every newly introduced `!== null` / `?->` / null-early-return must be justifiable against the corresponding declaration. Same gating role the Verifier had for the Step 2.1.4 Bugbot-flagged `readonly mixed $` sweep.
  - **PHPUnit** runs unconditionally on every step; the suite must stay green throughout.
- **Final run (after Early Push, Tester subagent):** wait on the four PHP Quality CI jobs (`phpunit (8.2)`, `phpunit (8.3)`, `phpstan`, `cs-fixer`, `security-audit`) via `gh run watch` and run the 3 Playwright E2E specs locally **in parallel**; both must pass. See `.cursor/agents/tester.md` § End-of-ticket tests for the exact commands and `.cursor/rules/orchestrator-subagent-workflow.mdc` § Final Test for the workflow position.
