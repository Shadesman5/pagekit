# Ticket: Step 2.4 — Build Tools Modernization (pnpm + Vite)

## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.4 — Build Tools (pnpm + Vite). GitHub Issue #159. Branch `feature/build-tools-pnpm-vite` (from `develop`). Replaces Yarn 1 + Webpack 4 + Gulp with pnpm + Vite in one PR; includes the user-approved minimal Vue 2.6.12 → 2.7.16 bump. **Runtime contract frozen:** every bundle keeps its exact `<module>/app/bundle/<name>.js` path, LESS keeps its `css/` output paths, asset copies keep the `app/assets/` layout, PHP views stay untouched, classic scripts + `Vue`/`UIkit`/`UIkit.util` globals stay.
- **Scope:**
  - **Package layer:** `package.json` (deps, scripts, `packageManager`, `engines`, `preinstall` guard), `yarn.lock` → `pnpm-lock.yaml`, `pnpm-workspace.yaml` (new — pnpm build-approval config), `app/scripts/checkYarn.js` (delete), `.gitattributes` (line 56 `yarn.lock text eol=lf` → `pnpm-lock.yaml`).
  - **Build configs:** root `webpack.config.js` + all 17 module `webpack.config.js` (delete), `.babelrc` (delete), new Vite build orchestration (root `vite.config.*` and/or `scripts/*.mjs` — see decision 4), `gulpfile.js` (delete), new `scripts/` ports for LESS/assets/CLDR.
  - **Standalone package toolchains (delete):** `packages/pagekit/theme-one/gulpfile.js`, `packages/pagekit/theme-one/package.json`, `packages/pagekit/blog/package.json` (dead marketplace-era `archive`/`install` scripts on webpack-4/gulp deps; root pipeline is the single build path). `packages/pagekit/theme-one/css/theme.css` (tracked) is regenerated by the new pipeline and committed.
  - **Source import sites (webpack-loader dependents):** `app/system/modules/widget/app/components/template-settings.js`, `app/system/modules/site/app/components/template-settings.js`, `app/system/modules/finder/app/components/panel-finder.vue` (2 imports) — `.html` imports → `?raw`. (Verified: these 4 are the only `.html` import sites; **zero** `.json` import sites and **zero** SFC `<style>` blocks exist — `json-loader`/css-loader rules are vestigial.)
  - **PHP (single file):** `app/console/src/Commands/BuildCommand.php` — the `exec('node_modules/.bin/webpack -p')` / `exec('yarn compile-js …')` WIN/else split becomes one package-manager-agnostic `exec('node scripts/…')` call (release-tooling shell-out only; no runtime behavior change; no other PHP changes anywhere).
  - **Lint/format:** `.eslintrc` → `eslint.config.js` (flat), `.eslintignore` (delete — folded into flat `ignores`), `.prettierrc` (stays as-is), `.git-blame-ignore-revs` (new), one-time tree format of `.js`/`.vue` in lint scope.
  - **CI (4 files, job names frozen):** `.github/workflows/frontend.yml` (job `frontend`), `e2e.yml` (jobs `e2e-smoke`, `e2e-merge`), `nightly.yml` (job `e2e-viewports` — `guard`/`infection-full` untouched), `e2e-weekly.yml` (job `e2e-sweep` — `guard` untouched).
  - **Agent env / Docker / docs / rules:** `.cursor/Dockerfile` (yarn → pnpm install), `.cursor/install.sh`, `.cursor/modernize-helper.sh`, `.cursor/README.md`, `AGENTS.md`, `README.md` (~23 yarn/webpack/gulp lines), `docker-compose.yml` (`node` service command only), `.cursor/rules/pagekit-files.mdc` + `.cursor/rules/pagekit-standards.mdc` (description texts), `tests/e2e/README.md` (`npm install` lines → pnpm).
  - **Committed verification artifacts:** `migration-docs/branches/phase-2/step-2-4-build-inventory-webpack.txt`, `…-build-inventory-vite.txt`, `…-audit-yarn-before.txt`, `…-audit-pnpm-after.txt` (see decision 2).
  - **No changes:** PHP views/templates, runtime PHP under `app/`/`packages/` (except the BuildCommand exec line), `playwright.config.js` + spec bodies, root `Dockerfile`, `docker/`, `docs-site/` (verified: zero yarn/webpack/gulp refs), `.github/dependabot.yml` (npm ecosystem covers `pnpm-lock.yaml` natively; vue-major ignore rules stay valid).
- **Deferred:**
  - **Step 3.2 (Vue 2.7 Bridge)** — Composition-API trials, deprecation-warning analysis on the 2.7 runtime. PHASE_3 §3.2 already carries the pull-forward note — no amendment needed.
  - **Step 3.3.3 (Vue 3 Core)** — swap `@vitejs/plugin-vue2` → `@vitejs/plugin-vue` + lift the Vite 7 major pin (plugin is archived, peer range caps at Vite 7 — decision 1); ES-module/script-tag delivery redesign + Vite dev server/HMR-PHP integration. **PHASE_3 §3.3.3 amended in this plan** (same commit).
  - **Steps 3.3.1 / 3.3.2 / 3.3.5** — vue-resource → axios, vue-event-manager → mitt, vue-intl rewrite, lodash removal. PHASE_3 already covers — no amendment needed.
  - **Step 3.1 (UIkit 3.5 → 3.21)**, **Step 3.4 (TypeScript on the Vite pipeline)** — PHASE_3 already covers — no amendment needed.
  - **Non-goals:** Webpack 5 / Yarn Berry (dead evaluation branches), TinyMCE 6+ (stays ~5.10.9), Vue 3 syntax of any kind, coverage floors/Infection/PHP tooling (untouched).
- **Bridges:** None. Every change is a delete or a replacement with no parallel old/new path; no `TEMPORARY BRIDGE` tags. The Vite-7 pin is toolchain config tracked via ROADMAP/PHASE_3 — **no forward TODO tag in code** (per task prompt). `BuildCommand.php`'s existing Step 5.6 TODO stays untouched.
- **⚠️ Architect decisions + verified discovery (Refactorer MUST heed):**
  1. **Vite pin = latest 7.x; `@vitejs/plugin-vue2@^2.3.4`.** Verified 2026-07-26: the plugin repo is **archived** ("Vue 2 has reached EOL"); latest release 2.3.4 (2025-10-04) has peer `vite: ^3 || ^4 || ^5 || ^6 || ^7`, `vue: ^2.7.0-0`. The task prompt's "Vite-8 support merged Apr 2026" is **wrong** — PRs #118/#119 were never merged/released (repo archived, issue #117 locked). Pin the newest released Vite 7.x (Vite 7 = previous-major support line, still receives security backports; requires Node `^20.19.0 || >=22.12.0`). Never use `--legacy-peer-deps`-style overrides to force Vite 8. Revisit lands with Step 3.3.3 (PHASE amended).
  2. **Baseline artifacts are committed, not left in `/tmp`.** V2 Conductor batches checklist steps across separate cloud agents — `/tmp` does not survive between steps. The webpack ground-truth inventory and the before-audit go to `migration-docs/branches/phase-2/step-2-4-*.txt` in Step 1's commit; Step 9 diffs against them. Inventory command (extends the task prompt's globs with the **editor asset copies**, which are part of the contract):
     `git ls-files --others --ignored --exclude-standard -- '**/app/bundle/*' 'app/assets/*' 'app/system/modules/editor/app/assets/*' 'app/installer/assets/css/*' 'app/system/modules/theme/assets/css/*' | sort`
     Note: `packages/pagekit/theme-one/css/theme.css` is **tracked** (not in the ignored inventory) — its parity check is `git diff` after regeneration.
  3. **Bundle contract details (verified against all 17 configs):** 56 entries total. Externals `vue`/`uikit`/`uikit-util` → globals `Vue`/`UIkit`/`UIkit.util`; aliases `@installer` → `app/installer`, `@system` → `app/system` (used across app + packages). Five entries carry webpack `output.library` names — debug `debugbar`→`Debugbar`, editor `editor`→`Editor`, captcha `captcha-interceptor`→`Captcha`, finder `panel-finder`+`link-storage`→`Finder`, site `panel-link`→`Links`. Verified: **nothing consumes these window globals** (components self-register via `Vue.component(...)` side effects; PHP loads bundles via `$view->script(...)` with dependency names) — still, set the Rollup/IIFE `output.name` for these five entries for exact parity (free, avoids subtle regressions). No dynamic `import()` anywhere → per-entry classic builds are safe.
  4. **Vite mechanism (one mechanism for all 17 modules):** a single **entry manifest** (module dir → `{ entryName: srcPath, iifeName? }`) replaces the 17 `webpack.config.js` files; one Node orchestrator (`scripts/` + optional thin root `vite.config`) drives **one `vite build` per entry** via the Vite JS API (Rollup rejects multi-input `iife`/code-splitting — per-entry builds are the constraint-correct shape), with bounded concurrency and a `--watch` flag (Vite watch API) replacing `watch-js`. Per-build: `format: 'iife'`, self-contained (only the three externals above), `outDir` = module dir, `entryFileNames` → `app/bundle/<name>.js`, **`sourcemap: false`** (webpack-4 prod parity; `.map` files are not gitignored and must not appear), minified, `define: { 'process.env.NODE_ENV': …}` (webpack `mode` used to provide this — classic scripts have no `process`), esbuild **`target: 'es2017'`** (matches the UIkit 3.5 browser floor: Chrome 61+/FF 60+/Edge 16+/Safari 11.1+ — Safari 11.1 lacks full ES2018 regex features; record in branch doc). `.vue` via `@vitejs/plugin-vue2`; JSON imports native; the 4 `.html` imports become `?raw`. Assert no stray `.css` asset is emitted (zero SFC style blocks exist).
  5. **pnpm pin = latest stable 11.x** via `packageManager` (pnpm 11 = current stable major as of 2026-07; pnpm 12 is alpha/`next-12` only — do not use; pnpm 10 is previous-major). pnpm 11 moved build-script approval to the **`allowBuilds` map in `pnpm-workspace.yaml`** (successor of `pnpm.onlyBuiltDependencies` named in the task prompt — use the pnpm-11 mechanism): approve `esbuild` plus whatever `pnpm install` surfaces (git-hosted deps may request builds; approve only what is needed, never blanket-allow). `resolutions` does **not** migrate blindly: `source-map-resolve` entered via the gulp chain (deleted) and `chokidar` becomes a direct devDependency (decision 7) — expect **no `overrides` at all**; verify against the generated lockfile. Verify the three GitHub-fork deps (`Codemirror`, `JSONStorage`, `vue-intl` — uatrend forks, direct deps) resolve under pnpm 11 defaults. `engines`: `node: "^20.19.0 || >=22.12.0"` (Vite 7 floor; CI's Node 22 and `node:22-alpine` satisfy it), drop the `yarn` entry. Yarn guard `app/scripts/checkYarn.js` is deleted; `preinstall: "npx only-allow pnpm"` replaces it. pnpm strict linking: if any survivor breaks on an undeclared import, **add the missing dependency — never `shamefully-hoist`**.
  6. **Install-time auto-build: DECOUPLE (decision).** The `install` lifecycle hook is deleted; `pnpm install` installs, **`pnpm build`** builds (JS + CSS + asset copies). Rationale: explicit beats implicit; yarn-1 root-`install`-hook semantics do not translate cleanly to pnpm's script policy, and the hook was already only a convenience the CI comments had to explain. Applied consistently at every reliance site: all 4 workflows get/keep an explicit build step, `.cursor/install.sh` adds `pnpm build`, `AGENTS.md` caveat is rewritten (the "yarn install triggers a full production build" note dies), README quickstart becomes `pnpm install && pnpm build`, compose `node` service runs `pnpm install && pnpm build && pnpm watch`.
  7. **Script surface (target state):** `build` (JS + LESS + assets — what CI and `php pagekit build` call), `build:js` / `build:css` / `build:assets` (individually invokable), `watch` (single orchestrator watching JS via Vite watch API + LESS via chokidar — replaces `watch-js`/`watch-less`/`watch-all`, deleting `npm-run-all` and the `lint-watch`/`eslint-watch` pair), `cldr`, `lint`, and the unchanged `test:e2e*`/`test:smoke*` family. `chokidar` moves `dependencies` → `devDependencies` (its only consumer is the watch script; it was never a runtime dep). `lodash` **stays** in `dependencies` (runtime asset source for the `_` global — `app/assets/lodash/dist/lodash(.min).js` is script-loaded by `app/system/modules/view/index.php`), as do `vue`, `uikit`, `flatpickr`, `tinymce`, `marked`, `Codemirror` (asset-copy sources) and the bundled imports (`blueimp-md5`, `ismobilejs`, `JSONStorage`, `vee-validate`, `vue-nestable`, `vue-intl`, `vue-event-manager`, `vue-resource`). `dotenv` stays (consumed by `playwright.config.js`). Delete with their chains: `webpack`, `webpack-cli`, `babel-loader`, `@babel/*`, `vue-loader`, `vue-style-loader`, `vue-template-compiler`, `vue-hot-reload-api`, `css-loader`, `style-loader`, `file-loader`, `html-loader`, `json-loader`, `less-loader`, `glob`, `minimist`, `gulp`, `gulp-*`, `merge-stream`, `npm-run-all`, `eslint-watch`, `eslint-webpack-plugin`, `babel-eslint`, `eslint-config-airbnb-base`, `eslint-plugin-import`. `less` and `cldr-core`/`cldr-localenames-modern` stay (consumed by the new scripts).
  8. **LESS pipeline = 3 roots (one pipeline, no parallel tooling):** `app/installer/assets/less/installer.less` → `assets/css/installer.css` and `app/system/modules/theme/assets/less/theme.less` → `assets/css/theme.css` (both gitignored outputs, glob parity `<root>**/less/*.less` with the `less/`→`css/` dirname rename), **plus** `packages/pagekit/theme-one/less/theme.less` → `css/theme.css` (tracked output; folding theme-one in replaces its deleted standalone gulpfile). Options parity: `compress: true`, `relativeUrls: true`. **Banner fix:** installer/system-theme banner sources the version from `app/system/config.php` (the SSoT — regex `'version' => '…'` like `.github/scripts/check-version-ssot.php` does) with title "Pagekit", or drops the version — never `undefined`; theme-one keeps its banner data from its own `composer.json` (title "One", version, MIT — all present, verified). theme-one's private uikit copy (`theme-one/app/assets/`) has **no consumer** (views load only `theme:css/theme.css` + `theme:js/theme.js`, both tracked) — not ported.
  9. **Asset-copy port (identical layout):** `uikit`→`app/assets/uikit/`, `vue`→`app/assets/vue/`, `flatpickr`→`app/assets/flatpickr/`, `lodash` (`lodash*.js` files)→`app/assets/lodash/dist/`; `tinymce`→`…/editor/app/assets/tinymce/`, `marked`→`…/editor/app/assets/marked/`, `codemirror` (npm package name **`Codemirror`**, case-sensitive on Linux)→`…/editor/app/assets/codemirror/`. The committed `tinymce_skin` dir lives beside the copies (gitignore exception) — copies must not delete it. Runtime loads **both** debug and min variants (`vue.js`/`vue.min.js`, `uikit.js`/`uikit.min.js`, `lodash.js`/`lodash.min.js` — verified in `view/index.php`) → copy whole dists as today.
  10. **CLDR port fixes TWO bugs:** (a) the literal-path template bug (`` `{cldr.languages}${src}/formats.json` `` missing `$`); (b) the **dead formats source** — gulp reads `app/assets/vue-intl/dist/locales/`, which never exists (the assets task never copied vue-intl), so the formats pass has silently no-oped for years. New source: `node_modules/vue-intl/dist/locales/` (**verified 2026-07-26 via GitHub API: the uatrend fork ships `dist/locales/*.json`, lowercase names** — matching the `id.toLowerCase()` lookup). After porting, run it once and review `git diff --stat app/system/languages/ app/system/modules/intl/data/`: commit if the diff is empty/plausible (formats.json files may legitimately change now that the source path works); investigate before committing if it is large or structurally surprising.
  11. **ESLint pin = ESLint 10 flat config (recorded deviation from the prompt's "ESLint 9"):** ESLint 9 hits **EOL 2026-08-06** (verified) — pinning 9 would ship a dead major within days. `eslint-plugin-vue@^10` (≥10.8 peers ESLint 10; `flat/vue2-recommended` verified present in v10.x) + `vue-eslint-parser@^10.3` + `@eslint/js` recommended as the base (airbnb-base has no flat support — do not chase parity rule-by-rule) + espree (default parser; babel syntax is gone with Babel). Port the existing custom rules **except** pure-formatting ones (decision 12) and the script-tag globals (`_`, `Vue`, `UIkit`, `Theme`, `marked`, `flatpickr`, `grecaptcha`, `$pagekit`, `$editor`, `CodeMirror`, `tinyMCE`); port `.eslintignore` (incl. `**/theme/**/theme.js`, `**/theme/**/components/**`, bundles/assets/vendor) into flat `ignores` and delete the file. Keep `eslint-config-prettier` (flat-compatible; bump major as needed) as the last config entry.
  12. **Formatting policy: (a) format-once + blocking (decision, forwarded from Step 2.2).** Prettier 3.9.6 + the existing `.prettierrc` (unchanged; `tabWidth: 2` agrees with `.editorconfig`'s js/vue `indent_size = 2` — the tree is largely 4-space, so expect a very large one-shot diff; that is the point) formats the lint scope (`.js`/`.vue` outside bundles/assets/vendor/node_modules). ESLint drops the stylistic rules Prettier now owns (`indent`, `quotes`, `comma-dangle`, `arrow-body-style`, `operator-assignment`, `object-curly-newline`, `vue/html-indent`, `vue/max-attributes-per-line`; `eslint-config-prettier` guards the rest). After formatting, `pnpm lint` must exit **0** — remaining correctness findings are fixed, or a chronically-violated opinion rule is consciously disabled with a one-line justification in the config (never inline disables sprayed across files). The ~13k-violation advisory limbo **dies in this ticket**: CI's ESLint and Prettier checks both become **blocking full-tree** (supersedes the diff-scoped advisory mode). New `.git-blame-ignore-revs` lists the format commit; because the Orchestrator commits after the step, the **hash is appended in the next checklist step** (Step 7 resolves it via `git log` — explicitly planned, not an oversight).
  13. **`php pagekit build` (BuildCommand):** replace the WIN/else `exec()` split with one cross-platform `exec('node scripts/<js-build-entry>')` (package-manager-agnostic — survives any future PM change; behavior parity: the command only ever built JS bundles, not CSS). This is the ticket's **only** PHP change.
  14. **CI job names frozen:** `frontend`, `e2e-smoke`, `e2e-merge`, `e2e-viewports`, `e2e-sweep` (required-check continuity — Step 2.2 lesson). Only steps inside them change: pnpm setup (`pnpm/action-setup` pinned by **commit SHA** — repo convention — or corepack activation; either must resolve the version from `packageManager`), `cache: pnpm` in setup-node, `pnpm install --frozen-lockfile`, explicit `pnpm build` (decision 6). `frontend` gate: build blocking (as today) + `pnpm lint` blocking + `pnpm exec prettier --check .` blocking (decision 12); the diff-scoping machinery (fetch-depth 0, base-ref fetch, changed-files step) is deleted with the advisory mode. E2E-related workflows keep PHP/Playwright steps untouched.
  15. **Agent Dockerfile + install.sh pnpm mechanism:** `.cursor/Dockerfile` uses Debian's `nodejs`/`npm` (php:8.5-cli base) — Debian's node ships **without corepack**; replace `RUN npm install -g yarn@1.22` with `RUN npm install -g pnpm@<pinned>` (same pattern, deterministic, no corepack key-rotation risk in image builds). `.cursor/install.sh` additionally gets an **idempotent pnpm bootstrap guard** (`command -v pnpm || sudo npm install -g pnpm@<pinned>`, same pattern as its existing rg/jq/composer guards) — running cloud agents boot from the **old snapshot** (yarn baked, no pnpm) until the environment is rebuilt, and install.sh is the real source of truth on cold boot. Human docs (README) document the corepack path (`corepack enable`) as primary with `npm install -g pnpm` as fallback. `docker-compose.yml` `node` service (`node:22-alpine`, ships corepack): `sh -c "corepack enable && pnpm install && pnpm build && pnpm watch"`.
  16. **Baseline verified in plan phase:** repo at `develop`-merged state (PR #242); PHP server runs; **`node_modules/` is not installed in fresh VMs** — Step 1 installs and builds before recording the baseline. PHPUnit/PHPStan are the untouchable green gates on every step (this ticket changes no runtime PHP).
- **Checklist:**
  1. **Baseline green + committed ground truth.** Run the safety gates: `composer install`, `./app/vendor/bin/phpunit`, `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M`, `yarn install --frozen-lockfile`, `yarn compile-js --mode=production`, `yarn gulp`. Record + commit `migration-docs/branches/phase-2/step-2-4-build-inventory-webpack.txt` (inventory command, decision 2) and `step-2-4-audit-yarn-before.txt` (`yarn audit --level moderate … || true`). Any red gate → stop and report (pre-existing failure), do not touch the toolchain.
  2. **Vue 2.6.12 → 2.7.16 (minimal bump, still webpack/yarn).** `vue` → `~2.7.16`; delete `vue-template-compiler` (2.7 ships its own compiler; `vue-loader` 15.11 auto-detects). Rebuild (`yarn install && yarn compile-js --mode=production && yarn gulp`); the copied `app/assets/vue/` dist becomes 2.7. Gate: local E2E smoke (`yarn test:smoke` — the 3 `@ci` specs, chromium-desktop; yarn is still the runner in this step) passes on Vue 2.7 **before** any pipeline change so regressions stay attributable. Scope guard: no Composition API, no code changes beyond what the bump forces; plugin stack (`vue-resource`, `vue-event-manager`, `vue-intl` fork, `vee-validate` 3, `vue-nestable`) is exercised by the smoke specs.
  3. **Vite JS pipeline — delete webpack + Babel.** Implement decision 4 (entry manifest + per-entry IIFE builds, externals/aliases/paths/library-name parity, `?raw` at the 4 `.html` sites). Point `compile-js`/`watch-js` scripts at the new mechanism (yarn still the runner this step). Swap the `BuildCommand.php` exec line (decision 13). Delete: root + 17 module `webpack.config.js`, `.babelrc`, and every webpack/babel/loader dep (decision 7 list, lockfile regenerated). Gate: build completes; bundle-path set (`**/app/bundle/*` slice of the inventory) identical to baseline; no `.css`/`.map` emitted; spot-check one library entry (e.g. finder) exposes its IIFE name and self-registers; PHPUnit + PHPStan green (BuildCommand touched).
  4. **LESS + assets + CLDR scripts — delete Gulp (incl. standalone package toolchains).** Port per decisions 8–10 into `scripts/` (Node, ESM); wire `build` = js + css + assets and the single `watch` orchestrator (decision 7); `install` lifecycle hook temporarily points at `yarn build` (parity until Step 5 decouples); `chokidar` → devDependencies. Delete `gulpfile.js`, all `gulp-*` + `merge-stream` + `npm-run-all` deps, `packages/pagekit/theme-one/gulpfile.js`, `packages/pagekit/theme-one/package.json`, `packages/pagekit/blog/package.json`. Regenerate + commit `packages/pagekit/theme-one/css/theme.css`; run the ported `cldr` once and handle the diff per decision 10. Gate: `yarn build` from clean produces the full inventory (bundles + 2 ignored css outputs + asset copies incl. editor assets); banners show real title/version (grep `-c 'undefined'` = 0 in generated css headers).
  5. **pnpm swap — delete Yarn.** Implement decisions 5 + 6: `packageManager` (pnpm 11 latest), engines, `preinstall: npx only-allow pnpm`, delete `checkYarn.js` + `yarn.lock` + `resolutions`, commit `pnpm-lock.yaml` + `pnpm-workspace.yaml` (allowBuilds: esbuild + install-surfaced needs), delete the `install` lifecycle hook (decoupling), rename any remaining `yarn`-flavored script phrasing, `.gitattributes` line swap. Verify the three GitHub forks resolve; verify `esbuild` postinstall ran (`node_modules/.bin/esbuild --version`). Gate: from wiped `node_modules`: `pnpm install --frozen-lockfile && pnpm build` reproduces the inventory; the preinstall guard demonstrably blocks a non-pnpm install (verify without leaving artifacts); PHPUnit + PHPStan green.
  6. **ESLint 10 flat config + format-once.** Implement decisions 11 + 12: `eslint.config.js`, dep swaps (eslint@^10, eslint-plugin-vue@^10, vue-eslint-parser@^10.3, @eslint/js; delete airbnb/babel-eslint/eslint-plugin-import/eslint-watch/eslint-webpack-plugin/gulp-eslint remnants), delete `.eslintrc` + `.eslintignore`, drop `lint-watch`; run Prettier once over the lint scope; fix/justify residual lint findings; create `.git-blame-ignore-revs` (hash appended next step). Gate: `pnpm lint` exit 0; `pnpm exec prettier --check .` exit 0; `pnpm build` still green (build scripts are in lint scope — write them clean).
  7. **CI workflows (4 files, job names frozen).** Implement decision 14 on `frontend.yml`, `e2e.yml`, `nightly.yml`, `e2e-weekly.yml`; update the install-hook comments to the explicit-build reality; pin any new action by commit SHA; append the Step-6 format-commit hash (via `git log`) to `.git-blame-ignore-revs`. Gate: YAML parses (`python3 -c 'import yaml…'` per file); `rg -n 'yarn' .github/workflows/` returns nothing; job-name grep confirms `frontend`/`e2e-smoke`/`e2e-merge`/`e2e-viewports`/`e2e-sweep` unchanged.
  8. **Agent env + Docker + docs + rules.** Implement decision 15 (`.cursor/Dockerfile`, compose `node` service); `.cursor/install.sh` (pnpm bootstrap guard + `pnpm install --frozen-lockfile` + `pnpm build`, comment updates), `.cursor/modernize-helper.sh` (`pnpm watch`), `.cursor/README.md`; `AGENTS.md` (services table rows: pnpm build/lint/watch; delete the install-hook caveat; replace the "~13k ESLint errors" caveat with the blocking-lint reality; Playwright rows untouched); `README.md` (requirements: Node `^20.19.0 || >=22.12.0` + pnpm via corepack, quickstart, command reference — zero yarn/webpack/gulp remnants); rule texts in `pagekit-files.mdc`/`pagekit-standards.mdc` (`yarn compile-js` → `pnpm build`); `tests/e2e/README.md` npm-install lines → pnpm. Gate: `rg -in 'yarn|webpack|gulp' --hidden -g '!.git' -g '!node_modules'` returns hits **only** in the allow-list: `migration-docs/branches/**`, `migration-docs/pull-requests/**`, `migration-docs/audits/**`, `migration-docs/TODO/**` (incl. this ticket's task prompt + PHASE history), `migration-docs/tickets/**`, `CHANGELOG*.md`, `.cursor/BUGBOT.md` (historical PR table), `app/vendor/**`, committed third-party editor assets (`tinymce_skin`), and generated-language/data files if any. Any other hit = missed scrub, FAIL.
  9. **Verification — inventory + audit parity.** Wipe ignored build outputs only (`git clean -Xdf -- '**/app/bundle' 'app/assets' 'app/system/modules/editor/app/assets' 'app/installer/assets/css' 'app/system/modules/theme/assets/css'` — never touch tracked files; `tinymce_skin` is tracked and must survive) + wipe `node_modules`; `pnpm install --frozen-lockfile && pnpm build`; regenerate the inventory → commit `step-2-4-build-inventory-vite.txt`; **path set must be identical** to Step 1's baseline (investigate every delta). `pnpm audit > …/step-2-4-audit-pnpm-after.txt || true`; verify the webpack-transitive advisories (picomatch, braces, micromatch, serialize-javascript, elliptic) are gone; commit. Gate: inventory diff empty; PHPUnit + PHPStan green; local E2E smoke (3 `@ci` specs, chromium-desktop) green on the final pipeline (doubles as the end-of-ticket E2E trigger).

## EXECUTION STATE
<!-- Machine-readable progress index for the Orchestrator/Conductor. Mirrors the Checklist 1:1
     (same numbers + short titles). Size hint per step: S = small/atomic, M = medium,
     L = large or loop-risk. A step orchestrator flips its box to [x] in the SAME commit as that
     step's code + tests (after full step PASS incl. test-writer when applicable) -->
- [x] Step 1 (M) — Baseline green + committed webpack inventory/audit ground truth
- [x] Step 2 (M) — Vue 2.6.12 → 2.7.16 under webpack; E2E smoke on 2.7
- [x] Step 3 (L) — Vite JS pipeline (entry manifest, per-entry IIFE); delete webpack+Babel; BuildCommand exec swap
- [x] Step 4 (M) — LESS/assets/CLDR node scripts; delete gulp + standalone package toolchains; wire `build`
- [x] Step 5 (L) — pnpm swap: packageManager pin, only-allow guard, lockfile, decouple install-build; delete yarn
- [x] Step 6 (L) — ESLint 10 flat config + format-once Prettier; lint green
- [x] Step 7 (M) — CI: 4 workflows on pnpm + explicit build; blocking lint/format; job names frozen
- [x] Step 8 (M) — Agent env + Docker + docs + rules on pnpm/Vite; zero-reference grep gate
- [x] Step 9 (L) — Verification: clean rebuild, inventory parity, audit diff, E2E smoke

## TESTING STRATEGY
- **Per step (production gate):** Refactorer → Verifier → Tester (PHPUnit + PHPStan) — both must stay green untouched on every step (only Step 3 touches PHP at all: one exec line in `BuildCommand.php`; any other PHP regression = out-of-scope drift).
- **Per step (coverage — inline light):** **skip on ALL steps** (`test-writer: skip`). Steps 1–2, 4–9 change no production PHP under `app/`/`packages/`. Step 3's sole PHP delta is a one-line `exec()` string swap in a release-tooling console command with no testable seam (no existing BuildCommand test; an exec-mock test would be noise) — skip with this recorded reason.
- **Per step notes (extra Tester gates beyond PHPUnit + PHPStan):**
  - Step 1: all baseline commands exit green; both snapshot files exist, non-empty, committed.
  - Step 2: production build completes; local E2E smoke (3 `@ci` specs, chromium-desktop) PASS on Vue 2.7 — mandatory before Step 3.
  - Step 3: JS build completes; bundle-path slice of the inventory identical; `rg --files -g '*.map' -g '*.css'` over `**/app/bundle/` returns nothing; PHPStan/PHPUnit cover the BuildCommand touch.
  - Step 4: `yarn build` from clean reproduces the full inventory (bundles + css + asset copies); generated css banners contain no `undefined`; `git status` shows only expected tracked-file changes (theme-one css, languages data if the cldr diff was accepted).
  - Step 5: `pnpm install --frozen-lockfile && pnpm build` from wiped `node_modules` green; only-allow rejects a non-pnpm install; fork deps present in `pnpm-lock.yaml`.
  - Step 6: `pnpm lint` exit 0; `pnpm exec prettier --check .` exit 0; `pnpm build` green.
  - Step 7: YAML sanity-parse all 4 workflows; zero `yarn` matches under `.github/workflows/`; frozen job names verified verbatim.
  - Step 8: zero-reference grep gate with the Step-8 allow-list; every documented command names only scripts that exist in `package.json`.
  - Step 9: inventory diff vs baseline empty; after-audit committed and free of the five named webpack transitives; local E2E smoke PASS.
  - Branch doc records which gates ran per step and the three recorded decisions (Vite 7 pin rationale, install-decouple, format-once) — PASS/FAIL prose, no metric tables.
- **E2E (Execute — last checklist step only):** when ticking Step 9 completes every `## EXECUTION STATE` box, the Orchestrator delegates `"final E2E run"` to Tester (3 Playwright specs, local PASS/FAIL — Step 9's own smoke gate satisfies the same commands; run once, not twice, if the step gate already passed on the final tree). **Not** gated on PR/CI — see `.cursor/agents/tester.md` § End-of-ticket E2E.
- **Finalize:** Orchestrator opens PR → waits on the PR checks (`gh pr checks <pr> --watch` — the reworked `frontend` + `e2e-smoke` + PHP gates ARE the real CI proof) → Bugbot → version/CHANGELOG/ROADMAP. One version bump at Finalize only.
- **Manual Work list (branch doc "Deferred / Out-of-Scope" — user actions, agents do NOT perform):**
  1. Required-check sanity after the PR: `frontend` and the E2E smoke check must still report under their old names; realign the Ruleset (admin rights) if anything was renamed despite the freeze.
  2. Local dev machines: one-time `corepack enable` (or `npm install -g pnpm`), delete local `node_modules/`, run `pnpm install && pnpm build`; remove stale yarn artifacts (global yarn cache optional).
  3. Format-once follow-up (policy (a)): per-clone `git config blame.ignoreRevsFile .git-blame-ignore-revs`.
  4. Rebuild the cloud-agent environment snapshot so the `.cursor/Dockerfile` pnpm change takes effect (running agents use the pinned snapshot until then — no risk: `install.sh`'s idempotent pnpm bootstrap guard from decision 15 keeps cold boots self-sufficient).
