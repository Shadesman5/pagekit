## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.1.4 (PHPStan Level 5→6 — Return Types). Closes Phase 1 audit Step 1.1 (Mailer Migration) ⚠️ → 🛡️ via the Mail-related fixes in Checklist Step 7.
- **Scope:**
  - `phpstan.neon`: bump `level: 5` → `level: 6` (final step), add **two** Composer-generated autoload scaffolding entries to `excludePaths` — `packages/autoload.php` and `packages/composer` (same exclusion pattern as `app/vendor` / `*/node_modules/*`). Both files are `@generated by Composer` (see header comment in `packages/autoload.php` line 5) and form one coherent unit: `packages/autoload.php` is a single-line wrapper that returns `ComposerAutoloaderInit{hash}::getLoader()`, and that class lives in `packages/composer/autoload_real.php`. Excluding only `packages/composer` while keeping `packages/autoload.php` analyzable produces a new `class.notFound` error on `packages/autoload.php:9` — the class becomes unresolvable. The caller in `/workspace/autoload.php` (project root) is still analyzed and already guards with `file_exists(__DIR__ . '/packages/autoload.php')`, so excluding `packages/autoload.php` does not remove coverage of any consumer.
  - `phpstan-baseline.neon`: surgical removals only (the two `packages/composer/*` entries; plus any L5 entries that become unmatched as a side effect of return-type fixes). **Never** run `--generate-baseline`.
  - **Return-type / parameter-type / iterable-value / property-type / generics** sweep across ~1493 PHPStan-Level-6 errors discovered on the current `develop` HEAD: `app/modules` 936, `app/system` 339, `app/installer` 102, `app/console` 13, `packages/pagekit` 62. (`packages/composer` 41 are excluded by the new path filter.) Identifier breakdown: `missingType.iterableValue` 824, `missingType.parameter` 426, `missingType.return` 154, `missingType.property` 45, `missingType.generics` 44.
  - **Constructor `readonly mixed $` sweep** (§5 of task prompt): 144 occurrences across 41 source files (`app/system/modules/*` 100, `app/installer/src/*` 15, `app/system/src/*` 6, `packages/pagekit/blog/src/Controller/*` 10, plus `app/system/modules/captcha/src/CaptchaListener.php` 2 and `app/system/modules/content/src/Plugin/MarkdownPlugin.php` 1). Replace each with the concrete container-resolved type, derived from the corresponding `index.php` / `ServiceProvider`. The 6 occurrences in `app/modules/kernel/src/Tests/ControllerResolverTest.php` are the documented test-only exception (handled in Checklist Step 14).
  - **Audit-finding files** (Phase 1 review, §3 of task prompt) — verified locations:
    - `app/system/modules/mail/src/Mailer.php` (`send()` missing `: bool`)
    - `app/system/modules/mail/src/Message.php` (`send(&$errors)` / `queue(&$errors)` untyped out-parameters)
    - `app/system/modules/mail/src/Controller/MailController.php` (`mixed` mailer type)
    - `app/system/modules/user/src/Controller/ResetPasswordController.php` + `RegistrationController.php` (`mixed` mailer type)
    - `app/system/modules/mail/index.php` (remove unused `auth_mode` config key)
    - `packages/pagekit/blog/src/Model/Post.php` (relation properties `$user`, `$comments` typed `mixed` → narrow to `?User` / `?array`)
    - `app/console/src/Commands/SetupCommand.php` (`execute()` missing `: int`, `exit;` instead of `return Command::SUCCESS`), `app/console/src/Commands/TranslationFetchCommand.php` and `app/console/src/Commands/ExtensionTranslateCommand.php` (currently typed `: void` — must change parent-incompatible `void` → `int` and remove the matching baseline entries that exist for these two)
    - `app/modules/log/src/Logger.php` (`Logger::__invoke()` PSR-3 typed signature)
    - `app/modules/view/modules/twig/src/TwigLoader.php` + `TwigCache.php` (parent-compatible types)
    - `app/modules/debug/src/DataCollector/AuthDataCollector.php` (§4.1 — `instanceof User` narrow; existing TODO comment on line 61–63 is the explicit hand-off to this step and must be removed when the fix lands).
- **Deferred:**
  - **Step 2.1.5 (PHPStan Level 6→7 — Null Safety)** — do NOT add `?Type` declarations beyond what is strictly required to keep an existing public/protected signature consistent (e.g. interface compatibility) or to silence a Level-6 error. Property nullability sweeps are out of scope.
  - **Step 2.1.6 (PHPStan Level 7→8 — Strict Typing)** — `mixed` is acceptable in this step where genuinely unknown (e.g. `__call`, `__invoke`, container/registry payloads). The pre-existing `// TODO: Must be refactored in Step 2.1.6` on `Mailer::beforeSend()` / `afterSend()` (interface-pollution split) stays untouched.
  - **Step 2.1.7 (QueryBuilder API standardization)** — touches `app/modules/database/`; do not refactor query-builder call sites in this step beyond adding return types.
  - **Step 2.1.8 / 2.1.9 (Infection / Coverage)** — out of scope.
  - **PSR-11 `Container::get()` / `ContainerInterface::get()`** — keep returning `mixed` per PSR-11 contract (Step 2.0.1); do NOT narrow.
  - **`MailerInterface` split** into `MailerInterface` + `MailPluginInterface` — already TODO-flagged for Step 2.1.6; leave the existing TODO and the no-op `beforeSend()`/`afterSend()` in place.
  - **`packages/composer/*`** (Composer-generated autoload scaffolding) — excluded from PHPStan, no fixes, no baseline entries, no `// TODO` markers (this is generated vendor code, not Pagekit code).
  - **PHPStan baseline regeneration** — explicitly forbidden. The only allowed baseline edits are surgical removals of entries that become unmatched.
- **Bridges:** None. Adding return types is a forward-only change; if a child override breaks parent-class compatibility (e.g. `TwigLoader::findTemplate()`, `TwigCache::__construct()`, `Logger::__invoke()`, console `execute()` returning `: void` instead of `: int`), the child is fixed to match the parent — never wrapped or aliased. Audit-flagged `mixed` mailer parameters are tightened directly to `Pagekit\Mail\Mailer` (the concrete container service registered as `'mailer'` in `app/system/modules/mail/index.php` line 19).
- **Checklist:**
  1. **Discovery snapshot + composer exclusion + baseline pre-clean.** Add **two** lines to `phpstan.neon` `excludePaths` (both Composer-generated autoload scaffolding, kept together as one logical unit — option (a) per architect amendment 2026-05-04): insert `packages/autoload.php` and `packages/composer` between `app/modules/*/vendor` and `*/node_modules/*`. Both lines must be added in this single Checklist Step 1 commit; adding only one of them yields a `class.notFound` error on `packages/autoload.php:9` (the wrapper references `ComposerAutoloaderInit{hash}::getLoader()`, defined in the now-excluded `packages/composer/autoload_real.php`). Rejected alternatives: option (b) `bootstrapFiles` would force PHPStan to execute generated code whose class-name hash mutates on every `composer install` — fragile and unrelated to the Pagekit codebase; option (c) a single new baseline entry would re-introduce Composer-scaffold noise into the maintained baseline in the very same step that removes it (and would itself need surgical removal whenever the hash changes). Surgically remove the two `packages/composer/*` entries from `phpstan-baseline.neon` (currently at lines ≈ 2473–2487 — `path: packages/composer/ClassLoader.php` and `path: packages/composer/autoload_real.php`, including their parent `-` list markers and `message`/`identifier`/`count` keys; do not touch any other entry). Do **not** add a baseline entry for `packages/autoload.php` — exclusion supersedes it. Do **not** bump the PHPStan level yet. Capture a discovery snapshot to `tmp/phpstan_l6_discovery.txt` via `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M --level=6 --error-format=raw 2>&1 | tee tmp/phpstan_l6_discovery.txt` for the Refactorer to consume per directory; this file is gitignored (under `tmp/`) and never committed. Commit: `chore(phpstan): exclude generated composer autoload files`.
  1.5. **Relocate `FileUtil` test trait to its consumer module (locality of concern).** The trait `Pagekit\Tests\FileUtil` currently lives at `app/modules/application/src/Tests/FileUtil.php`, but its **only** two consumers (verified via ripgrep — `rg -n "FileUtil"`) sit in `app/modules/filesystem/`: `app/modules/filesystem/src/Tests/FilesystemTest.php` (line 9 `use`, line 14 `use FileUtil;`) and `app/modules/filesystem/src/Tests/Adapter/StreamAdapterTest.php` (line 10 `use`, line 15 `use FileUtil;`). Under the per-group scoped L6 check, Group A (Step 2) sees the trait but not its consumers, producing a spurious `trait.unused` error plus 16 stale `missingType.*` errors for trait methods that genuinely have other-module callers. Architect amendment 2026-05-04 selects **option (b)** of the four candidates (`(a)` expand scope, `(b)` co-locate, `(c)` per-file carve-out, `(d)` baseline ignore) — option (b) is the only choice consistent with the plan's "no-suppression, locality-of-concern" stance. Steps for the Refactorer:
     - Move the file: `git mv app/modules/application/src/Tests/FileUtil.php app/modules/filesystem/src/Tests/FileUtil.php`.
     - Edit the moved file's namespace declaration: `namespace Pagekit\Tests;` → `namespace Pagekit\Filesystem\Tests;` (matches the `Pagekit\Filesystem\` PSR-4 root in `composer.json` line 80).
     - Update both consumer files: `use Pagekit\Tests\FileUtil;` → `use Pagekit\Filesystem\Tests\FileUtil;`.
     - Run `composer dump-autoload -o` from the workspace root (autoload classmap regenerates; do not commit `app/vendor/`).
     - Do **not** add return/parameter types to the trait in this step — that work belongs to Step 4 (Group C), where `app/modules/filesystem/` is the explicit scope.
     - Sibling traits in `app/modules/application/src/Tests/` (`FtpUtil`, `DbUtil`) and helper TestCases (`FtpTestCase`, `DbTestCase`, `ContainerTest`, `ContainerPsr11Test`) are **not** moved — they either have consumers within `Pagekit\Tests\*` itself or are direct PHPUnit `TestCase` subclasses analyzed in their own Group A context. Only `FileUtil` has the cross-group locality problem.
     - Verifier check: `rg -n "Pagekit\\\\Tests\\\\FileUtil" app/ packages/` returns zero matches; `rg -n "Pagekit\\\\Filesystem\\\\Tests\\\\FileUtil" app/` returns 3 matches (the moved trait + the 2 consumer `use` statements). Tester re-runs PHPUnit (the two relocated `TestCase`s must stay green) and re-captures `tmp/phpstan_l6_discovery.txt` so subsequent Group A and Group C error counts reflect the move (Group A drops the 16 trait-context errors plus the `trait.unused`; Group C gains the same 16 errors against the relocated file, which Step 4 then fixes). Commit: `refactor(tests): relocate FileUtil trait to filesystem module for locality of concern`.
  2. **`app/modules/` Group A — Application core (kernel + application + config + log + session + auth + migration).** ~200 L6 errors (216 minus the 16 `FileUtil` trait-context errors relocated in Step 1.5). Add return types, parameter types, `array<...>` iterable-value annotations, generic-class type parameters, and property types on every method/property flagged in the discovery snapshot under `app/modules/{kernel,application,config,log,session,auth,migration}/`. **Audit fix:** rewrite `Pagekit\Log\Logger::__invoke($level, $message, array $context = [])` to the PSR-3-compatible signature `public function __invoke(int|string $level, string|\Stringable $message, array $context = []): void` (Monolog's `BaseLogger::log()` returns `void`). Where a method genuinely cannot be narrowed (e.g. `__call`, `__invoke` on a container), use `mixed` and document the rationale inline only if it is non-obvious. Do NOT add `@phpstan-ignore`. Commit: `refactor(types): add return types to kernel, application, config, log, session, auth, migration`.
  3. **`app/modules/` Group B — Database.** 207 L6 errors (205 missing-type + the 2 `trait.unused` scoping artifacts described below — the latter are NOT counted toward the gate). Same return-type + iterable-value + parameter-type sweep across `app/modules/database/`. **Do not** refactor query-builder call sites (deferred to Step 2.1.7); annotate them only. For DBAL `Result` / `Statement` returns, declare the concrete Doctrine class (`\Doctrine\DBAL\Result`). For ORM hydrator iterables, annotate as `array<int, T>` or `iterable<T>` based on call-site usage. **Cross-module ORM traits — `trait.unused` scoping artifact (architect amendment 2026-05-04, option (d)):** `app/modules/database/src/ORM/ModelTrait.php` and `app/modules/database/src/ORM/PropertyTrait.php` are foundational ORM building blocks consumed by 7 model files across 5 other modules (`app/system/modules/{widget,user,site,comment}/Model/*`, `packages/pagekit/blog/src/Model/*`; verified via `rg -n "use (ModelTrait|PropertyTrait)"`). Under the per-group scoped L6 check, these traits sit in scope but their consumers do not, producing two spurious `trait.unused` errors that will NEVER fire under Step 16's global L6 run (where every consumer is in scope). Of the four candidates evaluated — `(a)` expand Group B's scope to include all 5 consumer modules (rejected: pollutes Group B with code that belongs to Steps 7–14, breaks per-step accounting and the No Mercy / locality-of-concern stance); `(b)` carve out the two trait files from the scoped command (rejected: awkward CLI plumbing — PHPStan's `analyse` accepts only positive paths, not `--exclude` flags, so this would require enumerating every subdirectory of `app/modules/database/src/` *except* `ORM/ModelTrait.php` and `ORM/PropertyTrait.php`, fragile to future restructuring); `(c)` add a single named `ignoreErrors` entry in `phpstan.neon` with `identifier: trait.unused` scoped to those two paths (rejected: violates the No Mercy "no suppression layers" rule — even a focused `ignoreErrors` is a permanent config wart that lives forever in `phpstan.neon`, contradicts Step 1's "no baseline additions" discipline, and pretends a real-rule-on-real-files exemption when the underlying issue is purely a scoping artifact); `(d)` drop the strict per-step `trait.unused` gate for cross-module trait files and rely on Step 16's global L6 to confirm zero errors (selected) — option (d) is the only choice that requires zero config changes, zero scope pollution, and zero permanent suppressions, while honoring the natural division of labor between per-step scoped checks (incremental gates) and Step 16's global check (final authority). **Refactorer:** still type the two trait files in this Step 3 (return types, parameter types, iterable-value annotations on every method/property) — they live in `app/modules/database/` and belong to Group B's typing sweep. Only the `trait.unused` rule is exempted from the per-step gate, NOT the missing-type rules. Do **not** add `@phpstan-ignore`, do **not** add `// TODO`, do **not** edit `phpstan.neon` or `phpstan-baseline.neon`. **Standing convention for any future Group's scoped L6 check:** when a trait file lives in its natural home (the module that owns the API) and its consumers span other groups, the per-step Tester treats `trait.unused` errors against that file as scoping artifacts and excludes them from the failure gate. Step 16's global L6 (where all consumers are in scope) is the final authority for `trait.unused`. This convention applies to any cross-module trait discovered in later groups; relocation (option (b) of Step 1.5's `FileUtil` decision) remains the preferred treatment when a single-module relocation target exists, but for multi-consumer foundational API traits like the two ORM traits here, no relocation target exists and option (d) is the standing answer. Commit: `refactor(types): add return types to database module`.
  4. **`app/modules/` Group C — View + Twig submodule + Markdown + Filter + Filesystem.** ~343 L6 errors across `app/modules/view/`, `app/modules/view/modules/twig/`, `app/modules/markdown/`, `app/modules/filter/`, `app/modules/filesystem/` (327 baseline + 16 errors against the `FileUtil` trait relocated here in Step 1.5). The trait now lives at `app/modules/filesystem/src/Tests/FileUtil.php` and is fully typed as part of this Group C sweep — add `string|false` / `string` / `bool` / `void` return types and `?string` / `int` / `string` parameter types per the existing trait body. **Latent-bug fix bundled with the trait typing:** the trait's `mirror()` method (line 77 pre-move) throws `new IOException(...)` with no `use` statement, so the symbol currently resolves to `Pagekit\Tests\IOException` (non-existent class — the four baseline entries at `phpstan-baseline.neon` lines 778–799 document this for both consumers via the trait inclusion). Add `use Symfony\Component\Filesystem\Exception\IOException;` to the relocated trait. This naturally renders the four `Pagekit\Tests\IOException` baseline entries unmatched; surgically remove them in this same Step 4 commit (the same way Step 1 removes the two `packages/composer/*` entries and Step 13 removes the two console `void`-vs-`int` entries — never `--generate-baseline`). **Audit fixes:** in `app/modules/view/modules/twig/src/TwigLoader.php` line 28, change the `findTemplate(string $name, bool $throw = true)` signature to match Twig 3's `protected function findTemplate(string $name, bool $throw = true): ?string` (Twig 3 declares `?string`; the current child is missing `: ?string`). In `app/modules/view/modules/twig/src/TwigCache.php` line 11, type the property `protected string $dir;`; in line 16, type the constructor `__construct(string $directory, int $options = 0)` to match `\Twig\Cache\FilesystemCache::__construct()`. Commit: `refactor(types): add return types to view, twig, markdown, filter, filesystem`.
  5. **`app/modules/` Group D — Routing + Feed + Debug.** ~186 L6 errors across `app/modules/routing/`, `app/modules/feed/`, `app/modules/debug/`. **Audit fix (§4.1 of task prompt):** in `app/modules/debug/src/DataCollector/AuthDataCollector.php`, type-narrow with `if (!$user instanceof User) { return [...empty bag...]; }` between the existing null check (line 46–54) and the `return [...]` block (line 56–65). After the narrow, `$user->isAuthenticated()` and `User::findRoles($user)` type-check cleanly under L6. Remove the existing `// TODO: Must be refactored in Step 2.1.4 ...` block (lines 61–63). `UserInterface` itself is **not modified** — debug-bar concerns must not pollute the auth contract (Single Responsibility). Type the `collect()` return as `array<string, mixed>`. Do NOT touch `Pagekit\Debug\Middleware\DebugLogger::log()` / `emergency()` / `alert()` etc. signatures beyond adding the PSR-3 `int|string $level` and `string|\Stringable $message` parameter types — these are PSR-3 contract methods. Commit: `refactor(types): add return types to routing, feed, debug + AuthDataCollector instanceof narrow`.
  6. **Application URL + Container core (`app/modules/application/Container.php`, `Event/`, `Module/`).** Already partially covered by Group A. Final sweep step for any remaining `app/modules/` errors not yet typed (cross-module utilities, Container generics `IteratorAggregate`/`ArrayAccess`, `Module/ModuleManager` `IteratorAggregate` generics, `Event/Event` `ArrayAccess` generics, `Module/ModuleInterface::main(): mixed`, `EventDispatcherInterface::on/off/subscribe/unsubscribe(): self`). After this step, scoped L6 on `app/modules/` must report 0 missing-type errors. Commit: `refactor(types): finalize app/modules return-type sweep`.
  7. **`app/system/modules/mail/` + Mail audit cluster + cross-module side-effect fix on `MenuApiController` (architect amendment 2026-05-04, option (c)).** 11 L6 errors in the module + the cross-cutting Mail audit fixes (Phase 1 review) + one minimal cross-module side-effect fix forced by Step 7's typing. Files:
     - `app/system/modules/mail/src/Mailer.php` — `send(Email $message): bool` (line 37), `MailerInterface::send()` interface companion update.
     - `app/system/modules/mail/src/Message.php` — `send(?array &$errors = null): int` and `queue(?array &$errors = null): int` (the `&$errors` parameter is already an out-parameter; type as `?array` to retain backwards compatible call sites passing nothing). Update `MessageInterface` signatures in lockstep.
     - `app/system/modules/mail/src/Controller/MailController.php` — replace `private readonly mixed $mailer` (and any other audit-mentioned `mixed` mailer occurrences in this file, currently 3) with `private readonly \Pagekit\Mail\Mailer $mailer`. Resolve from the container service `'mailer'` registered in `app/system/modules/mail/index.php`.
     - `app/system/modules/mail/index.php` — delete the `'auth_mode' => null,` line (line 111, dead config key per audit). Confirm via `grep -rn "auth_mode" app/ packages/` that no consumer reads it; if any consumer is found (none is expected), refactor and delete.
     - All other `readonly mixed $` properties in `app/system/modules/mail/src/Controller/MailController.php` typed to their concrete container-resolved class.
     - **Cross-module side-effect fix on `app/system/modules/site/src/Controller/MenuApiController.php` (architect amendment 2026-05-04, option (c) — minimal one-line source fix + surgical baseline removal):** Step 7's typing of the mail module narrows PHPStan's inferred return type of `Pagekit\__()` (the translator helper) from `mixed` to `string` at the call sites in `MenuApiController::indexAction()` (line 35 in the develop-HEAD literal). This change in inferred type fires a *new* PHPStan error AND simultaneously renders the *existing* `offsetAccess.notFound` baseline entry (`phpstan-baseline.neon` line ≈ 1407–1411, regex literal `Offset 'count' does not exist on array\{id\: 'trash', label\: mixed, fixed\: true\}`) unmatched (verified empirically: with only Step 7's mail diffs applied and MenuApi reverted to develop, `./app/vendor/bin/phpstan analyse --level=5` reports both `ignore.unmatched (non-ignorable)` on the old `label: mixed` baseline regex AND a fresh `offsetAccess.notFound` error with `label: string` at line 40 — the configured-level L5 gate fails on both counts and Step 7 cannot be committed). Of the three options evaluated:
       - **(a)** Allow the Refactorer's restructured fix that introduces a second `Node::where(['menu' => 'trash'])->count()` call and increments the baseline `count: 1` → `count: 2` on the `QueryBuilder::count()` entry — **rejected**: violates the plan's "surgical removals only, never additions or count increments" rule (TESTING STRATEGY § first bullet); a `count` increment on an existing baseline entry is semantically a baseline addition (one new tracked-debt instance), not a removal; the second query also introduces a behavioural change (an extra DB round-trip per `indexAction()` invocation, even when the trash menu is empty — strictly worse than the existing fold-into-the-foreach pattern that only counts trash if it's already present in the menu list).
       - **(b)** Revert MenuApi to develop-HEAD and edit the existing baseline regex `label: mixed` → `label: string` to keep tracking the offsetAccess error, deferring the source fix to Step 9 — **rejected on two grounds**: first, the regex edit is *itself* a baseline edit (the No Mercy rule treats regex literal edits as additions when the new regex matches an error the old regex did not); second, deferring forces Step 9 to do exactly the same surgical removal anyway (the Step 9 broader return-type sweep on MenuApi will type the literal correctly and unmatch the entry again), creating a pointless edit-then-remove churn cycle across two commits to solve a problem caused entirely by Step 7's typing.
       - **(c)** Apply a minimal one-line source fix to MenuApi (add `, 'count' => 0` to the trash literal) and surgically remove the now-unmatched `offsetAccess.notFound` baseline entry in the same Step 7 commit — **originally selected (2026-05-04 amendment #1) but empirically falsified by Tester observation; superseded by amendment #2 below**. Original rationale: zero logic change, zero behavioural change (the trailing `if (!$menus['trash']['count']) { unset($menus['trash']); }` block already drops the trash menu when count is 0), same number of `count()` calls as develop-HEAD, 16-character source delta. **Falsification:** with `'count' => 0` in the initial literal at line 34, PHPStan does NOT propagate the by-reference foreach mutation (`$menu['count'] = Node::where(...)->count();` at line 37 with `foreach ($menus as &$menu)`) back into the inferred shape of `$menus['trash']['count']` — it still narrows `$menus['trash']['count']` to the literal constant `0` from the initial array literal, and therefore fires a **new** `booleanNot.alwaysTrue` error at line 40 (`!$menus['trash']['count']` is always true) that did not exist in the develop-HEAD baseline and is not silenceable without violating the plan's no-`@phpstan-ignore` rule. Verified empirically with the architect's probe edit (16-char `, 'count' => 0` addition): `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M --level=5 app/system/modules/site/src/Controller/MenuApiController.php` reports exactly `Negated boolean expression is always true. booleanNot.alwaysTrue` at line 40, exit 1.
       - **(c′) — architect amendment 2026-05-04 #2, supersedes #1 (selected).** Adopt the Tester-proposed local accumulator pattern (the Tester's option (c) in the falsification report) — the only candidate among the six listed (a–f) that simultaneously satisfies (i) no `@phpstan-ignore` (rules out original-(a) and Tester-(a)), (ii) no type-laundering hack (`(int) null`, `intval('0')`, `max(0, 0)`, `0 + 0` — rules out Tester-(b) and Tester-(e); the plan's preamble explicitly bans both `Do not add type casts just to silence errors` and `Do not widen parameter or return types just to make the error go away`), (iii) no second `Node::where(['menu' => 'trash'])->count()` call after the foreach (rules out Tester-(d) — same reason as original-(a) above: a `count: 1 → count: 2` increment on the existing `QueryBuilder::count()` baseline entry is semantically a baseline addition, plus the extra DB round-trip is a behavioural regression even when no trash menu exists; the plan's TESTING STRATEGY § first bullet permits surgical *removals* only and explicitly forbids `count: N → N+1` increments), (iv) no baseline addition (rules out Tester-(f) — adding a temporary baseline entry for `booleanNot.alwaysTrue` "to be removed in Step 9" is a baseline addition, semantically forbidden in the same way that regex literal edits are; the plan never adds entries it later removes — only Step 1 / 4 / 7 / 13 / 16 surgical removals are permitted), and (v) no deferral of the source-fix to Step 9 (rules out Tester-(f) again — the deferral does not solve the L5 gate failure on Step 7's commit, which is the actual blocker). The accumulator pattern adds 5 lines + modifies 1 line (semantic-equivalent rewrite of the post-loop check), preserves the foreach structure verbatim, makes zero baseline mutations beyond the planned `offsetAccess.notFound` surgical removal, and was empirically verified by the architect's probe edit on the live working tree to clear the L5 gate cleanly (exit 0 for `./app/vendor/bin/phpstan analyse --level=5 app/system/modules/site/src/Controller/MenuApiController.php`; the four residual L6 errors at lines 29 / 52 / 88 / 88 are the unrelated `missingType.iterableValue` / `missingType.parameter` errors already scoped to Step 9). The Verifier's prior "restructuring out of scope" rejection was correct under amendment #1's "single-character literal addition" framing; amendment #2 explicitly redefines scope to authorize a *minimal local accumulator* (`$trashCount` tracked inside the existing foreach) — the foreach loop body itself is unchanged structurally (still iterates all menus, still assigns `$menu['count']`); only an inner `if ($menu['id'] === 'trash')` capture is added plus a defensive `unset($menu)` after the loop and a one-line condition rewrite. This is structurally smaller than Tester-(d)'s second-query approach (which adds a separate DBAL round-trip and a baseline increment) and strictly smaller than the broader Step 9 MenuApi work (which adds return types, parameter types, and four concrete constructor types). Step 9's broader MenuApi work is **unchanged and remains in Step 9** — amendment #2 only modifies the `indexAction()` body (lines 30–45 in develop-HEAD), nothing else in MenuApi is touched in Step 7.
     - Refactorer instructions for the MenuApi side-effect fix (architect amendment 2026-05-04 #2): edit lines 34–42 of `app/system/modules/site/src/Controller/MenuApiController.php` to apply both the literal placeholder AND the local accumulator pattern in a single coherent diff. Replace the block:
       ```php
       $menus['trash'] = ['id' => 'trash', 'label' => __('Trash'), 'fixed' => true];

       foreach ($menus as &$menu) {
           $menu['count'] = Node::where(['menu' => $menu['id']])->count();
       }

       if (!$menus['trash']['count']) {
           unset($menus['trash']);
       }
       ```
       with:
       ```php
       $menus['trash'] = ['id' => 'trash', 'label' => __('Trash'), 'fixed' => true, 'count' => 0];

       $trashCount = 0;
       foreach ($menus as &$menu) {
           $menu['count'] = Node::where(['menu' => $menu['id']])->count();
           if ($menu['id'] === 'trash') {
               $trashCount = $menu['count'];
           }
       }
       unset($menu);

       if (!$trashCount) {
           unset($menus['trash']);
       }
       ```
       Both halves of the diff are required together: the `'count' => 0` literal addition makes the array shape consistent with the post-loop `$menus['trash']['count']` writes (so PHPStan does not fire `offsetAccess.notFound` on the foreach assignment target via the by-reference write into `$menus['trash']`), and the `$trashCount` accumulator makes the post-loop conditional type-honest (PHPStan correctly infers `$trashCount` as `int` after the loop and does not narrow `!$trashCount` to a constant). The defensive `unset($menu)` after the foreach is canonical PHP practice when iterating by reference and is required here because subsequent code mutates `$menus` (the `unset($menus['trash'])` on the next line could otherwise interact with the dangling reference). Do **not** add a second `Node::where(...)->count()` call (Tester-(d), rejected). Do **not** introduce any `@phpstan-ignore` annotation (Tester-(a), rejected). Do **not** apply type-laundering hacks like `(int) null` / `intval('0')` / `max(0, 0)` / `0 + 0` (Tester-(b)/(e), rejected). Do **not** add a `// TODO` comment (the change is semantically equivalent to develop-HEAD and not technical debt; the broader MenuApi typing in Step 9 is the natural next step and does not need a forward marker since Step 9 covers MenuApi explicitly). Surgically remove the `offsetAccess.notFound` baseline entry (currently `phpstan-baseline.neon` line ≈ 1407–1411 — the `-` block including `message`/`identifier`/`count`/`path` keys, regex `label\: mixed`); the `QueryBuilder::count()` baseline entry stays untouched at `count: 1` (the accumulator's inner `if` does not introduce a new `count()` call). Verifier check: `git diff phpstan-baseline.neon` for Step 7 must show **only** the surgical removal of the `offsetAccess.notFound` entry plus the two mail-test `assertIsArray`/`assertIsString` `method.alreadyNarrowedType` removals (made unmatched by the test rewrite that drops the redundant assertions); **zero** `count: N → N+1` changes on any entry; **zero** new entries. Verifier check on the source: the `indexAction()` body matches the second code block above verbatim (modulo whitespace); no `@phpstan-ignore` strings introduced anywhere in the file.
     - Per-step Tester runs `./app/vendor/bin/phpstan analyse --level=6 --no-progress app/system/modules/mail` (must exit 0) AND the configured-level full sweep `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` (must exit 0, with the surgical baseline removals listed above being the only baseline diff). Commit message: `refactor(types): mail module return types + Phase 1 audit fixes` with body line `Includes minimal one-line side-effect fix on app/system/modules/site/src/Controller/MenuApiController.php (add 'count' => 0 to trash literal) — required because Step 7's mail-module typing narrowed PHPStan's inference of __() at the call site, breaking the existing offsetAccess.notFound baseline entry. Broader MenuApi return-type sweep remains in Step 9.`
  8. **`app/system/modules/user/` + retroactive ORM trait + Auth event narrowing (architect amendment 2026-05-04 #3, scope expansion #2 for Step 8 — selected option (b) of the four candidates).** 69 L6 errors + 49 `readonly mixed $` across `Controller/{AuthController(10), RegistrationController(10), ResetPasswordController(11), ProfileController(6), UserApiController(5), RoleApiController(2), UserController(2)}` + `Event/{AccessListener(3), AuthorizationListener(3)}`. Replace every `readonly mixed $foo` with the concrete type, derived from the user-module `index.php` service registrations (`Pagekit\User\Auth\Auth`, `Pagekit\Module\ModuleManager`, `Symfony\Component\HttpFoundation\Request`, `Pagekit\Session\Session`, `Symfony\Contracts\Translation\TranslatorInterface`, `Symfony\Component\Validator\Validator\ValidatorInterface`, etc.). **Audit fix:** mailer parameters in `RegistrationController` + `ResetPasswordController` typed `\Pagekit\Mail\Mailer` (not `mixed`, not `MailerInterface` — concrete class because the controllers call `Mailer::create()` and `Mailer::send()`). Add return types to all action methods (`array` for JSON-returning actions, `Response` / `RedirectResponse` for redirects, `string` for view-returning actions). Add the `array<string, string>` value-type annotation on `Role::$properties` (line 56), the `array<int, string>|null` annotation on `User::$permissions` (line 99), the `array<int, string>` annotation on `Role::$permissions` (line 53), and the `array<int, string>` / `array<string, mixed>` return-type docblocks on `User::getStatuses()` / `User::jsonSerialize()`.

     **Scope expansion #2 — retroactive Step 2 + Step 3 cleanup forced by Step 8's typing chain reaction.** Step 8's narrowing of `private readonly mixed $auth` → `private readonly \Pagekit\Auth\Auth $auth` in `AuthController` (and `private readonly mixed $userProvider` → concrete in other constructors) caused PHPStan to resolve the inherited `Auth::login()` / `Auth::logout()` return type as `\Pagekit\Event\EventInterface` (the Step-2-committed signature), which lacks `getResponse()` / `hasResponse()` — firing 4 new `method.notFound` errors at `AuthController.php:77,108`. Independently, the per-step scoped L6 check on `app/system/modules/user` reports 8 unmatched baseline patterns on `User.php` and `Role.php` that became unmatched as a side effect of the **Step 3 trait typing already at HEAD** (`PropertyTrait::$_properties` typed `array<string, array<string, mixed>>`, `ModelTrait::create(): static`, `ModelTrait::find(): ?static`) — these are leftover Step 3 baseline entries that Step 3's commit failed to surgically remove. A wider scoped L6 sweep across all 7 ORM-trait consumers (`app/system/modules/{widget,user,site,comment}/Model/*` + `packages/pagekit/blog/src/Model/*`) reveals 18 additional unmatched baseline entries on the same `where()` / `count()` / `update()` / `whereIn()` / `whereExists()` / `whereInSet()` / `offset()` / `orderBy()` `method.notFound` patterns plus `create()` / `find()` `return.type` patterns plus `staticProperty.notFound` / `isset.property` patterns — all of them rendered unmatched by the same Step 3 trait typing. Step 16's global L6 will catch these eventually, but they fire as soon as ANY downstream step pulls the trait into a concrete-class scoped check, so they cannot be deferred past Step 8 without breaking Step 8's gate.

     **Four options evaluated:**
     - **(a)** Roll back Step 8's `array<string, string>` doctag on `Role::$properties` (and the matching User typing) and defer all model property typing to a future post-Step-3-cleanup step — **rejected**: the unmatched baseline entries on User.php / Role.php fire from Step 3's trait typing (already at HEAD), NOT from any Step 8 doctag (verified empirically: with Step 8 user-module typing applied + cross-module fixes stashed, scoped L6 on `app/system/modules/user` reports `Found 12 errors` = 4 real `EventInterface::getResponse/hasResponse` errors at `AuthController.php:77,108` + 8 unmatched ignore patterns on `User.php`/`Role.php` — none of the 8 patterns reference any new doctag). Removing the doctags has zero effect on the gate; this option does not unblock Step 8.
     - **(b)** Accept the Refactorer's cross-module edits as scope expansion #2 for Step 8, framed as **retroactive Step 2 + Step 3 cleanup**: the ORM trait edits (`ModelTrait`, `PropertyTrait`, `QueryBuilder`) live in `app/modules/database/` (Step 3's territory) and `Auth.php` lives in `app/modules/auth/` (Step 2's territory) — they are structurally Step 2/3 work that Step 3's commit failed to ship complete. The cross-module baseline removals are mandatory surgical removals once the trait typing fires, identical in form to the precedents already authorized in Step 1 (`packages/composer/*`), Step 4 (`Pagekit\Tests\IOException`), Step 7 (`offsetAccess.notFound` on `MenuApiController` + `method.alreadyNarrowedType` on mail tests), and Step 13 (console `void`-vs-`int`). All edits are forward-only refinements — no `@phpstan-ignore`, no `// TODO`, no behavioural change, no API break, no baseline additions. **Selected.**
     - **(c)** Split the work into a retroactive "Step 7.5: ORM trait + Auth event narrowing" commit slotted between Step 7 and Step 8, then a clean Step 8 with only the user-module local typing — **rejected**: the workflow rule "one commit per Checklist Step" and the Architect's checklist (which has no Step 7.5) make this a structural reordering even though it's technically a forward-only insertion. The Refactorer would have to revert all the in-progress Step 8 work, ship Step 7.5, then re-do Step 8 — wasting one full Refactorer/Verifier/Tester cycle for zero substantive benefit (the diff content is identical either way; only the commit boundary changes). The plan's "minimum cross-module spillover" stance is better served by absorbing the spillover into Step 8 with explicit amendment framing than by adding a phantom checklist step that wasn't planned.
     - **(d)** Drop only the `Role::$properties` `array<string, string>` doctag — **rejected** for the same empirical reason as (a): the doctag is not the trigger. The 4 `EventInterface::getResponse/hasResponse` errors and the 26 unmatched baseline entries fire regardless of whether the doctag exists.

     **Refactorer-authorized edits for scope expansion #2 (already on the working tree as of 2026-05-04, retain verbatim):**
     - `app/modules/auth/src/Auth.php`: narrow `login(UserInterface $user, $remember = false): EventInterface` → `: LoginEvent`, narrow `logout(): EventInterface` → `: LogoutEvent`. The restructure (separate `$event = new LoginEvent(...); $this->events->trigger($event); return $event;`) is required because `EventDispatcherInterface::trigger(EventInterface $event): EventInterface` widens the return; binding `$event` first preserves the narrower static type. Drop the now-unused `use Pagekit\Event\EventInterface;` import. **Why narrow rather than `assert($event instanceof LogoutEvent)` at the consumer:** the plan's preamble explicitly bans `Do not use assert() or inline @var PHPDoc tag to override PHPStan's inferred type`. Narrowing the source signature is type-honest (the runtime value IS always the concrete event subclass passed into `trigger()`) and is the canonical fix.
     - `app/modules/database/src/ORM/ModelTrait.php`: add `instanceof static` guards on `create()` and `find()` (with `\LogicException` defensive throws) so PHPStan can honour the existing `: static` / `: ?static` return types against the underlying `EntityManager::load(): object` / `QueryBuilder::first(): ?object` returns. Add the explanatory docblock paragraph above each method documenting the narrowing rationale. **Why guards rather than `@phpstan-assert`:** the plan bans inline PHPDoc overrides; runtime guards with throws are the canonical pattern for narrowing an `object` return to `static`.
     - `app/modules/database/src/ORM/PropertyTrait.php`: replace `isset(static::$properties)` with a `get_class_vars(static::class)` reflection lookup (in both `getProperties()` and `getPropertyDescriptor()`). PHPStan cannot statically narrow `static::$properties` from a trait that does not declare the field, so the `isset()` form fires `isset.property` (`(array) in isset() is not nullable`) on consumers that do declare it (Node, Role) AND `staticProperty.notFound` on consumers that do not (Comment, Page, User, Widget, Post). The `get_class_vars()` lookup is the canonical PHP reflection pattern for this exact scenario and is type-safe under both consumer shapes. Add the explanatory docblock above both methods.
     - `app/modules/database/src/ORM/QueryBuilder.php`: add `@method` docblock tags above the class declaration mirroring the existing `__call()` proxy contract (lines 17–40 of the diff: `where`, `orWhere`, `whereIn`, `orWhereIn`, `whereExists`, `orWhereExists`, `whereInSet`, `select`, `from`, `join`, `innerJoin`, `leftJoin`, `rightJoin`, `groupBy`, `having`, `orHaving`, `orderBy`, `offset`, `limit`, `count`, `update`, `delete`, `getSQL`, `execute`). This is pure documentation of behaviour that already exists at runtime via `__call()`; it does not add any new method or change any signature. PHPStan will then resolve `Node::where(...)`, `User::where(...)`, etc. without firing `method.notFound`.

     **Mandatory surgical baseline removals for scope expansion #2 (24 entries across 18 paths):** all entries on the following paths that become unmatched under the L5 gate after the source edits above are surgically removed (the same `-` block including `message`/`identifier`/`count`/`path` keys; never `--generate-baseline`):
     - `app/modules/database/src/ORM/Relation/{BelongsTo,HasMany,HasOne,ManyToMany}.php` — 5 `whereIn` / `orderBy` `method.notFound` entries.
     - `app/system/modules/comment/src/Model/Comment.php` — 5 entries (`staticProperty.notFound` ×3, `where` / `update` `method.notFound`, 2× `return.type` for `create()` / `find()`).
     - `app/system/modules/site/src/Controller/{MenuApiController,NodeApiController}.php` — 4 entries (`count`, 2× `update`, `where`).
     - `app/system/modules/site/src/Model/{Node,Page}.php` — 11 entries (`where`, `whereIn`, `orderBy` `method.notFound` + `return.type` for `create()` / `modelFind()` / `find()` + `staticProperty.notFound` + `isset.property`).
     - `app/system/modules/user/src/Controller/{UserApiController,UserController}.php` — 6 entries (2× `count`, `offset`, 8× `where`, 2× `whereExists`, `whereInSet`).
     - `app/system/modules/user/src/Model/{Role,User}.php` — 8 entries (`return.type` ×4, `staticProperty.notFound`, `isset.property`, `where` `method.notFound` ×2).
     - `app/system/modules/widget/src/Model/Widget.php` — 3 entries (`staticProperty.notFound`, 2× `return.type`).
     - `packages/pagekit/blog/src/Controller/{CommentApiController,PostApiController,SiteController}.php` — 3 entries (`where` `method.notFound` aggregate).
     - `packages/pagekit/blog/src/Model/Post.php` — 3 entries (2× `return.type`, `isset.property`).

     **What does NOT belong to Step 8 even under scope expansion #2** — the affected modules' OTHER work (constructor `readonly mixed` sweep on Comment/Site/Widget/Blog controllers, return-type sweep on Site/Widget/Blog actions, audit fixes on Post relation typing, etc.) **stays in Steps 9 / 10 / 14** as originally planned. Scope expansion #2 authorizes ONLY the four cross-module source edits enumerated above (Auth narrowing + ORM trait improvements + QueryBuilder docblock) and the 24 mandatory surgical baseline removals. Any other edit to a file outside `app/system/modules/user/` is out of scope and must be reverted.

     **Verifier checks for scope expansion #2:**
     - `git diff HEAD app/modules/auth/src/Auth.php` shows ONLY the narrowed `login(): LoginEvent` / `logout(): LogoutEvent` signatures, the lockstep `$event = new ...; $this->events->trigger($event); return $event;` restructure, and the dropped `EventInterface` import — no other edits.
     - `git diff HEAD app/modules/database/src/ORM/ModelTrait.php` shows ONLY the `instanceof static` guards on `create()` / `find()` plus the explanatory docblocks — no other edits.
     - `git diff HEAD app/modules/database/src/ORM/PropertyTrait.php` shows ONLY the `get_class_vars(static::class)` lookups in `getProperties()` / `getPropertyDescriptor()` plus the explanatory docblocks — no other edits.
     - `git diff HEAD app/modules/database/src/ORM/QueryBuilder.php` shows ONLY the `@method` docblock additions above the class — no other edits.
     - `git diff HEAD phpstan-baseline.neon` shows ONLY removals (the 24 entries enumerated above plus any user-module entries on `app/system/modules/user/**` made unmatched by Step 8's local typing) — zero additions, zero `count: N → N+1` increments.
     - `rg -n "@phpstan-ignore" app/modules/auth app/modules/database/src/ORM app/system/modules/user packages/pagekit/blog 2>/dev/null` returns zero new matches relative to HEAD.
     - `rg -n "// TODO" app/modules/auth/src/Auth.php app/modules/database/src/ORM/{ModelTrait,PropertyTrait,QueryBuilder}.php` returns zero new matches relative to HEAD.

     Commit: `refactor(types): user module return types + readonly mixed sweep + retroactive ORM trait/Auth event narrowing` with body line `Scope expansion #2 (architect amendment 2026-05-04 #3): includes retroactive Step 2 (Auth.php LoginEvent/LogoutEvent narrowing) and Step 3 (ModelTrait instanceof static guards, PropertyTrait get_class_vars lookup, QueryBuilder @method docblocks) cleanup forced by Step 8's typing of $this->auth as Auth. 24 cross-module baseline entries surgically removed across database/comment/site/user/widget/blog modules; all other work in those modules stays in Steps 9/10/14.`
  9. **`app/system/modules/site/` + `app/system/modules/intl/`.** 132 L6 errors (66 + 66) + 17 `readonly mixed $` (12 site, 5 intl). Same pattern: return types on actions, concrete container types for constructor parameters. Site: `Controller/{NodeApiController(5), NodeController(4), MenuApiController(5), PageController(2)}`, `Event/NodesListener(1)`, `MenuHelper(2)`. Intl: `Controller/{IntlController(4), IntlApiController(1)}`. **Note (architect amendment 2026-05-04 #2 supersedes #1):** `MenuApiController::indexAction()` already received the `'count' => 0` literal placeholder + local `$trashCount` accumulator pattern in Step 7 (lines 34–47 in the post-Step-7 file), and the matching `offsetAccess.notFound` baseline entry was surgically removed in the same Step 7 commit. That `indexAction()` body remains unchanged across Step 9; the broader Step 9 work on this file (return-type annotations on `indexAction()`, `saveAction()`, `deleteAction()`; `string` parameter type on `deleteAction()`; `readonly mixed` → concrete container types on the four constructor parameters — `private readonly Pagekit\Config\Repository $config`, `private readonly Pagekit\Site\MenuHelper $menu`, `private readonly Symfony\Component\HttpFoundation\Request $request`, `private readonly Pagekit\Filter\FilterManager $filter` — derived from `app/system/modules/site/index.php` service registrations) is unchanged and proceeds normally. The 5 errors counted for `MenuApiController` in this step's discovery snapshot are the four `missingType.iterableValue` / `missingType.parameter` errors plus the `readonly mixed` typing work; the `offsetAccess.notFound` and `booleanNot.alwaysTrue` are no longer present (resolved together in Step 7 via the accumulator pattern). Commit: `refactor(types): site and intl modules return types + readonly mixed sweep`.
  10. **`app/system/modules/` tail (widget, finder, captcha, dashboard, content, cache, view, settings, comment, info).** 74 L6 errors + ~22 `readonly mixed $` distributed across `widget/Controller/*` (6 mixed), `finder/Controller/*` (7 mixed), `captcha/CaptchaListener.php` (2), `dashboard/Controller/DashboardController.php` (5), `content/Plugin/MarkdownPlugin.php` (1), `cache/Controller/CacheController.php` (2), `view/Event/ResponseListener.php` (1), `settings/Controller/SettingsController.php` (3), `info/Controller/InfoController.php` (1). Replace each `mixed` with the concrete container service (or `string` for scalar config values like `$version`, `$key`). Add return types on all actions. Commit: `refactor(types): system tail modules return types + readonly mixed sweep`.
  11. **`app/system/src/`.** 32 L6 errors + 6 `readonly mixed $` in `Controller/{AdminController(4), ExceptionController(2)}`. Same pattern. After this step, scoped L6 on `app/system/` must report 0 missing-type errors. Commit: `refactor(types): app/system/src return types + readonly mixed sweep`.
  12. **`app/installer/`.** 102 L6 errors + 15 `readonly mixed $` across `Controller/{InstallerController(3), MarketplaceController(1), PackageController(7), UpdateController(3)}` + `Package/PackageFactory(1)`. Includes the 6 `requirements.php` `RequirementCollection` generic-class fixes (`implements IteratorAggregate<int, Requirement>`, `getIterator(): \ArrayIterator<int, Requirement>`, all `array<Requirement>` annotations) and `install-demo.php` `defaults()` typing. Replace each `readonly mixed $foo` with the concrete container-resolved type (`Pagekit\Package\PackageManager`, `Pagekit\Module\ModuleManager`, `Symfony\Component\HttpFoundation\Request`, `Pagekit\Database\Connection`, etc.). Commit: `refactor(types): installer return types + readonly mixed sweep`.
  13. **`app/console/`.** 13 L6 errors + console audit fixes (Phase 1 review). Files:
     - `app/console/src/Commands/SetupCommand.php` line 46 — change `protected function execute(InputInterface $input, OutputInterface $output)` to `: int`. Replace the bare `exit;` on line 51 with `return Command::SUCCESS;` (or `Command::FAILURE` for the error path); the existing `Command` import is `Pagekit\Application\Console\Command` which extends `Symfony\Component\Console\Command\Command`. Add `return Command::SUCCESS;` at the end of `execute()`.
     - `app/console/src/Commands/TranslationFetchCommand.php` line 34 — currently `: void`. Change to `: int` and add `return Command::SUCCESS;` at the end. Remove the matching baseline entry `'#^Return type \(void\) of method ... TranslationFetchCommand::execute\(\) should be compatible with return type \(int\) of method Symfony\\Component\\Console\\Command\\Command::execute\(\)$#'` (search `phpstan-baseline.neon` for `TranslationFetchCommand`).
     - `app/console/src/Commands/ExtensionTranslateCommand.php` line 40 — same fix; same baseline entry surgical removal.
     - `app/console/src/Commands/ArchiveCommand.php` line 97 — `getPackageFilename(string $name): string`.
     - `SelfupdateCommand::download(string $url, string $file): void`.
     - `NodeVisitor/{NodeVisitor.php(13/32), PhpNodeVisitor.php(19)}` — `array<string, mixed>` for the `$results` property and the `$files` parameter; same array shape on the `traverse()` return.
     Per-step Tester runs `./app/vendor/bin/phpstan analyse --level=6 --no-progress app/console` (must exit 0). Commit: `refactor(types): console return types + Phase 1 audit fixes`.
  14. **`packages/pagekit/blog/`.** 55 L6 errors + 10 `readonly mixed $` across `Controller/{BlogController(3), CommentApiController(2), PostApiController(2), SiteController(3)}`. **Audit fix (Phase 1 review):** in `packages/pagekit/blog/src/Model/Post.php`:
     - line 92 — change `public mixed $user = null;` to `public ?\Pagekit\User\Model\User $user = null;` (the existing `BelongsTo` relation already targets `User`).
     - line 96 — change `public mixed $comments = null;` to `/** @var array<int, \Pagekit\Blog\Model\Comment>|null */ public ?array $comments = null;` (the `HasMany` relation hydrates an array).
     - line 101 — type the `protected static array $properties` value-type annotation as `array<string, string>`.
     - lines 107, 150 — `getStatuses()` and `jsonSerialize()` return-type generics (`array<int, string>` / `array<string, mixed>`).
     - `PostModelTrait::saving/deleting/getAuthors` — add parameter and return types.
     Replace each `readonly mixed $foo` in the four controllers with the concrete type derived from `packages/pagekit/blog/index.php`. Commit: `refactor(types): blog package return types + Post model relation typing`.
  15. **`packages/pagekit/theme-one/`.** 7 L6 errors in `functions.php` (`attrs()`, `image()`, `bgImage()`, `isImage()`). Add `array $attrs`, `string $url`, `string $link`, `array $options` parameter types and `string` / `array<string, mixed>` return types. Commit: `refactor(types): theme-one return types`.
  16. **Bump PHPStan to Level 6 + ControllerResolverTest decision + final baseline cleanup.**
     - Edit `phpstan.neon`: `level: 5` → `level: 6`.
     - **`app/modules/kernel/src/Tests/ControllerResolverTest.php` decision (§5 of task prompt):** keep the 6 `private readonly mixed $` properties as-is (they intentionally exercise the `ControllerResolver`'s name-based resolution under test). Do NOT add `@phpstan-ignore` annotations — `mixed` is a fully-typed PHP type and PHPStan Level 6 does not flag it as missing. Add a single class-level docblock above `class ControllerResolverTest extends TestCase` documenting the rationale: `/** @phpstan-type ResolverTestService = mixed — properties below intentionally typed mixed to verify the resolver resolves by parameter NAME, not TYPE. See Step 2.1.4 §5.1 of the modernization roadmap. */`. Commit message must call out this decision verbatim.
     - **Surgical baseline cleanup:** run `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M`. For every "Ignored error pattern ... was not matched in reported errors" warning, surgically remove that single entry (its `-` block including `message`/`identifier`/`count`/`path` keys) from `phpstan-baseline.neon`. Do **not** run `--generate-baseline`. Final state: `phpstan-baseline.neon` `git diff` shows removals only (or zero changes), and `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` exits 0.
     - **Final sanity check:** `grep -rn "readonly mixed \$" app/ packages/ | grep -v "app/modules/kernel/src/Tests/ControllerResolverTest.php" | grep -v "app/vendor" | wc -l` returns `0`.
     Commit: `chore(phpstan): raise level to 6 + document ControllerResolverTest test-only mixed`.

## TESTING STRATEGY
- **Per step (Tester subagent):** PHPUnit + PHPStan (mandatory after every checklist step).
  - Steps 1–15 run PHPStan at the **config-defined level (5)** — exit 0 required, plus `git diff --quiet phpstan-baseline.neon` (no baseline additions and no `count: N → N+1` increments on existing entries; only Step 1 has an allowed surgical removal of the two `packages/composer/*` entries; Step 4 has an allowed surgical removal of the four `Pagekit\Tests\IOException` entries (lines ≈ 778–799) made unmatched by the `IOException` `use` import added alongside the `FileUtil` trait relocation; Step 7 has allowed surgical removals of (a) the `offsetAccess.notFound` entry on `MenuApiController.php` (made unmatched by the architect amendment 2026-05-04 #2 fix on `indexAction()` — `'count' => 0` placeholder on the trash literal + local `$trashCount` accumulator inside the existing foreach + `unset($menu)` after the loop + post-loop conditional rewritten against `$trashCount` instead of `$menus['trash']['count']`; amendment #1's "single-line literal addition" is empirically falsified by a `booleanNot.alwaysTrue` error at line 40 and is superseded — see Checklist Step 7 for the full rationale) and (b) the two `method.alreadyNarrowedType` `assertIsArray`/`assertIsString` entries on `MailControllerTest.php` and `SendmailTransportTest.php` made unmatched by the test rewrite that drops the redundant assertions; **Step 8 has allowed surgical removals of the 24 cross-module baseline entries enumerated in Checklist Step 8 (architect amendment 2026-05-04 #3, scope expansion #2)** — these are the entries on `app/modules/database/src/ORM/Relation/{BelongsTo,HasMany,HasOne,ManyToMany}.php`, `app/system/modules/comment/src/Model/Comment.php`, `app/system/modules/site/src/Controller/{MenuApiController,NodeApiController}.php`, `app/system/modules/site/src/Model/{Node,Page}.php`, `app/system/modules/user/src/Controller/{UserApiController,UserController}.php`, `app/system/modules/user/src/Model/{Role,User}.php`, `app/system/modules/widget/src/Model/Widget.php`, `packages/pagekit/blog/src/Controller/{CommentApiController,PostApiController,SiteController}.php`, `packages/pagekit/blog/src/Model/Post.php` made unmatched by the retroactive Step 2 (Auth event narrowing) + Step 3 (ORM trait `instanceof static` guards, `get_class_vars` lookup, QueryBuilder `@method` docblocks) cleanup that Step 8's typing chain reaction forced into Step 8's commit; Step 13 has allowed surgical removals of the two `TranslationFetchCommand` / `ExtensionTranslateCommand` `void`-vs-`int` entries; Step 16 has the final cleanup pass).
  - Steps 2–15 additionally run a **scoped Level-6 check**: `./app/vendor/bin/phpstan analyse --level=6 --no-progress --memory-limit=512M <step's directory(s)>` and assert `0 errors` (or, for Steps 2–6, that the count matches the discovery-snapshot deltas for the directories already processed). The Tester must NOT run a global L6 sweep on intermediate steps — that fails until Step 16. Document the scoped-paths command for each step in the Tester's per-step run log. Step 1.5 has its own scoped Level-6 check: `./app/vendor/bin/phpstan analyse --level=6 --no-progress --memory-limit=512M app/modules/application/src/Tests` must report 0 `trait.unused` errors (the `FileUtil` file is gone) and `./app/vendor/bin/phpstan analyse --level=6 --no-progress --memory-limit=512M app/modules/filesystem/src/Tests` must show the 16 `missingType.*` errors against the relocated trait file (these are then fixed in Step 4). The Step 2 scoped command (`app/modules/{kernel,application,config,log,session,auth,migration}`) therefore needs **no scope expansion** — the trait relocation done in Step 1.5 is what makes the Step 2 scoped L6 check pass cleanly (architect amendment 2026-05-04, option (b)).
  - **Step 3 `trait.unused` exemption (architect amendment 2026-05-04, option (d), standing convention).** The scoped L6 command for Step 3 is `./app/vendor/bin/phpstan analyse --level=6 --no-progress --memory-limit=512M app/modules/database` and it WILL report 2 `trait.unused` errors against `app/modules/database/src/ORM/ModelTrait.php` and `app/modules/database/src/ORM/PropertyTrait.php` (these traits' 7 consumer model files live across 5 other modules — `app/system/modules/{widget,user,site,comment}/Model/*` and `packages/pagekit/blog/src/Model/*` — and are out of scope for the per-step check). Tester treats these 2 specific errors as scoping artifacts and EXCLUDES them from the gate: assert that the Step 3 scoped L6 output contains exactly 2 `trait.unused` errors and that both target one of those two file paths, then assert all OTHER errors are zero. Step 16's global L6 (where every consumer is in scope) is the final authority — it must report 0 `trait.unused` total. This same exemption pattern applies to any cross-module foundational-API trait discovered in later groups (Groups C–D, Steps 4–6); relocation (Step 1.5 option (b)) remains preferred when a single-module relocation target exists, but option (d) is the standing answer when multiple consumer modules exist and no natural relocation target exists. No `phpstan.neon` / `phpstan-baseline.neon` / `@phpstan-ignore` / `// TODO` edits are permitted to silence these scoping artifacts — the exemption lives in the Tester's gate logic only, not in the codebase.
  - Step 16 runs the **global Level-6 check**: `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` (now under `level: 6`) and asserts exit 0. Tester also runs the `grep -rn "readonly mixed \$" ...` sanity check and asserts count `0`.
  - PHPUnit runs unconditionally on every step; the suite must stay green throughout.
- **Final run (after Early Push, Tester subagent):** wait on the four PHP Quality CI jobs (`phpunit (8.2)`, `phpunit (8.3)`, `phpstan`, `cs-fixer`, `security-audit`) via `gh run watch` and run the 3 Playwright E2E specs locally **in parallel**; both must pass. See `.cursor/agents/tester.md` § End-of-ticket tests for the exact commands and `.cursor/rules/orchestrator-subagent-workflow.mdc` § Final Test for the workflow position.
