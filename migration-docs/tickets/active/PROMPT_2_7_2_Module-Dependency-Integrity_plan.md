## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.7.2
- **Scope:** Fail-closed module requirements (done in Checklist Step 1), a reverse dependency index, a downward module graph (foundation joins the existing `kernel` module; composition root stays `application`; no alias), archive install refusal, disable/uninstall pre-flight (panel + API + console), restore pre-flight, and prefix-folded dump selection. Remaining work: move `UrlProvider` / `Response` / `#[Access]` into `routing`; relocate `Application`, `Event`, `Module`, `Container`, `Util` (and Step 1's `UnsatisfiedRequirementException` / `ModuleManager`) into `app/modules/kernel` under their current namespaces beside the existing `Pagekit\Kernel\` HTTP types; absorb `view/twig` into `view`; break sibling imports; import-edge test; `PackageArchive` `require` refusal; prefix fold + empty-dump refusal; disable/uninstall pre-flight; console `enable` / `disable` (same seam as the panel — panel-only activation is rejected); restore pre-flight. Extensions and themes UI (`app/package/app/lib/package.js`, `uninstall.vue`, `views/extensions.php`, `views/themes.php`). PHPUnit around the graph, archive, pre-flight, dump, and restore.
- **Deferred:** Step 2.7.3 — static discovery must carry `require` and retarget the archive parser that reads it (PHASE §2.7.3 already states this). Step 2.8 — the author contract states the unregistered-`require` refusal (PHASE §2.8 already states this). Step 5.0 — automatic orphan removal and install-reason bookkeeping; a package-scoped restore; a purge that drops the tables the pre-flight names (PHASE §5.0 already states this). Non-goals: which characters an install prefix may contain; marketplace (5.6).
- **Bridges:** None. No class alias, no forwarding subclass, no `is_callable` or `mixed` stand-in, no second type whose only job is to hide an import. Remove `// TODO: Must be refactored in Step 2.7.2 (Module Dependency Integrity)` from `PackageController::uninstallAction` once the pre-flight runs before the snapshot (Checklist Step 8). No new step tags.
- **Checklist:**
  1. **Fail-closed requirements and `requiredBy`.** `ModuleManager::resolveModules()` must not record a depender whose `require` it did not satisfy, and it must not log and continue. A name that is not in the registered set throws, and the message names the depender and the missing module and says it is not registered. A name that is registered but not active throws a different message naming both and saying it is registered but disabled. Active means: in the enabled set, or inside the always-loaded closure. The enabled set is system config `extensions` plus `site.theme` when that value is a non-empty string. The always-loaded closure is the transitive `require` of the boot module (`system` once `SystemModule::main` is running). Core modules stay loadable because they sit in that closure, not because they appear in `extensions`. Do not recurse into a registered-but-disabled package (that would load it). A cycle still throws the existing circular-requirement message, including when a package is activated. `load()` of a name that was asked for directly and is not registered still throws `Undefined module`. Already-enabled extensions whose requirement fails do not take the request down: `ExtensionLoader` already catches the throw, auto-disables the extension, and stores the message on the existing failure record (the admin notice). Themes keep today's rule (recorded, not switched off). Set the activity policy at the start of `SystemModule::main`, before `ExtensionLoader::load`, so extension boot sees it. `load('system')` itself still fails the request when a core `require` is not registered — that path is not an extension auto-disable. Build `requiredBy` from the registered manifests once per registration (invalidate when `register()` runs again) so a lookup does not walk every manifest. `PackageManager::enable()` runs the same check before any config write, lifecycle hook, or `package.enable` event, including when the module is already loaded (an update does not re-walk inside `load()`). Surface that refusal on the enable action even when debug is off; other failures stay the generic enable error. No override flag.
  2. **Routing owns URL helpers.** Move `Pagekit\Application\UrlProvider` and `Pagekit\Application\Response` to `Pagekit\Routing\UrlProvider` and `Pagekit\Routing\Response`, files under `app/modules/routing/src`. Update every call site, including `packages/pagekit/*`, tests, and `application/index.php` (it constructs them). Delete the old classes. `routing` does not require `application`. No alias.
  3. **Kernel owns the foundation; each prefix has one directory.** The `kernel` module already exists and owns `Pagekit\Kernel\` (HttpKernel). Move into that same module directory, keeping namespaces: `Pagekit\Application` (the class), `Pagekit\Application\TrustedProxies`, `Pagekit\Application\Exception`, `Pagekit\Application\Console`, `Pagekit\Event`, `Pagekit\Module` (including `ModuleManager` and `UnsatisfiedRequirementException` from Step 1), `Pagekit\Container`, `Pagekit\Util`, and their tests. Retarget Composer `"Pagekit\\"` from `app/modules/application/src` to `app/modules/kernel/src`. `kernel`'s manifest `autoload` covers the namespaces it now owns (existing `Pagekit\Kernel\` stays). Do not rename HttpKernel types. `application/index.php` requires `kernel` and keeps its stack `require`; it no longer contains those classes. `kernel` requires none of `application`, `routing`, `database`, `auth`, `session`, `view`, or `system/*`. Activity-policy behaviour from Step 1 must not change — only the file home. `system/settings` stops autoloading `Pagekit\System\`; `SettingsController` becomes `Pagekit\Settings\Controller\SettingsController` with that prefix only on `system/settings` (Composer `Pagekit\System\` stays on `app/system/src`). Move `FileLocatorAsset` and `Pagekit\View\Event\ResponseListener` into `app/modules/view/src` and delete `system/view`'s `Pagekit\View\` autoload; `system/view` requires `view`. `view/twig` is not a module: move its wiring into `view/index.php`, autoload `Pagekit\Twig\` from `view`, delete the `view/twig` manifest, the `include` of it, and `view`'s `require` of it. `TwigLoader` may use `FilesystemLoader` because both belong to `view`. No alias.
  4. **Break the remaining sibling imports.** `Pagekit\User\Attribute\Access` becomes `Pagekit\Routing\Attribute\Access`; delete the old class; `AccessListener` stays in `system/user` and imports the routing attribute; update every `#[Access]` import, including `packages/pagekit`. Add `UserInterface::isAuthenticated(): bool`; `User` keeps the role check; `CaptchaListener` uses that interface and `Auth` and drops `Pagekit\User\Model\User`; the `UpdateCheckOnLoginTest` double implements the method. Move `CacheKeyUtil` to `Pagekit\Util`; `MetadataManager` and `LoginAttemptListener` import it there. `Filesystem::getUrl` uses the constants on `Symfony\Component\Routing\Generator\UrlGeneratorInterface` and does not import `Pagekit\Routing`. `database/index.php` does not name `Pagekit\Debug`; the debug module registers the DBAL middleware, and database applies container-held middlewares, skipping them when that service is absent. No alias, no `is_callable`, no `mixed`, no type whose only job is to hide `Mailer`.
  5. **Declared edges match imports.** A test reads every core module (`app/modules/*/index.php`, `app/system`, `app/system/modules/*`, `app/package`, `app/installer`, `app/console` — not `packages/pagekit/*`) for `use Pagekit\…` and for attribute classes that resolve to `Pagekit\…` (imported short names and FQCNs). The owner is the module directory that contains the class file (longest path). A module's own directory is not an edge. The importer's transitive `require` must reach that module. A `class_exists` or string probe is not an edge. An import of a module that already requires the importer fails the test; move the type. Do not add a test exception for parent imports. Add the manifest `require` entries the test reports. No override.
  6. **Archive `require` before any package file is written.** `PackageArchive` reads `require` from `index.php` the same way it reads `autoload`: a list of literal strings in the literal `return` array, without executing the file. Absent means none. A non-literal `require` is refused like a non-literal `autoload`. Expose the list the same way `autoload()` is exposed today. One check, used by `PackageController::uploadAction` (before the upload is staged) and by `PackageManager::install()` (before `replaceTree`, so `php pagekit install` and the panel install both hit it): every name must be registered, and the message names the missing module. Throw `ArchiveRefusedException` so the upload's existing catch turns it into a 400 and the install command's existing `RuntimeException` catch prints it. When `install()` is about to `enable()` an already-loaded package, also run the activation rule from Step 1 (`assertRequirements` / cycle once the archive's `require` is added to the registered graph) before `replaceTree`, using the archive's list — the in-memory manifest is still the previous one. A fresh install that does not enable may require a module that is registered but disabled; activation refuses later. A refusal writes nothing under `packages/`.
  7. **Prefix fold, and refuse an empty dump when it is taken.** `DatabaseDumper::schema()` and `DatabaseRestorer::name()` compare the prefix byte for byte today; collision checks fold through `comparable()` and `lower_case_table_names`. Move that fold to one helper beside `RestoreTableNames` and point selection, the ownership check in `name()`, and the collision checks at it, so they cannot drift. SQLite and a MySQL server that does not fold (value other than `1` or `2`, or no answer) keep a byte comparison. A dump whose selection is empty throws before `stage()` writes a file a snapshot could mark whole, from `dump()` / `read()`, with the staging file removed by the existing failure path. The message says the dump selected no table. `DatabaseRestorer::end()` still refuses a dump that holds no tables. Uninstall already snapshots first, so this refusal removes nothing.
  8. **Disable and uninstall pre-flight.** One query both the API and `PackageManager` call. It reports three things, and nothing else: **blockers** — enabled modules (the `extensions` list and the active theme) that `requiredBy` says require the target; they would keep a requirement nobody satisfies; **orphans** — modules the target requires that are outside the always-loaded closure and that no remaining enabled module requires (reported only; not removed and not a refusal); **data risk** — a hint from what already exists, with no new manifest field: whether the package lifecycle exposes a `MigrationSet`; whether the config service has a row named for the module (the `packages.{module}` version key inside the system row does not count); node-type ids from the `nodes` key on the registered manifest; live tables whose folded name starts with the folded installation prefix, the module name, and `_` (the helper from Step 7). Blockers throw before `disable()` runs hooks or pulls `extensions`, and before `uninstall()` takes a snapshot. Several names in one call are all checked before the first snapshot or the first disable. No override. `PackageManager::disable()` of the active theme, when the pre-flight allows it, clears `site.theme` (enable already sets it; disable currently leaves it for themes). Delete the Step-2.7.2 TODO on `PackageController::uninstallAction`, and delete the method comment that says the confirm cannot yet say what else was counting on the package. Add a package route that returns this query. The controller is not the only guard.
  9. **Panel and console.** Extensions: the status toggle and the uninstall confirm read that route and show blockers, orphans, and the data-risk hint. Themes: the uninstall confirm shows the same result (that page's destructive control). Blockers remove the proceed action. Enable keeps today's error notify, which now carries the named requirement refusal. Console: `enable` and `disable` commands, same shape as `UninstallCommand`, calling `PackageManager::enable()` / `disable()` so they share the pre-flight. There is no `--force`. `php pagekit install` still does not enable; its help says that install places the package and runs the install lifecycle, and that activation is `php pagekit enable`. `php pagekit uninstall` picks up the pre-flight because it already calls `PackageManager::uninstall()`. Operator sentences go through `__()` in the panel and the command output. Vue/JS stays within the repo prettier and eslint rules.
  10. **Restore pre-flight before any package file is put back.** `PackageSnapshotter::details()` records the running application version (`$app->get('version')`, the application module's `version`) on new metadata only. The dump format, including `DumpFormat::VERSION`, stays as it is. `SnapshotStore` reads the key back; a snapshot that has none exposes it as missing, not as a guessed version. `restore()` asks, before `reinstate()`: the recorded application version against the running one, and the dump's `packages.*` map against the running system config. The map is the `packages` object on the `system` row of the dumped config table (`@system_config`), read from the dump, not from a new header field. Refuse when the recorded version is missing or differs, and for every module name whose version differs or that is present on only one side. The message names the snapshot and each module. The same answer includes what a MySQL restore would refuse — no table prefix, a copy name past 64 characters, a reserved `_r_` / `_b_` name that is not this installation's, an inbound foreign key from a table outside the dump, another restore holding the lock — via a query method on `DatabaseRestorer` that runs those checks and releases the lock before it returns. It does not clear leftovers and does not replace tables. `restore()` still performs its own checks when it later runs. A race between release and the real acquire is acceptable because `restore()` checks again. Operator refusals are one exception type extending `RuntimeException` (so existing `expectException(RuntimeException::class)` still matches); `SnapshotController::restoreAction` returns that message in the existing `message` field. Any other throwable keeps today's fixed line and the log. The snapshots page already notifies with that field; drop the comment that says the real reason is only in the log. A refusal leaves `packages/` as it was. An internal failure after `reinstate()` has started stays the existing restore failure path.
  11. **Review (Bugbot + Security) + E2E.** No production refactor in this step. Bugbot, then security review, then the final E2E run, including fix-loops those reviews require.

## EXECUTION STATE
<!-- Machine-readable progress index for the Orchestrator/Conductor. Mirrors the Checklist 1:1
     (same numbers + short titles). Size hint per step: S = small/atomic, M = medium,
     L = large or loop-risk, XL = Review (Bugbot + Security) + E2E (mandatory last step, weight 8).
     A step orchestrator flips its box to [x] in the SAME commit as that step's code + tests
     (after full step PASS incl. test-writer when applicable; for XL after reviews + E2E PASS) -->
- [x] Step 1 (L) — Fail-closed requirements and requiredBy
- [x] Step 2 (L) — Routing owns URL helpers
- [x] Step 3 (L) — Kernel owns the foundation
- [x] Step 4 (L) — Break sibling imports
- [x] Step 5 (M) — Import edges match manifest require
- [x] Step 6 (M) — Archive require refused before write
- [x] Step 7 (M) — Prefix fold and empty-dump refusal
- [x] Step 8 (L) — Disable and uninstall pre-flight
- [x] Step 9 (M) — Panel and console activation
- [x] Step 10 (L) — Restore pre-flight before files return
- [x] Step 11 (XL) — Review (Bugbot + Security) + E2E

## TESTING STRATEGY
- **Per step (production gate):** Refactorer → Verifier → Tester (PHPUnit + PHPStan) — production code must be green before any new tests are written
- **Per step (coverage — inline light):** test-writer → Verifier (test files only) → Tester (PHPUnit + PHPStan) — **skip** when the step changes no production PHP under `app/` or `packages/` (docs/config/ROADMAP-only steps); mark those steps `test-writer: skip` here
- **Per step notes:**
  - Step 1: resolver tests use fixture modules declared in the test, not first-party packages. Cover unregistered vs registered-but-disabled (messages differ and the depender is not loaded), a cycle at `enable()`, `requiredBy` including a depender, the active theme counted as enabled, and `ExtensionLoader` auto-disable plus the failure record when an already-enabled extension's requirement fails. A direct `load()` of an unknown name still throws `Undefined module`.
  - Step 2: call sites construct `Pagekit\Routing\UrlProvider` and `Pagekit\Routing\Response`. The old classes are absent. `routing` does not require `application`.
  - Step 3: `Pagekit\Event`, `Pagekit\Module`, `Pagekit\Container`, `Pagekit\Util` and `Pagekit\Application` resolve from `kernel`. Composer `"Pagekit\\"` points at `kernel/src`. `Pagekit\Kernel\` HttpKernel types are unchanged. `kernel` does not require the stack. Step 1 activity-policy / `requiredBy` / enable-refusal suites stay green without behaviour change. `SettingsController` is `Pagekit\Settings`. One directory owns `Pagekit\View\`. There is no `view/twig` module. `application` requires `kernel`.
  - Step 4: `#[Access]` resolves to `Pagekit\Routing\Attribute\Access`. `CaptchaListener` does not import `Pagekit\User`. `CacheKeyUtil` is `Pagekit\Util\CacheKeyUtil`. `Filesystem` does not import `Pagekit\Routing`. `database` does not name `Pagekit\Debug`.
  - Step 5: the import test is the gate. It fails when a `use` or attribute has no transitive `require` to the module that contains the class, including when that edge would cycle. No parent-import exception.
  - Step 6: `PackageArchive` refuses a non-literal `require`; upload and `PackageManager::install` refuse an unregistered name before anything is written under `packages/`; an update that would enable refuses a registered-but-disabled requirement before `replaceTree`.
  - Step 7: extend `DatabaseDumperTest` / `DatabaseRestorerTest`. An uppercase prefix on a folding MySQL server selects the installation's tables; byte-identical names still match when the server does not fold; a selection of no table throws at dump time and leaves no complete dump.
  - Step 8: blockers stop `disable()` and `uninstall()` before a snapshot exists; orphans and data-risk are in the payload and do not stop it; the theme counts as a blocker; several names are all refused before the first change; table names use the Step 7 fold; theme disable clears `site.theme`.
  - Step 9: PHPUnit covers the `enable` / `disable` commands (same refusals as the manager, no `--force`) and the install command help stating that install does not activate. The Vue change must stay prettier- and eslint-clean (frontend CI). The browser pass is the XL E2E run.
  - Step 10: a version mismatch and a `packages.*` mismatch refuse by name before `reinstate()`; a snapshot with no recorded application version refuses by name; a MySQL refusal (prefix, length, reserved name, inbound FK, lock) refuses before files change and `packages/` is unchanged; that message is the panel payload; an unrelated throwable still gets the fixed line.
  - Step 11: `test-writer: skip` — review and E2E only.
- **Review + E2E (Execute — mandatory last `(XL)` step):** Bugbot → Security → Tester `"final E2E run"`. Not gated on PR/CI. Details: `orchestrator-v2-step.mdc`.
- **Finalize:** `orchestrator-v2-finalize.mdc` (PR → CI → optional coverage → PR Bugbot/Security → version only if product).
- **Deferred / Out-of-Scope (optional):** Static module discovery and a new manifest format (2.7.3), including retargeting the `require` reader. Package author contract text (2.8). Orphan removal, install-reason bookkeeping, package-scoped restore, and purge-that-drops-owned-tables (5.0). Which characters an install prefix may contain. Marketplace (5.6).

## IMPLEMENTATION NOTES
<!-- Refactorer-owned. One bullet per decision the plan left open or the Refactorer deviated from:
     `File::symbol` — chosen vs. rejected, why, the invariant a test must hold. Files that follow the
     plan get no line; a step with no open decision gets `none`. Architect leaves every step `_none yet_`. -->
### Step 1
- `ModuleManager::isActive` — Before `setActivityPolicy`, a registered module is active. Rejected treating "not in extensions" as disabled then, because `load('system')` walks core requirements before `SystemModule::main` can name the closure. Invariant: an unregistered require throws out of `load('system')` naming the depender and saying "not registered", and that throw is not an extension auto-disable; a registered-but-disabled require throws only after the policy is set, and that module is not loaded.
- `ModuleManager::assertRequirements` — Returns when the root is not registered. Rejected throwing `Undefined module` from `enable()`, because the installer enables packages without registering `packages/` and the existing enable suites do the same. Invariant: `load()` of an unknown name still throws `Undefined module: $name`; `enable()` of a registered module throws `UnsatisfiedRequirementException` or `Circular requirement "%s > %s" detected.` before any config write, lifecycle hook, or `package.enable`.
- `ModuleManager::resolveModules` — A cycle reachable only through a disabled module is reported as registered-but-disabled. Rejected walking that module to find the cycle, because walking it would load it. Invariant: a cycle among the module being activated and modules that are active still throws the existing circular-requirement message; the disabled module is absent from the loaded set.
- `UnsatisfiedRequirementException::messageId` — The exception message is the English sentence with the names filled in; `PackageController::enableAction` translates that id even when debug is off. Rejected calling `__()` inside the resolver, because the walk runs before the translator is booted. Invariant: both the exception message and the enable `error` name the depender and the requirement and say either "not registered" or "registered but disabled"; any other enable failure, including a circular requirement, stays the generic enable error when debug is off.
- `ModuleManager::requiredBy` — Direct dependers only, in registration order, duplicate entries once. The cache is dropped on `register()`, and the always-loaded closure (the boot module plus registered modules reachable through `require`) is rebuilt then. Rejected scanning every manifest per lookup, and counting transitive dependers. Invariant: if A lists B in `require`, `requiredBy('B')` contains `A` whether or not A is enabled; a module `system` requires is active after the policy is set even when it is absent from `extensions`.
- `SystemModule::activeModules` — Enabled set is module-config `extensions` plus a non-empty `site.theme`, copied at the start of `main` (the list `ExtensionLoader` loads). Rejected re-reading the config service on each check, because `enable()` pushes onto that service object and not this list. Invariant: a theme set when `main()` starts satisfies a requirement even when it is absent from `extensions`; a name pushed onto the config service later in the request does not.
- `PackageManager::assertPackageRequirements` — Parameter is `string`. A non-string `Package::get('module')` skips the walk. Rejected `mixed` on the parameter: the walk accepts a module name, and `Package::get` is the generic container. Invariant: `enable()` of a package whose `module` value is not a string does not throw from the requirement walk; a non-empty registered name still throws `UnsatisfiedRequirementException` or `Circular requirement "%s > %s" detected.` before any config write, lifecycle hook, or `package.enable`.
- `Arr::pull` — Pack the list by assigning `array_values` through the by-ref parameter. Rejected `$array = &$packed`, which rebinds the parameter so `Config::pull` writes the gapped array back. Invariant: `Config::pull('extensions', $name)` on `['needs-off', 'pages']` leaves `['pages']` at index 0.
- `PackageController::errorHandler` — The cleanup closure calls `restore_error_handler()` and `restore_exception_handler()`. Rejected reinstalling the previous callable with `set_error_handler` / `set_exception_handler`, because that pushes another frame and, when the previous handler is null, skipping the call leaves the frame in place. Invariant: after `enableAction()` returns, both handler stacks match the ones that were active when the action was entered.
### Step 2
none
### Step 3
- `kernel/index.php` autoload — `Pagekit\` and `Pagekit\Kernel\` both map to `src`. Rejected a prefix per sub-namespace (`Pagekit\Application\` → `src`): that would resolve `Pagekit\Application\Exception` to `src/Exception.php` and would not load the `Pagekit\Application` class. Invariant: `Pagekit\Application`, `Pagekit\Container`, `Pagekit\Event\Event`, `Pagekit\Module\ModuleManager` and `Pagekit\Util\Arr` load from `app/modules/kernel/src`, and `Pagekit\Kernel\HttpKernel` still loads from `app/modules/kernel/src/HttpKernel.php`.
- `composer.json` `Pagekit\Twig\` — mapped to `app/modules/view/src/Twig`, the same directory as the view manifest. Rejected leaving it off Composer: an optimized dump scans `Pagekit\View\`'s `src` and skips a `Pagekit\Twig` class sitting in that tree. Invariant: `Pagekit\Twig\TwigLoader` is `app/modules/view/src/Twig/TwigLoader.php` and there is no `view/twig` module.
### Step 4
- `debug/index.php` `db.middlewares` — One `DebugMiddleware` and its logger, registered at the start of `main` (the bar may be off), shared by every connection. Rejected a new interface and rejected constructing it in `database/index.php`: that file must not name `Pagekit\Debug`. Rejected a new logger per connection: the bar would keep only the last one. Invariant: when `db.middlewares` is absent, `dbs` adds no wrapper; when the debug module has loaded, every connection is wrapped with that same middleware and `db.debug_logger` is the logger the bar reads.
- `database/index.php` — Appends `Doctrine\DBAL\Driver\Middleware` entries from `db.middlewares` after any already on the connection, and skips other entries. Rejected replacing the key, which would drop ones the connection config listed. Invariant: an absent `db.middlewares` service leaves the connection's own list unchanged; a non-middleware entry is not wrapped.
- `AdministratorWhoUpdates::isAuthenticated` — Returns true. The double is the signed-in administrator the login check runs for. Invariant: the class implements `UserInterface`; `CaptchaListener` calls `UserInterface::isAuthenticated()` and does not import `Pagekit\User`.
- `Filesystem::getUrl` — `UrlGeneratorInterface::ABSOLUTE_PATH` is `1` and `NETWORK_PATH` is `3`, the integers the Pagekit generator inherited. Invariant: `getUrl($file)` is the path form, `getUrl($file, 3)` the network path, and `getUrl($file, true)` the absolute URL.
### Step 5
- `ValidatesRequestTrait` — `Pagekit\Routing\ValidatesRequestTrait`. Rejected leaving it on `system`: site, user, and widget import it and `system` already requires them. Rejected `system/site` as the home: user and widget use it too, and site already imports user. Invariant: those modules require `routing` and do not import `Pagekit\System`.
- `DataModelTrait` — `Pagekit\Database\ORM\DataModelTrait`. Rejected site, user, or widget (site imports user; widget imports both) and rejected `kernel` (the trait imports `Pagekit\Database`). Invariant: site, user, and widget require `database` and do not import `Pagekit\System\Model`.
- `NodeInterface` / `NodeTrait` — `Pagekit\Site\Model`, beside `Node`, the only implementor. Invariant: no core module outside `system/site` imports them.
- `Unique` / `UniqueValidator` — `Pagekit\Database\Validator\Constraints`, because the validator takes `Connection`. Rejected `system/user`: `system` registers the validator for every model. Invariant: `User` imports `Unique` from database; `ValidatorServiceProvider` imports `UniqueValidator`, and `system` reaches database through `application` with no direct `database` require.
- `use function` / `use const` — Not an edge. `__()` has no class file. Rejected requiring `system/intl` from every controller that imports the function. Invariant: `use function Pagekit\__` adds no require.
- `use Pagekit\Database\ORM\Attribute` — A namespace import, not a class (the interface is `Attribute\Attribute`). The attribute classes under that prefix are the edges. Invariant: comment, site, user, and widget require `database`; that same use inside database is not an edge.
- Edge scan — Production PHP only. Rejected scanning `Tests/`: a database test that imports `Application` would cycle, because `application` requires `database`, and `Application` cannot move. Invariant: the import test ignores `Tests/` and `tests/` and allows no parent-import exception in production code.
### Step 6
- `PackageArchive::requirementList` — Names go into a PHP array and an omitted key is appended with `$values[]`, so the list is the one `foreach` yields. `"-0"` stays a string key; the omitted element after it is integer 0 and does not replace it. From PHP 8.3 a negative integer continues at n+1, so after `"-4"` the omitted name is `-3` and a later `"0"` does not replace it. Rejected `integerKey()` plus a `$next` that starts at 0: `(string)(int)"-0"` is not `"-0"`, so the counter stayed 0 and stores the element after `"-4"` at 0, where a later `"0"` overwrites it and the counter's list is `['kept', 'system']`. That name was never overwritten in the PHP array, so it is not dropped from `require()`. An omitted key once the next index cannot be allocated is refused, because that literal throws. A missing `require` is `[]`, including when a spread precedes the literal keys and `require` is not written again. A spread, a non-string value, or a non-literal key inside `require` is refused like a non-literal `autoload`. Rejected executing the file, and rejected refusing an archive whose spread is followed by a literal `name` and `autoload` but no `require`. Invariant: `require()` equals that `foreach` when the literal can be built; `['-0' => 'unregistered', 'system']` is `['unregistered', 'system']`; `['-4' => 'kept', 'unregistered', '0' => 'system']` is `['kept', 'unregistered', 'system']`; `open()` throws `ArchiveRefusedException` mentioning `'require'` when the value is not an array of string literals or the next index is past `PHP_INT_MAX`, and writes nothing.
- `ModuleManager::assertRequirementsUsing` — Sets `$require` on the named manifest, inserting one when the name is not registered, and restores that manifest and the `requiredBy` cache whether the walk returns or throws. Rejected leaving the list in place: `replaceTree` can still fail. Invariant: after the call, `isRegistered` and `requiredBy` match what they were before; a cycle still throws `Circular requirement "%s > %s" detected.`; a registered-but-disabled requirement still throws `Module "%depender%" requires "%required%", which is registered but disabled.`; `enable()` afterwards still walks the require from before the call.
- `PackageManager::assertArchiveRequirements` — The first unregistered name throws `ArchiveRefusedException` (`Module "%depender%" requires "%required%", which is not registered.`, archive text through `printable`, depender is `PackageArchive::module()`). The activation walk runs only when `install()` is about to `enable()`, on `PackageArchive::module()`. Rejected walking on upload and on a fresh install: a registered-but-disabled requirement is allowed until activation. Rejected overlaying the previous package's `module` value: the archive's `require` belongs to the module `index.php` names. Invariant: upload of an unregistered name is a 400 before `move()`; `install()` throws before `replaceTree`, so nothing is created under `packages/`; a fresh install whose requirement is registered but disabled still unpacks; an update of a loaded package whose archive requires a registered-but-disabled module throws before `replaceTree`.
- `PackageManager::moduleManager` — A `module` service that is not a `ModuleManager` cannot show a name as registered, so a non-empty `require` is refused as not registered. An empty `require` with nothing to activate does not read the service. Rejected skipping the check: a name nobody can look up would be installed. Invariant: with no `ModuleManager`, `require()` of `['missing']` throws `ArchiveRefusedException` naming `missing` before a file is written; `require()` of `[]` does not read the service.
### Step 7
- `TableNameFold::prefixed` — A byte prefix matches without asking the server. The server is asked only when the names differ by case, and only `lower_case_table_names` of `1` or `2` (string or int) then counts the table; a folded mismatch does not ask. Rejected folding on every comparison: `name()` runs while the dump is read, before the lock, and a neighbour table would ask on every dump. Invariant: `pk_items` against `pk_` issues no `SHOW`; `other_items` against `pk_` issues none; `pk_items` against `PK_` matches only when the answer is `1` or `2`; SQLite never runs that statement; any other value, or no row, keeps the byte comparison. A same-case MySQL refusal still asks `lower_case_table_names` after `GET_LOCK` and before `REFERENTIAL_CONSTRAINTS`. `RestoreTableNames::isReserved` stays a byte match.
- `DatabaseDumper::read` — Throws `The dump selected no table.` before `stage()` only when the database lists at least one table and the prefix selects none. A database that lists no table is written (header and the closing line, zero tables). Rejected throwing then: uninstall snapshots first, and the uninstall suites open SQLite that has never had a table, so the removal would stop with no dump file. Rejected putting the sentence on the outer exception: `dump()` already wraps every failure as `Failed to dump the database to "%s".` Invariant: a connection with no tables returns `['tables' => 0, 'rows' => 0]` and the finished path exists; a database whose only table lies outside the prefix throws, `getPrevious()` is exactly `The dump selected no table.`, and the finished path and the `.part` file are both absent. `DatabaseRestorer::end()` still throws `The database dump holds no tables, so there is nothing in it to restore.`
### Step 8
- `PackageImpact::blockers` — An enabled depender counts even when it is also in this call. A self-require does not. Rejected subtracting the whole call: `uninstall` would snapshot the first package, then `disable()` of that package alone would throw because the sibling is still enabled. Invariant: `uninstall` of A and B, where enabled B requires A, throws `RemovalBlockedException` before any snapshot and both stay installed; `disable()` of a module whose only depender is itself still pulls it from `extensions`.
- `PackageImpact::orphans` — Names in this call are not orphans. A required name is an orphan only outside the always-loaded closure and only when no enabled module outside this call requires it. Before `setActivityPolicy` the closure is empty. Invariant: `removalImpact` does not list the package's own module; it lists a required module that is not always-loaded when the package is the only enabled depender; after the policy, a module the boot module requires is not an orphan.
- `PackageImpact::query` / `dataRisk` — The only keys are `blockers`, `orphans`, and `dataRisk` (`migrations` bool, `config` bool, `nodes` list, `tables` list). `config` is `ConfigManager::has($module)`. `nodes` are string keys on the registered manifest, in key order. `tables` are the live names, byte-sorted, for which `TableNameFold::prefixed($name, $prefix . $module . '_')` is true. No `Connection` yields no tables. Invariant: `pk_blog_post` matches module `blog` and prefix `pk_` with no `SHOW`; `PK_blog_post` matches only when `lower_case_table_names` is `1` or `2`; `pk_blogpost` and `other_blog_post` do not; `packages.blog` on the system row leaves `config` false; a row named `blog` leaves it true. Orphans and data risk do not refuse.
- `PackageManager::migrationSet` — A throwable while reading the lifecycle is no `MigrationSet` and does not refuse. The pre-flight and the disable/uninstall hooks share one `LifecycleRunner`, so the file is required once when the read succeeds. Rejected a second `require`: a file that declares a class would fatal. Invariant: `scripts.php` that does not return a lifecycle still leaves `extensions` without that module and reports `migrations` false; a `migrations()` that returns a `MigrationSet` reports true and does not refuse; that same object then runs the disable hook and, on uninstall, the uninstall hook.
- `PackageManager::assertRemovable` — One `RemovalBlockedException` after every name is resolved and before the first snapshot or the first hook. One blocker and one module: `"%blocker%" requires "%name%", so it cannot be switched off.` Otherwise: `"%blockers%" require "%names%", so nothing was switched off.` Names are the module, or the package name when `module` is not a non-empty string. Invariant: `uninstall(['present', 'missing'])` throws `Unable to find "missing".` and does not snapshot `present`; a blocker names the depender and the target in that sentence and leaves `extensions` unchanged.
- `PackageManager::clearActiveTheme` — `site.theme` is removed only when the type is `pagekit-theme` and the value is the module name, and only after the pre-flight allows it. Rejected clearing on a module-name match for an extension. Invariant: disabling the active theme unsets `site.theme`; disabling another theme or an extension leaves it.
- `PackageController::impactAction` — `@system/package/impact` takes package `name`, requires CSRF, and returns the three keys without throwing on blockers. `disableAction` rethrows `RemovalBlockedException` as `BadRequestHttpException` with the same message. Invariant: the payload lists an enabled theme that requires the package; that disable is a 400 with the sentence and does not pull `extensions`.
### Step 9
- `EnableCommand::loadRegistered` — A registered module is loaded before `enable()`, because that load throws `UnsatisfiedRequirementException` and a successful enable runs `main()` the way the panel does. An unregistered name is not loaded and is passed to `enable()`, which does not walk it. Rejected calling `enable()` only: the same refusal would be wrapped, and `main()` would not have run. Invariant: a registered module whose requirement is disabled exits 1 and the display is `Module "%depender%" requires "%required%", which is registered but disabled.` or `which is not registered.`; `extensions` is unchanged; the command does not throw `Undefined module` for a name that is not registered.
- `EnableCommand::execute` / `DisableCommand::execute` — A requirement refusal and a `RemovalBlockedException` are printed and return failure. Any other throwable, including `Unable to find "%name%".` and `Circular requirement "%s > %s" detected.`, propagates. There is no `--force`. Success prints `"%title%" enabled.` or `"%title%" disabled.` with the title, or the package name when `title` is not a non-empty string. Names are resolved before the first `enable()` or the `disable()` call. Rejected letting the pre-flight exception escape, and rejected passing the command-line strings into the manager: those methods take packages. Invariant: `disable` of a blocked package exits 1, the display is the manager's blocked sentence, and `extensions` is unchanged; `getDefinition()->hasOption('force')` is false; an unknown name throws before any package changes.
- `InstallCommand` help — `getDescription()` is `Install places the package and runs the install lifecycle. Activation is php pagekit enable.` `getHelp()` is that plus `It does not enable the package.` Invariant: both contain `install lifecycle` and `php pagekit enable`; `execute()` does not call `enable()`, so a successful install leaves the new module out of `extensions`.
- `impact-query.js::canProceed` / `impact.vue` — Remove and Disable render only when the payload has `blockers`, `orphans`, and `dataRisk` (`migrations` and `config` booleans, `nodes` and `tables` arrays) and `blockers` is empty. A failed read or any other shape leaves the button out. Each of the three results is one sentence, including the empty data-risk line `No migrations, saved settings, node types, or tables were found.`; tables are named and said to be left as they are. Rejected treating a missing `blockers` key as none. Invariant: a non-empty `blockers` list, a transport failure, or a payload missing one of those keys shows no proceed control; `enable` still notifies `response.data.error`.
### Step 10
- `PackageSnapshotter::packageReasons` — A module present in the dump and absent live is not a refusal only when the archived `composer.json` names it (`module` when that is a string, otherwise `basename` of `name`, the same rule as `PackageFactory::load`). Rejected `$snapshot['module']`: rewriting that file to `../../escaped` left the dump's real module unexempted and restore refused before `reinstate()`. Rejected the archive directory name: `packages.*` is keyed by the manifest module. A differing version, including that module's, still refuses, as does every other one-sided name. Invariant: restore after uninstall returns the package and the database when the application version matches and every other `packages.*` entry matches, including when metadata `module` is not the archived one; a module the archive does not name that is only on one side is named in `RestoreRefusedException`, and `packages/` is unchanged.
- `SnapshotStore::application` — Null when metadata has no string `application`. Rejected putting the key on the array `get()` returns: that array is the listing, and a missing version must not be stored as `''` or copied from the running installation. Invariant: a snapshot written without the key yields null; a non-string value yields null; `""` is a recorded version and matches a snapshotter built with `""`.
- `PackageSnapshotter::applicationReasons` — A missing or non-string `application` (`SnapshotStore::application()` is null for both) refuses with `It records no application version.` Rejected returning no reason: the dump would still replace the schema, and the snapshot cannot show it was taken on this installation. `""` stays a recorded version and matches a snapshotter built with `""`. A different string still names both versions. Invariant: restore whose metadata has no string `application`, or whose `application` is not a string, throws `RestoreRefusedException` containing `It records no application version.` and the snapshot id before `reinstate()`, and `packages/` is unchanged; a recorded `""` against `""` is not that refusal; a recorded `"1.2.43"` against `""` throws naming both versions before `reinstate()`, and `packages/` is unchanged.
- `PackageSnapshotter::__construct` / `PackageModule::applicationVersion` — The version defaults to `''`, including when the `version` service is absent or not a string. Rejected a required argument: existing construction passes six arguments, and `''` is the application module's own default. Invariant: one snapshotter not given a version records `""` and restores a snapshot it just took when packages agree; a recorded `"1.2.43"` refuses against `""` before `reinstate()`.
- `PackageSnapshotter::assertRestorable` — A `RuntimeException` from reading the dump is not an operator refusal. Rejected failing there: an incomplete dump is still reported by `restore()` after the files are back. Invariant: a truncated dump leaves the package files restored, the database unchanged, and the message containing `incomplete`; a version or `packages.*` mismatch throws `RestoreRefusedException` and leaves `packages/` unchanged. `DatabaseRestorer::refusals` returns the first MySQL sentence `restore()` would throw, releases the lock, and does not drop leftover copies.
### Step 11
- `user/index.php` `view.scripts` — Skip `hasAccess('user: manage users')` unless `config.file` exists. Rejected removing `system/user` from `debug`'s require: `AuthDataCollector` imports `User`, `Role`, and `UserRepository`, and that edge has to stay. Rejected catching the missing table: SQLite creates `pagekit.db` when the connection opens, before the exception. Invariant: booting the installer with no `config.php` does not create `pagekit.db`; once that file exists, `widget-user` is still registered only when the current user has `user: manage users`.
### Finalize
- `infection.yml` infection-diff — Pass `--ignore-msi-with-no-mutations` on the diff command only. Rejected `ignoreMsiWithNoMutations` in `infection.json.dist`: a nightly full run that generates no mutants must still fail. Invariant: a diff of imports, attributes, or interface signatures exits 0; a diff that produces mutants still requires covered MSI ≥ 80; `nightly.yml` does not pass the flag.
