# Ticket: Step 2.2 — CI/CD Pipeline

## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.2 — CI/CD Pipeline. GitHub Issue #157. Base branch `develop`. Land after 2.1.14 (PHP 8.5 everywhere — confirmed); before 2.3 (Docker) and 2.4 (build tools).
- **Scope:**
  - `.github/workflows/php-quality.yml` → **`php-tests.yml`** (rename + delete old; PR/merge job split; MySQL leg; version-SSoT guard job; artifacts for reporting).
  - **New workflows:** `.github/workflows/infection.yml` (PR diff gate), `e2e.yml` (PR smoke + merge), `frontend.yml` (PR), `nightly.yml` (guard + Infection full + E2E 3 viewports), `e2e-weekly.yml` (guard + full sweep, dispatch-first), `quality-report.yml` (sticky PR comment), `quality-collect.yml` (live snapshot → `quality-data` branch).
  - **New scripts:** `.github/scripts/` — version-SSoT check, quality-report renderer, quality-snapshot builder (zero-dep Node `.mjs`, mirroring `.github/conductor/` convention; or plain PHP for the SSoT check).
  - **New config:** `phpunit-mysql.xml.dist` (MySQL `db_*`/`tmpdb_*` PHPUnit `<var>` globals for `DbUtil`).
  - **E2E selection model:** `playwright.config.js` (env-composed viewport/browser projects), `@ci` tags on the 3 optimized specs, `test.describe.fixme` quarantine on the 8 unoptimized specs, **delete `playwright.smoke.config.js`**, `package.json` script updates, `tests/e2e/README.md` + `tests/e2e/COMPLETE_TEST_PLAN.md` reference updates, `AGENTS.md` Playwright bullet (PW_VIEWPORTS).
  - **Dashboard/live snapshot:** `.github/workflows/pages-deploy.yml` (overlay from `quality-data` + pin its actions by SHA), `docs-site/content/javascripts/quality-dashboard.js`, `docs-site/data/quality-snapshot.demo.json`, `.github/quality/quality-snapshot.json` (8.5 seed), `.github/quality/README.md`, `docs-site/content/quality/index.md`.
  - **Rename consumers:** `.cursor/rules/orchestrator-v2-finalize.mdc` (CI-gate watch), `.cursor/rules/orchestrator-subagent-workflow.mdc`, `.cursor/agents/architect.md` (Finalize line), `codecov.yml` (comment path).
  - **Agent/rule slimming:** `migration-docs/branches/branch-doc-skeleton.md`, `.cursor/rules/orchestrator-v2-step.mdc`, `.cursor/rules/orchestrator-v2-finalize.mdc`, `.cursor/rules/orchestrator-subagent-workflow.mdc`, `.cursor/agents/{tester,test-writer,doc-writer}.md`.
  - **No changes:** coverage floor value (`MIN_LINE_COVERAGE: '3.8'` + provenance comment stay), `phpstan-baseline.neon`, `infection.json.dist` scope/thresholds (80/80 kept), `docs-site/hooks/copy_snapshot.py` (path unchanged), the 11 spec bodies beyond tags/fixme markers.
- **Deferred:**
  - **Step 2.9 (Phase 2 Closeout)** — coverage floor raise toward 75 %/60 % (ratchet stays at 3.8); MSI ratchet / widening Infection past auth + user; **full DB portability of the PHPUnit suite** so the `phpunit-mysql` leg can become a required gate (PHASE §2.9 amended); edge-case E2E scenarios.
  - **Step 3.6.1 (E2E Test Suite Rework)** — repairing/rewriting the 8 quarantined specs; tablet/mobile viewport compatibility of the `@ci` specs; growing the `@ci` set (PHASE §3.6.1 amended). `data-testid` selectors → Step 3.6.
  - **Step 2.3 (Docker)** — production images; **Step 2.4 (Build tools)** — pnpm + Vite, ESLint 9 flat config, final formatting policy (Prettier stays advisory until then).
  - **Non-goals:** release automation hooks (none needed yet), any 8.2/8.3/8.4 CI leg, Codecov made blocking (stays non-blocking).
- **Bridges:** None (no parallel old/new behavior). Forward-debt markers only (Rule 5):
  - `phpunit-mysql` job `continue-on-error: true` + `# TODO: Must be refactored in Step 2.9 (Phase 2 Closeout)` — flip to required once the suite is DB-portable.
  - Quarantined specs: `test.describe.fixme` + `// TODO: Must be refactored in Step 3.6.1 (E2E Test Suite Rework)`.
  - Nightly tablet/mobile legs `continue-on-error` + `# TODO: Must be refactored in Step 3.6.1 (E2E Test Suite Rework)`.
- **⚠️ Discovery notes (Refactorer MUST heed):**
  1. **Required status checks are job-name-bound, not workflow-name-bound.** Ruleset "Protect for Develop-Branch" (id 8144609) requires exactly: `phpunit (8.5)`, `phpstan`, `cs-fixer`, `security-audit`. Renaming the workflow file/`name:` does NOT orphan them **as long as job `name:` values stay byte-identical**. Keep `name: phpunit (${{ matrix.php }})` with matrix `['8.5']` for the SQLite leg; the MySQL leg must be a **separate job** (`phpunit-mysql`) so the required context `phpunit (8.5)` never changes. New required contexts (`infection-diff`, `e2e-smoke`, `frontend`) need an admin — Manual Work.
  2. **The PHPUnit suite is NOT DB-portable.** Only `DbTestCase`/`DbUtil` consumers (`ConfigManagerTest`, `EntityManagerTest`) honor `$GLOBALS['db_*']`; all other DB tests hardcode `pdo_sqlite` in-memory. Decision: SQLite leg stays the required gate; MySQL leg = separate **non-blocking** job (`continue-on-error: true`) running `phpunit -c phpunit-mysql.xml.dist` against a health-checked `mysql:8.4` service — it genuinely exercises the DbUtil paths on MySQL (no fake green; the honest limitation is recorded via the Rule-5 marker + Manual Work). `DbUtil` needs all 12 `db_*`/`tmpdb_*` globals; point `tmpdb_name` at the `mysql` system DB (root) so drop/create of the test DB works.
  3. **Workflow-name consumers break on rename:** `.cursor/rules/orchestrator-v2-finalize.mdc` waits on CI via `gh run list --workflow "PHP Quality"` — must be updated in the same step as the rename (this very ticket's Finalize depends on it). Prefer `gh pr checks <pr> --watch` (covers all new PR gates, not just one workflow). Also update the V1 rule prose and `.cursor/agents/architect.md` Finalize line, and the `php-quality.yml` path in `codecov.yml`'s comment.
  4. **No `@ci` tags exist yet; no Prettier is installed.** `@playwright/test` is `^1.55.1` → tag syntax `test.describe('title', { tag: '@ci' }, …)` is supported. Prettier: add a pinned `prettier` devDependency (exact version) — advisory use only.
  5. **`playwright.smoke.config.js` is redundant** under the tag model (its `testMatch` globs `**/setup/…` don't even match the real `01-setup/…` dirs). Delete it; repoint `test:smoke*`/`test:e2e:install` scripts to `--grep @ci` / direct spec paths on the default config.
  6. **Workflows triggered by `workflow_run`/`schedule` only run from the default branch** — `quality-report.yml`, `quality-collect.yml`, `nightly.yml`, `e2e-weekly.yml` cannot be observed on this ticket's PR. Give all four a `workflow_dispatch` trigger for post-merge validation; record the validation dispatches under Manual Work if the agent token cannot dispatch.
  7. **Skipped required checks block merges.** Do NOT add broad `paths`/`paths-ignore` filters to workflows carrying required jobs. Keep only the dormant metrics ignores (`.github/conductor/metrics/**`, plus `.github/quality/**` as defense-in-depth on push).
  8. **ESLint has ~13k pre-existing errors** — the frontend gate must be: production build (webpack + gulp) **blocking**; ESLint + Prettier **diff-scoped and advisory** (`continue-on-error` steps or non-failing exit). Node 22 + Yarn 1 build the tree fine (verified in this VM). Never attempt a full-tree lint fix.
  9. **Firefox does not support Playwright's `isMobile`** — define tablet/mobile projects as explicit `viewport` overrides only (no `isMobile`/`hasTouch`), uniform across browsers: desktop = `devices['Desktop *']` presets, tablet ≈ 820×1180, mobile ≈ 390×844.
  10. **E2E in one invocation works sequentially:** with `CI=true` the config resolves 1 worker and alphabetical file order runs `01-setup/installation` (needs NO pre-existing install — do not run `php pagekit setup` first) before `02-core/*` (which use that install). CI setup per E2E job: composer install, `mkdir -p tmp/... storage`, copy `tests/e2e/config/test-config.json` from `.example.json`, `npx playwright install --with-deps chromium` (weekly: no browser filter + `PW_BROWSERS=all`).
  11. **Pin every third-party action by commit SHA.** Reusable pins already in-tree: `actions/checkout@93cb6efe…` (v5), `shivammathur/setup-php@f3e473d1…` (v2), `actions/cache@caa29612…` (v5), `actions/upload-artifact@b7c566a7…` (v6), `codecov/codecov-action@0fb71748…` (v5.5.5), `actions/github-script@ed597411…` (v8). New pins needed for `actions/setup-node` and `actions/download-artifact` — resolve current release SHAs via `gh api`. `pages-deploy.yml` still uses tag refs — pin while touching it.
  12. **Baseline green (verified):** PHPUnit 725 tests OK (2 known PHPUnit-internal deprecations — owned by 2.9), PHPStan L8 clean, PHP 8.5.8, PCOV available. Infection 0.34 is installed (`--git-diff-lines` / `--git-diff-base` supported).
  13. **Sticky comment / snapshot are script-rendered** (no LLM formatting): jobs upload raw artifacts (Clover `coverage.xml`, PHPUnit `--log-junit junit.xml`, PHPStan `--error-format=json`, Infection `tmp/infection/infection.json` + `summary.log`, Playwright `--reporter=json`); `.github/scripts/*.mjs` normalize them. Gate conclusions (cs-fixer, security-audit, frontend) come from the check-runs API. Sticky comment upsert via `gh api` with hidden marker `<!-- quality-report -->` (idempotent).
  14. **Data branch:** write the live snapshot to a dedicated unprotected **`quality-data`** branch (single writer: `quality-collect`; no race with `conductor-metrics`). Pushes there cannot re-trigger `php-tests` (its push trigger is branch-filtered to main/develop) — the `paths-ignore` addition is defense-in-depth. After pushing, dispatch `pages-deploy.yml` (`gh workflow run`, needs `actions: write`) — GITHUB_TOKEN pushes never re-trigger `push` workflows by themselves.
  15. **Snapshot blend:** coverage/PHPStan/E2E from the latest green **merge** runs on develop; Infection **full** MSI from the latest successful **nightly** (null until the first nightly ran). Label E2E scope (`"scope": "smoke"` on merge; `"full"` weekly) and keys per leg (`"8.5-sqlite"` required, `"8.5-mysql"` `"required": false`) — additive schema v2 changes, document in `.github/quality/README.md`.
- **Checklist:**
  1. **Migrate `php-quality.yml` → `php-tests.yml` + update rename consumers.** New file `name: PHP Tests`, delete the old file (No Mercy). Keep the 4 jobs with byte-identical job names (`phpunit (${{ matrix.php }})` matrix `['8.5']`, `phpstan`, `cs-fixer`, `security-audit`), coverage ratchet (`MIN_LINE_COVERAGE: '3.8'` + provenance comment), Codecov (non-blocking, OIDC), concurrency, pins. PR/merge split: `cs-fixer` + `security-audit` get `if: github.event_name == 'pull_request'`; `phpunit` + `phpstan` run on both (merge run = fresh develop-tip numbers for `quality-collect`). Add artifacts for reporting: `junit.xml` (`--log-junit`), keep `coverage-clover`; `phpstan` job additionally emits/uploads `--error-format=json` output. Add `.phpunit.cache` to the cache step. Extend push `paths-ignore` with `.github/quality/**`. Update rename consumers now: `orchestrator-v2-finalize.mdc` CI gate → `gh pr checks <pr> --watch --fail-fast` (drop the `--workflow "PHP Quality"` lookup), `orchestrator-subagent-workflow.mdc` "PHP Quality" prose → "PHP Tests" / PR checks, `architect.md` Finalize line, `codecov.yml` comment path.
  2. **MySQL leg (non-blocking).** New `phpunit-mysql.xml.dist` (duplicate of `phpunit.xml.dist` + `<php><var>` block with the 12 `db_*`/`tmpdb_*` globals: `pdo_mysql`, host `127.0.0.1`, port `3306`, db `pagekit_test`, tmpdb `mysql`, root credentials). New job `phpunit-mysql` in `php-tests.yml` (PR + push): `mysql:8.4` service with healthcheck (`mysqladmin ping`), `continue-on-error: true` + Rule-5 marker `# TODO: Must be refactored in Step 2.9 (Phase 2 Closeout) — flip to required once the suite is DB-portable`, run `./app/vendor/bin/phpunit -c phpunit-mysql.xml.dist` (no coverage). Do NOT touch the required `phpunit (8.5)` context.
  3. **Version SSoT guard.** New script `.github/scripts/check-version-ssot.php` (plain PHP, no deps) asserting one PHP minor everywhere: `composer.json` `require.php` + `config.platform.php`, `php-tests.yml` matrix, `app/installer/requirements.php` `REQUIRED_PHP_VERSION`, `.cursor/Dockerfile` base image, root `Dockerfile` base image, `README.md` badge. Non-zero exit + named offender on drift. New lightweight job `version-ssot` in `php-tests.yml` (PR only, no composer install needed).
  4. **`quality-report.yml` — sticky PR comment scaffold.** Trigger: `workflow_run` (`types: [completed]`) on `PHP Tests`, `Infection`, `E2E`, `Frontend` + `workflow_dispatch`; proceed only when `workflow_run.event == 'pull_request'`; resolve the PR from the head SHA. Script `.github/scripts/quality-report.mjs` (zero-dep): download the run's artifacts, query check-runs for gate conclusions of the same head SHA, render a fixed markdown table (coverage %, floor, PHPStan, Infection-diff MSI, E2E smoke, CS-Fixer, security, frontend — "pending" for workflows that have not reported), upsert one comment via `gh api` keyed on hidden marker `<!-- quality-report -->`. Permissions: `pull-requests: write`, `actions: read`, `checks: read`. Missing workflows must render gracefully (Infection/E2E/Frontend land in later checklist steps).
  5. **`quality-collect.yml` + `pages-deploy.yml` overlay.** Trigger: `workflow_run` (`types: [completed]`, branches `develop`/`main`) on `PHP Tests` and `E2E` + `workflow_dispatch`; proceed only on `conclusion == 'success'` and `workflow_run.event == 'push'`. Script `.github/scripts/quality-snapshot.mjs`: query the latest completed merge runs of `PHP Tests` + `E2E` on the branch, download their artifacts (clover, junit, phpstan json, playwright json), pull Infection full MSI from the latest successful `Nightly` run artifact (null-safe), assemble schema-v2 snapshot (`"source": "github-actions"`, E2E `"scope": "smoke"`, phpunit keys `"8.5-sqlite"`/`"8.5-mysql"` with `"required"` flags); only write when both gate workflows are green. Push `.github/quality/quality-snapshot.json` to unprotected **`quality-data`** branch (create orphan-style if absent, mirroring the conductor-metrics push), then `gh workflow run pages-deploy.yml --ref develop`. Permissions: `contents: write`, `actions: write` (+ read). Never push to `develop`; never weaken the Ruleset. In `pages-deploy.yml`: add an overlay step fetching `origin/quality-data` → checkout `.github/quality/quality-snapshot.json` when the branch exists (mirror the conductor-metrics block) and pin all its actions by commit SHA.
  6. **Dashboard on the live snapshot (8.5-only).** `quality-dashboard.js`: render PHPUnit rows dynamically from the snapshot's `phpunit` keys (delete the hardcoded 8.2/8.3 rows), label coverage without a PHP-version suffix, show the E2E `scope` label, mark non-required legs (⚪). `quality-snapshot.demo.json` + seed `.github/quality/quality-snapshot.json`: keys → `"8.5-sqlite"` (+ `"8.5-mysql"`, `"required": false`), e2e gains `"scope"`. `docs-site/content/quality/index.md`: remove the "Demo data" admonition; update the data-source table (collector exists now). `.github/quality/README.md`: schema example → 8.5 keys + `scope`/`required` fields; refresh the Status section (live collection wired; snapshot served from the `quality-data` branch).
  7. **`infection.yml` — PR diff gate.** `name: Infection`, trigger `pull_request` → develop/main, concurrency, pins. Required job `infection-diff`: checkout `fetch-depth: 0`, PHP 8.5 + PCOV, composer cache/install, fetch base ref; early-exit green step when `git diff --name-only origin/$GITHUB_BASE_REF...HEAD` has no files under the Infection source scope (`app/modules/auth/src`, `app/system/modules/user/src/{Model,Auth,Event}`); otherwise `./app/vendor/bin/infection --threads=max --git-diff-lines --git-diff-base=origin/$GITHUB_BASE_REF --logger-github --min-covered-msi=80`; upload `tmp/infection/` logs as artifact. Keep well under 10 min (scope is small).
  8. **`frontend.yml` — PR gate.** `name: Frontend`, trigger `pull_request` → develop/main, concurrency, pins. Single required job `frontend`: pinned `actions/setup-node` (Node 22) + yarn cache; add pinned exact `prettier` devDependency (advisory tooling only); `yarn install --frozen-lockfile`; **blocking**: production build `yarn compile-js --mode=production` + `yarn gulp`; **advisory** (`continue-on-error: true` steps): ESLint + Prettier `--check` on changed `.js`/`.vue` files only (`git diff --name-only origin/$GITHUB_BASE_REF...HEAD`, skip when none). Never lint the full tree (13k pre-existing errors).
  9. **Playwright selection model (tags + projects, zero duplicate specs).** `playwright.config.js`: compose projects from env — default `[chromium-desktop]` (Desktop Chrome preset); `PW_VIEWPORTS=all` adds `chromium-tablet` + `chromium-mobile` (explicit `viewport` overrides only, no `isMobile` — Firefox constraint); `PW_BROWSERS=all` adds `firefox-desktop` + `webkit-desktop` (+ their tablet/mobile variants when both flags set). Tag the 3 optimized specs `@ci` (`test.describe('…', { tag: '@ci' }, …)`): `01-setup/installation`, `02-core/authentication`, `02-core/dashboard`. Quarantine the 8 others via `test.describe.fixme` + `// TODO: Must be refactored in Step 3.6.1 (E2E Test Suite Rework)` (shows as skipped, never red; do NOT repair spec bodies). Delete `playwright.smoke.config.js`; update `package.json` scripts (`test:smoke*` → `--grep @ci`; `test:e2e:install` → default config); update `tests/e2e/README.md`, `tests/e2e/COMPLETE_TEST_PLAN.md` references and the `AGENTS.md` Playwright bullet (add `PW_VIEWPORTS`). Local default behavior must stay: bare `npx playwright test <spec>` runs chromium-desktop only.
  10. **`e2e.yml` — smoke (PR, required) + merge.** `name: E2E`, concurrency, pins. Job `e2e-smoke` (`pull_request` → develop/main): PHP 8.5, composer cache/install, writable dirs, copy `test-config.json` from `.example.json`, pinned setup-node + yarn cache + `yarn install --frozen-lockfile`, Playwright browser cache (`~/.cache/ms-playwright` keyed on `@playwright/test` version), `npx playwright install --with-deps chromium`, run `npx playwright test --grep @ci --project=chromium-desktop` (installation spec installs via web UI — no `php pagekit setup` beforehand), upload Playwright JSON report + HTML on failure. Job `e2e-merge` (`push` → develop/main): identical selection against the merged tip; uploads the JSON report artifact that feeds `quality-collect`. Target < 10 min.
  11. **`nightly.yml` — guarded Infection full + E2E viewports.** `name: Nightly`, `schedule` ~02:00 UTC + `workflow_dispatch`, concurrency. Job `guard` (per task-prompt snippet): `workflow_dispatch` always runs; else `git log --since='24 hours ago'` non-empty. Job `infection-full` (`needs: guard`, `if: should_run`): full `./app/vendor/bin/infection --threads=max` on the configured scope (80/80 thresholds from `infection.json.dist`), upload `tmp/infection/infection.json` + `summary.log` (consumed by the snapshot blend). Job `e2e-viewports` (`needs: guard`): matrix over `project: [chromium-desktop, chromium-tablet, chromium-mobile]` with `PW_VIEWPORTS=all`, `--grep @ci`, `continue-on-error: ${{ matrix.project != 'chromium-desktop' }}` + Rule-5 marker `# TODO: Must be refactored in Step 3.6.1 (E2E Test Suite Rework) — tablet/mobile legs non-blocking until @ci specs are viewport-robust`. Same E2E env setup as Step 10.
  12. **`e2e-weekly.yml` — full sweep (dispatch-first).** `name: E2E Weekly`, triggers: `workflow_dispatch` + `schedule` (weekly) with job-level `if: github.event_name == 'workflow_dispatch' || vars.E2E_WEEKLY_ENABLED == 'true'`; `guard` with a 7-day window (dispatch bypasses). Sweep job: matrix over all 9 projects (`{chromium,firefox,webkit}-{desktop,tablet,mobile}`), `PW_BROWSERS=all PW_VIEWPORTS=all`, **no grep** (fixme'd specs report as skipped, not failed), `npx playwright install --with-deps`, `continue-on-error: ${{ matrix.project != 'chromium-desktop' }}`, upload reports. No active cron gate flip in this ticket (Manual Work).
  13. **Agent/rule slimming (metrics discipline).** `branch-doc-skeleton.md`: comment-guards that Verification stays links-only (CI sticky comment + dashboard) and the 📊 appendix must never become a metrics table. `orchestrator-v2-step.mdc` doc-writer template: replace `Verifier/Tester output (verbatim, incl. FAIL iterations): <paste>` with gate outcomes (PASS/FAIL per gate + one-line deviations; no metric numbers). `orchestrator-subagent-workflow.mdc` Doc-writer (Execute) row: same slimming. `orchestrator-v2-finalize.mdc` doc-writer template: CI outcome stays link + status; add "metrics: link sticky comment/dashboard only". `.cursor/agents/tester.md`: output = PASS/FAIL + minimal RCA only — never metric numbers for documentation (CI is the metrics SSoT). `.cursor/agents/test-writer.md`: reaffirm no-metrics output (files-only). `.cursor/agents/doc-writer.md`: quality numbers are CI-owned — branch docs link the PR sticky comment + quality dashboard, never metric tables.

## EXECUTION STATE
<!-- Machine-readable progress index for the Orchestrator/Conductor. Mirrors the Checklist 1:1
     (same numbers + short titles). Size hint per step: S = small/atomic, M = medium,
     L = large or loop-risk. A step orchestrator flips its box to [x] in the SAME commit as that
     step's code + tests (after full step PASS incl. test-writer when applicable) -->
- [x] Step 1 (M) — php-quality.yml → php-tests.yml + PR/merge split + rename consumers
- [x] Step 2 (M) — MySQL leg: phpunit-mysql.xml.dist + non-blocking service job
- [x] Step 3 (S) — Version SSoT guard script + job
- [x] Step 4 (M) — quality-report.yml sticky PR comment scaffold
- [x] Step 5 (L) — quality-collect.yml + quality-data branch + pages-deploy overlay
- [x] Step 6 (M) — Dashboard live alignment: 8.5 keys, scope labels, demo banner removal
- [x] Step 7 (M) — infection.yml PR diff gate (infection-diff)
- [x] Step 8 (M) — frontend.yml: blocking build + advisory diff-scoped lint
- [x] Step 9 (M) — Playwright tags + viewport/browser projects + smoke-config deletion
- [ ] Step 10 (L) — e2e.yml: e2e-smoke (PR) + e2e-merge (push)
- [ ] Step 11 (M) — nightly.yml: guard + infection-full + e2e-viewports
- [ ] Step 12 (M) — e2e-weekly.yml: gated full sweep (dispatch-first)
- [ ] Step 13 (S) — Agent/rule slimming: no metric tables, PASS/FAIL-only handoffs

## TESTING STRATEGY
- **Per step (production gate):** Refactorer → Verifier → Tester (PHPUnit + PHPStan) — production code must be green before any new tests are written. Baseline verified green (725 tests OK; PHPStan L8 clean; PHP 8.5.8).
- **Per step (coverage — inline light):** test-writer → Verifier (test files only) → Tester (PHPUnit + PHPStan) — **skip on ALL steps** (`test-writer: skip` — no step changes production PHP under `app/` or `packages/`; scope is CI YAML, `.github/scripts/`, PHPUnit/Playwright config, docs, and rules).
- **Per step notes (extra Tester gates beyond PHPUnit + PHPStan):**
  - Every step touching `.github/workflows/*.yml`: YAML syntax check per changed file — `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' <file>` (actionlint is not installed in the VM).
  - Steps 3–5: `node --check .github/scripts/*.mjs`; Step 3 additionally: `php .github/scripts/check-version-ssot.php` must exit 0 on the current tree (all sources already agree on 8.5).
  - Step 2: `./app/vendor/bin/phpunit -c phpunit-mysql.xml.dist` is **not** runnable locally (no MySQL in the VM) — validate the XML shape (`python3`/`xmllint` well-formedness) only; the real signal is the non-blocking CI leg on the PR.
  - Step 9: `npx playwright test --list --grep @ci` lists tests from exactly the 3 tagged specs (chromium-desktop only); bare `npx playwright test --list` shows the 8 quarantined specs as fixme; `PW_VIEWPORTS=all PW_BROWSERS=all npx playwright test --list` shows 9 projects. (Copy `test-config.json` from `.example.json` first.)
  - Step 6: no JS test runner exists for docs-site — Verifier review + `node --check docs-site/content/javascripts/quality-dashboard.js`.
- **E2E (Execute — last checklist step only):** when ticking Step 13 completes every `## EXECUTION STATE` box, the Orchestrator delegates `"final E2E run"` to Tester (3 Playwright specs, local PASS/FAIL — the specs now carry `@ci` tags; direct spec paths still work). **Not** gated on PR/CI — see `.cursor/agents/tester.md` § End-of-ticket E2E. Includes the manual gate: `php pagekit setup … -d sqlite --no-interaction` + admin login sanity.
- **Finalize:** Orchestrator opens PR → CI gate = **`gh pr checks <pr> --watch`** (per the Step-1 rule update — the watch must cover the NEW required workflows: PHP Tests jobs, `infection-diff`, `e2e-smoke`, `frontend`; `quality-report` posts the sticky comment only after this ticket merges to develop, see Discovery note 6) → Bugbot → version/CHANGELOG/ROADMAP. Attempt one `workflow_dispatch` validation of `nightly.yml` + `e2e-weekly.yml` post-merge if token permits; otherwise record under Manual Work.
- **Manual Work list (branch doc — user actions, agents do NOT perform):**
  1. Ruleset "Protect for Develop-Branch": add required contexts `infection-diff`, `e2e-smoke`, `frontend` (existing `phpunit (8.5)`, `phpstan`, `cs-fixer`, `security-audit` stay valid — verify after the first PR run).
  2. Enable weekly E2E: set repo variable `E2E_WEEKLY_ENABLED=true` once specs are ready.
  3. Tablet/mobile viewport compatibility of the `@ci` specs — record every failing spec×viewport combo from the first nightly dispatch; user optimizes, then removes the non-blocking flags.
  4. Un-quarantine the 8 `fixme` specs (optimize → tag `@ci`) — structural home: Step 3.6.1.
  5. MySQL leg stays non-blocking until Step 2.9 makes the suite DB-portable; watch its signal on PRs.
  6. Trigger `nightly.yml` + `e2e-weekly.yml` once via `workflow_dispatch` to prove they run (if not already done at Finalize).
