## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.1.8 — Infection Mutation Testing (GitHub Issue #155, sub-issue of #147 / Step 2.1). Branch `feature/infection-mutation-testing` (base `develop`). No Phase 1 audit cell closes here.
- **Scope:**
  - **Tooling (new):** `composer require --dev infection/infection` (pin `^0.29` or newer if the resolver picks an older release — PHPUnit 11 support requires Infection ≥ 0.29). Commit `composer.json` + `composer.lock`. Binary lands at `app/vendor/bin/infection` (non-standard vendor dir).
  - **Config (new):** `infection.json.dist` at repo root, scoped to the security core (§2.1 of the prompt): `app/modules/auth/src`, `app/system/modules/user/src/Model`, `app/system/modules/user/src/Auth`, `app/system/modules/user/src/Event`; `minMsi: 80`, `minCoveredMsi: 80`; logs + `tmpDir` under `tmp/infection`; `phpUnit.configDir: "."`, `customPath: "app/vendor/bin/phpunit"`.
  - **Tests written here (the real deliverable — mutation testing needs a test base):**
    - `app/modules/auth/src/Tests/NativePasswordEncoderTest.php` (NEW — 0 tests today).
    - `app/modules/auth/src/Tests/DatabaseHandlerTest.php` (EXTEND — add `read()` + `destroy()`; only `write()` covered today).
    - `app/system/modules/user/src/Tests/UserProviderTest.php` (NEW).
    - `app/system/modules/user/src/Tests/RoleTest.php` (NEW).
    - `app/system/modules/user/src/Tests/UserTest.php` (NEW — status flags / `getStatusText` / cached `hasPermission`; `UserAccessTest` already covers `hasAccess`).
    - `app/system/modules/user/src/Tests/LoginAttemptListenerTest.php` (NEW — high value, see discovery note 4).
    - `app/system/modules/user/src/Tests/AuthorizationListenerTest.php` (NEW).
    - `app/system/modules/user/src/Tests/AccessListenerTest.php` (NEW).
  - **`.gitignore`:** already contains `/infection.json` + `/tmp/infection/` (lines 63–65) — VERIFY only, no edit (see discovery note 2). Commit **only** `infection.json.dist`; never the local `infection.json`.
  - **Production code:** changed ONLY if a mutant exposes a genuine bug (a finding + a win) — never to silence a mutant.
- **Deferred:**
  - **Step 2.2 (CI/CD Pipeline):** Infection CI wiring (non-blocking, `workflow_dispatch` + weekly `schedule`, `--min-msi=80`). Do NOT touch `.github/workflows/php-quality.yml` here. The tracking note already exists in `PHASE_2_MODERNISING.md` (§2.2 "Workflow 1b", lines ~311 + ~326) — VERIFY present; add only if missing.
  - **Step 2.1.9 (Test Coverage Expansion):** the 7 user Controllers (request/response + E2E territory) and breadth across other modules. **Also** the DB/kernel-bound methods that cannot be unit-tested without a kernel/DB and would otherwise cap directory MSI (see discovery note 6): `UserProvider::find()/findByUsername()/findByCredentials()` happy paths (static `User::` lookups); `User::hasPermission()` uncached `findRoles()` path; the static DB trait methods in `UserModelTrait`/`RoleModelTrait`/`AccessModelTrait` (`findByEmail/findByLogin/updateLogin/findRoles/saving/removeRole`); `UserListener` (both handlers are static `User::` delegations — only `subscribe()` is unit-testable). Flag these via test-file notes / `infection.json.dist` `ignore` with a one-line 2.1.9 justification — do NOT lower `minMsi`.
- **Bridges:** None. This is dev-tooling + tests only; no parallel old/new behaviour, no shims, no adapters. (Nuance per §4.2: an escaped mutant revealing a real bug → fix it as a legitimate production change, or flag via the `github-issue-creator` skill if out of scope; a true equivalent mutant → document via `infection.json.dist` `ignore` / `@` annotation with a one-line justification. Neither is a compatibility bridge.)
- **⚠️ Discovery notes (prompt/PHASE_2 are partially stale or underspecified — Refactorer MUST heed these):**
  1. **No coverage driver in the agent VM.** `php -m | grep -iE 'xdebug|pcov'` returns nothing (PHP 8.3.6). Infection CANNOT run without Xdebug or PCOV (prompt §0.2 calls this the #1 first-run blocker, but assumes Xdebug is merely disabled — here neither extension is installed). A driver MUST be enabled before Step 8: install PCOV (`pecl install pcov` then enable, preferred — faster for coverage) **or** Xdebug (then run with `XDEBUG_MODE=coverage`). This is VM environment setup, not committed code. Treat as a hard precondition of Step 8; the per-step PHPUnit runs in Steps 2–7 do not need it.
  2. **`.gitignore` already ignores Infection artifacts** (`/infection.json`, `/tmp/infection/`). The validation-checklist item "add `infection.json` to `.gitignore`" is already satisfied — verify, don't duplicate.
  3. **The `source.directories` scope pulls in more than the §3.2 table names:**
     - `app/modules/auth/src` (recursive) also contains `Event/*` DTOs, `Exception/*`, interfaces (`UserInterface`, `UserProviderInterface`, `HandlerInterface`, `PasswordEncoderInterface`), and `AuthEvents` constants — Infection generates no meaningful mutants for interfaces/const/DTO getters, so the realistic auth targets remain `Auth`, `Handler/DatabaseHandler`, `Encoder/NativePasswordEncoder`.
     - `app/system/modules/user/src/Event` contains **FOUR** listeners, not two: `AccessListener`, `AuthorizationListener`, **`UserListener`**, **`LoginAttemptListener`**.
     - `app/system/modules/user/src/Model` contains the DB-bound **traits** (`UserModelTrait`, `RoleModelTrait`, `AccessModelTrait`) alongside `User`/`Role`. (`DataModelTrait` lives in `app/system/src/Model` → OUT of scope. Good.)
  4. **`LoginAttemptListener` is a top-tier target the prompt's §3.2 table omits.** It is the brute-force / rate-limit throttle, backed by an injected PSR-6 `CacheItemPoolInterface` (trivially mockable) with rich boundary logic (`count($attempts) >= ATTEMPTS`, `(time() - $lastAttempt) < DELAY`, `end()` last-timestamp read). Mutation value is on par with `NativePasswordEncoder` → it gets its own test (Step 6).
  5. **`__()` is `Pagekit\__()` (namespaced) and is NOT autoloaded in the PHPUnit bootstrap.** composer.json has no `autoload.files`; only the mail module ships its own `app/system/modules/mail/src/Tests/bootstrap.php` stub. Classes under test that call `__()` — `User::getStatusText()`/`getStatuses()`, `AuthorizationListener::onAuthorize()`, `LoginAttemptListener::onPreAuthenticate()` — will resolve to `Pagekit\__()`. Any test exercising those paths MUST make a `Pagekit\__()` stub available; follow the mail pattern (guarded `if (!function_exists('Pagekit\__')) { … }` returning `strtr($message, $args)`), either inline in the test file or a shared user-module `Tests/bootstrap.php`.
  6. **`UserProvider` lookups + several trait/listener methods are static-model/DB-bound** (`User::find()`, `User::where()`, `Role::where()`, etc.) → not unit-testable without a kernel/DB. Test the pure logic (esp. `validateCredentials()`); use **Covered MSI** to gauge test quality while iterating and drive **overall MSI** up by adding tests. Where a class genuinely needs DB/kernel, flag it for 2.1.9 (test-file note + `infection.json.dist` `ignore`) rather than dropping the 80 gate (§2.3).
- **Checklist:**
  1. **Install Infection + create `infection.json.dist` + verify ignore/driver prerequisites.** Run `composer require --dev infection/infection`; if the resolver selects < 0.29, re-require with an explicit `^0.29` (or newer) constraint for PHPUnit 11 compatibility. Create `infection.json.dist` exactly per prompt §2.1 (scoped dirs above, `excludes: ["Tests"]`, `minMsi: 80`, `minCoveredMsi: 80`, logs + `tmpDir` = `tmp/infection`, `phpUnit.configDir: "."`, `customPath: "app/vendor/bin/phpunit"`, `mutators: {"@default": true}`). Confirm `.gitignore` already ignores `/infection.json` + `/tmp/infection/` (present) and `mkdir -p tmp/infection`. **Do NOT run Infection yet** (no test base + no coverage driver). **Environment precondition for Step 8 (record in the branch doc at Finalize):** a coverage driver (PCOV or Xdebug) must be enabled in the VM — none is currently installed (discovery note 1). Also confirm the full suite is green first (`./app/vendor/bin/phpunit`) and record the starting scoped coverage for the branch doc (`--coverage-text --coverage-filter app/modules/auth/src --coverage-filter app/system/modules/user/src`, once a driver is present). Commit: `composer.json`, `composer.lock`, `infection.json.dist`.
  2. **Test `NativePasswordEncoder` (auth gate — top priority).** NEW `NativePasswordEncoderTest`: `hash()` produces a value that `password_verify()` accepts and `password_get_info()` reports as bcrypt with cost 10 (kills algorithm/cost mutants); `verify()` returns true for a matching hash and false for a wrong password; the **salt-rejection branch** — `verify($hash, $raw, $salt)` with non-null `$salt` throws `\InvalidArgumentException` (kills the `null !== $salt` condition mutant). Pure unit test, no mocks.
  3. **Extend `DatabaseHandlerTest` — cover `read()` + `destroy()`.** Reuse the existing `self::CONFIG` + mocking style. Mock `Connection` (with a `Doctrine\DBAL\Result` mock so `executeQuery(...)->fetchAssociative()` is controllable), a `RequestStack` whose `Request` carries the auth cookie (`pk_auth`), and `CookieJar`. `read()` branches: null-config → `null`; no token → `null`; no DB row → `null`; row present + within timeout → calls `update(access)` and returns `user_id`; **timeout expired + `STATUS_REMEMBERED`** → re-`write()`; **timeout expired + not remembered** → `null` (these two boundary branches are prime mutant targets). `destroy()` branches: null-config → early return; token present → `update(table, ['status' => STATUS_INACTIVE], ['id' => sha1(token)])`.
  4. **Test `UserProvider::validateCredentials()` + `findByCredentials()` guard.** NEW `UserProviderTest`: construct with a mocked `PasswordEncoderInterface`; assert `validateCredentials($user, $creds)` returns the encoder's verdict for both true/false AND that `verify()` is called with exactly `($user->getPassword(), $credentials['password'])` (kills argument-order/swap mutants — this gates authentication). Exercise the `findByCredentials()` `password`-stripping and the non-`User` `\LogicException` type-guard as far as feasible without a DB. Add a test-file note flagging `find()`/`findByUsername()`/`findByCredentials()` happy-path (static `User::` DB lookups) for Step 2.1.9 integration coverage (discovery note 6).
  5. **Test `Role` (all pure methods) + `User` status flags.** NEW `RoleTest`: `hasPermission`/`addPermission`/`clearPermissions`; `isLocked`/`isAnonymous`/`isAuthenticated`/`isAdministrator` across `ROLE_ANONYMOUS`/`ROLE_AUTHENTICATED`/`ROLE_ADMINISTRATOR` and a non-system id (kills the `==`/`in_array` mutants); `__toString`. NEW `UserTest`: `isAnonymous`/`isAuthenticated`/`isAdministrator` by seeding `$user->roles` (via `AccessModelTrait::hasRole`); `isActive`/`isBlocked` for both status values; `getStatusText()` for active, blocked, and unknown status (needs the `Pagekit\__()` stub — discovery note 5); `hasPermission()` **cached branch** by pre-seeding the protected `$permissions` via Reflection (covers the `in_array` result without DB). Flag the uncached `findRoles()` DB path for 2.1.9.
  6. **Test `LoginAttemptListener` (brute-force throttle — high value, discovery note 4).** NEW `LoginAttemptListenerTest`: mock `CacheItemPoolInterface` + `CacheItemInterface` (control `isHit()`/`get()`), stub `Pagekit\__()`. Cover `onPreAuthenticate`: missing/empty credentials → early return (no cache hit); attempts under `ATTEMPTS` → passes; **`count >= ATTEMPTS` AND `(time - last) < DELAY`** → throws `AuthException`; **`count >= ATTEMPTS` but last attempt older than `DELAY`** → passes (boundary — kills the `>=`/`<` mutants). `onAuthFailure`: appends `time()` to the array and calls `save($item)`. `onAuthSuccess`: calls `deleteItem($key)`. Optionally assert `getCacheKey()` sanitization is applied.
  7. **Test `AuthorizationListener` + `AccessListener`.** NEW `AuthorizationListenerTest`: mock `Auth`, `PasswordEncoderInterface`, `SessionInterface`. Cover `onSystemInit` (sets a `UserProvider` on `Auth`); `onRequest` (blocked `User` → `Auth::logout()` called; active/non-User → not); `onAuthorize` (blocked user throws `AuthException` — both `login`-set and not-activated message branches; non-blocked → no throw; needs `Pagekit\__()`); `onLogin` (`session->migrate()`); `onSuccess` (`session->remove(LAST_USERNAME)`); `onFailure` (`session->set(LAST_USERNAME, username)`); `subscribe()` mapping. NEW `AccessListenerTest`: prioritise the request-time enforcement (mock `Auth`/`UrlProvider`/`Response`/`RequestStack`): `onLateRequest` (no `_access` → return; unauthenticated + denied → `HttpException(401)`; authenticated + denied → `AccessDeniedHttpException`; granted → no throw), `onRequest` (XHR / already-authenticated / no admin-access → return; else redirect to `@system/login`), `onAuthorize` (admin-area redirect guard throws). `onConfigureRoute`/`processAccessAttribute` (Reflection over `#[Access]` on a fixture controller + `Route`) are nice-to-have; if disproportionately costly, cover the core path and flag the remainder for 2.1.9. Flag `UserListener` (static `User::` delegations; only `subscribe()` unit-testable) for 2.1.9.
  8. **Run Infection and reach the gate (loop-risk, needs the coverage driver from Step 1).** With a driver active (`XDEBUG_MODE=coverage ./app/vendor/bin/infection --threads=4`, or PCOV without the env var), analyze MSI + Covered MSI. For each escaped mutant (priority order per §4.2): (a) default — add/strengthen a test, re-run to confirm the kill; (b) if it reveals a real bug or dead code — fix it (legitimate production change + a win) or flag via `github-issue-creator` if out of scope, never hide it; (c) if it is a true equivalent mutant — add an `infection.json.dist` `ignore` entry / `@` annotation with a one-line justification. Drive **overall MSI ≥ 80% AND Covered MSI ≥ 80%** on the configured classes by adding tests, NOT by lowering the threshold; document any genuinely DB/kernel-bound exclusions with a 2.1.9 reference (discovery note 6). Keep the PHPUnit suite green throughout. Do NOT add Infection to CI (Step 2.2).

## EXECUTION STATE
<!-- Machine-readable progress index for the Orchestrator/Conductor. Mirrors the Checklist 1:1
     (same numbers + short titles). Size hint per step: S = small/atomic, M = medium,
     L = large or loop-risk. A step orchestrator flips its box to [x] in the SAME commit as that
     step's code (after Tester PASS) — no self-referential SHA. -->
- [ ] Step 1 (M) — Install Infection + infection.json.dist + verify ignore/driver prereq
- [ ] Step 2 (S) — NativePasswordEncoderTest (hash/verify/salt-rejection)
- [ ] Step 3 (M) — DatabaseHandlerTest: cover read() + destroy()
- [ ] Step 4 (M) — UserProviderTest: validateCredentials + findByCredentials guard (flag DB lookups → 2.1.9)
- [ ] Step 5 (M) — RoleTest + UserTest (status flags, getStatusText, cached hasPermission)
- [ ] Step 6 (M) — LoginAttemptListenerTest (brute-force throttle boundaries)
- [ ] Step 7 (L) — AuthorizationListenerTest + AccessListenerTest (flag UserListener → 2.1.9)
- [ ] Step 8 (L) — Run Infection: reach 80% MSI + Covered MSI, kill escaped mutants (driver-dependent)

## TESTING STRATEGY
- **Per step (Tester subagent):** PHPUnit + PHPStan (mandatory after every checklist step). Steps 2–7 add/extend tests → suite must stay green and PHPStan-clean. Step 8 additionally requires an **Infection run** with a coverage driver enabled (PCOV/Xdebug — none installed by default, see discovery note 1) as its acceptance gate: **MSI ≥ 80% AND Covered MSI ≥ 80%** on the configured security-critical classes.
- **Final run (after Early Push, Tester subagent):** wait on the four PHP Quality CI jobs (`phpunit (8.2)`, `phpunit (8.3)`, `phpstan`, `cs-fixer`, `security-audit`) via `gh run watch` and run the 3 Playwright E2E specs locally **in parallel**; both must pass. See `.cursor/agents/tester.md` § End-of-ticket tests for the exact commands and `.cursor/rules/orchestrator-subagent-workflow.mdc` § Final Test for the workflow position.
