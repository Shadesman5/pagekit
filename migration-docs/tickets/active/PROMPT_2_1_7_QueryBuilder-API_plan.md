## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.1.7 — QueryBuilder API Standardization (GitHub Issue #154). Closes Phase 1 audit **Step 1.5 (Doctrine DBAL 3.x) ⚠️ → 🛡️**.
- **Scope:**
  - **Core API (`app/modules/database`):** `src/Query/QueryBuilder.php` (split `execute()`/`executeQuery($type)` → public `executeQuery(): Result` + `executeStatement(): int`, delete legacy `execute()`), `src/ORM/QueryBuilder.php` (internal calls + `@method` docblock).
  - **Call sites (verified inventory — see note):** `app/system/modules/site/src/Model/NodeModelTrait.php` (`:88`, `:163`), `app/system/src/Validator/Constraints/UniqueValidator.php` (`:69`), `packages/pagekit/blog/src/Controller/BlogController.php` (`:108`, `:114`), `packages/pagekit/blog/src/Model/PostModelTrait.php` (`:32`).
  - **DBAL type normalization:** `src/Types/JsonArrayType.php`, `src/Types/SimpleArrayType.php`, `index.php` (type registration), `src/Connection.php` (`registerCustomTypeMappings()`), `src/ORM/ModelTrait.php` (`toArray()` case), `app/system/src/Model/DataModelTrait.php` (Column attribute only).
  - **DBAL 3 audit-fix (Phase 1 debt):** `src/Connection.php` (`exec()` alias), `src/Utility.php` (`getSchemaManager`/`createSchema`/`migrate()`), `app/installer/src/Installer.php` (`:240`), `app/modules/application/src/Tests/DbUtil.php` (`else` branch rewrite).
  - **Tests:** `app/modules/database/src/Tests/*` (add `executeQuery`/`executeStatement` + JSON-type coverage).
  - **Generics (deferred from 2.1.6, #153/PR #212):** `@template`/typed-array annotations on ORM QueryBuilder & repository fetch returns.
- **Deferred:**
  - **Step 2.5 (Extension Safety System):** `UniqueValidator` static service-locator → DI removal (`UniqueValidator.php:22,24` `static mixed $db` + `setDb()`, wired at `app/system/index.php:90`). PHASE_2 §2.5 (lines 662–666; TD-05 / SL-2, DECISION 2026-07-08 = container-aware `ConstraintValidatorFactory`) owns this cleanup, and it is §2.5 line 666 that states the `->execute()` at `:69` + `mixed` typing "are already covered by Step 2.1.7." Only the in-scope `:69` `->execute()`→`->executeQuery()` migration belongs here (Checklist Step 1); the static-locator/DI removal is Step 2.5.
  - **Step 2.1.11 (EntityManager DI):** `NodeModelTrait.php:18` static request-scoped `$nodes` cache (PHASE_2 §2.1.11 line 548 / RC-2 — already tagged in code).
  - **Step 2.1.12 (Residual `mixed`):** `DataModelTrait.php:13` `public mixed $data` → `public ?array $data` property narrowing is explicitly assigned to 2.1.12 (PHASE_2 §2.1.12). This ticket only changes its **Column attribute** (`json_array`→`json`), NOT the property type.
  - **Out of scope (DBAL platform):** `$platform->getName()` deprecation (`ConnectionTest.php:145`) — not a 2.1.7 audit-finding item.
- **Bridges:** None. Core API is split, all call sites migrated, and the legacy `execute()` deleted **within the same checklist step** (no intermediate compat shim). Rule 4 (DELETE OVER WRAP) applies throughout.
- **⚠️ Discovery corrections (task prompt/PHASE_2 are partially stale — refactorer MUST use this inventory):**
  1. Prompt §3.1 call-site table is **incomplete**: it omits `BlogController.php:108` & `:114`, lists `NodeModelTrait` generically (there are **two** sites, `:88` and `:163`), and ignores the SQL builder's own internal `get()`/`first()`.
  2. **Column-argument sites need `->select(...)` rewrites, not a blind `->execute()`→`->executeQuery()` swap.** New `executeQuery(): Result` takes **no** args, but these pass columns: `NodeModelTrait:88` `->execute('n.id')`, `BlogController:108` `->execute('id')`, `BlogController:114` `->execute('id, username')`. Rewrite each as `->select(<cols>)->executeQuery()`.
  3. Prompt §5.2 / PHASE_2 line 431 reference `DatabaseHandler.php addColumn('data','json_array',…)` — **STALE, no such code exists** (grep: zero `json_array` in `DatabaseHandler.php`; migration `Version20251023061532.php` already uses `'json'`). The **only** entity attribute carrying `json_array` is `DataModelTrait.php:12`. No DatabaseHandler change.
  4. `Connection::exec()` has **zero production callers** (grep: only native `$pdo->exec()` in Debug storage + `$realConn->exec()` in DbUtil, which is itself rewritten in Step 4). Safe to delete.
  5. `Utility::migrate()` has **zero callers** (grep: all `->migrate()` hits are `MigrationService`/`Session`/`Installer::runMigrations`, none via `getUtility()`). Superseded by the 2.0.4 MigrationService. Recommend **deletion** (Rule 4) — this also removes the deprecated `new Comparator()` + DDL-via-`executeQuery()` findings and the now-unused `Comparator` import. Fallback if kept: `createComparator()` + `introspectSchema()` + DDL via `executeStatement()`.
- **Checklist:**
  1. **Standardize the QueryBuilder execution API (core).** In `Query/QueryBuilder.php`: replace the protected `executeQuery($type='select')` with **public `executeQuery(): Result`** (SELECT — builds via `getSQLForSelect()`, calls `$this->connection->executeQuery(...)`) and **public `executeStatement(): int`** (UPDATE/DELETE — no public params; select the write SQL internally, e.g. infer UPDATE when `!empty($this->parts['set'])` else DELETE, **or** add a private `$type` set by `update()`/`delete()`). ⚠️ The prompt's `match($this->type)` snippet is aspirational — **there is no `$this->type` property**; do not copy it verbatim. Rewire internals: `update()` and `delete()` → `$this->executeStatement()`; move the `$columns` select-guard (`if (empty($this->parts['select'])) $this->select($columns);`) from the old `execute()` into `get()` and `first()`, which then call `$this->executeQuery()`. **Delete** the public `execute()` wrapper. Then migrate every call site in the **same** step: `ORM/QueryBuilder.php` `:87` & `:121` `$this->query->execute()` → `->executeQuery()` and update the `@method \Doctrine\DBAL\Result execute(...)` docblock → `executeQuery(): Result` (+ add `executeStatement(): int`); `NodeModelTrait.php:88` `->execute('n.id')` → `->select('n.id')->executeQuery()`, `:163` `->execute()` → `->executeQuery()`; `UniqueValidator.php:69` `->execute()` → `->executeQuery()`; `BlogController.php:108` `->execute('id')` → `->select('id')->executeQuery()`, `:114` `->execute('id, username')` → `->select('id, username')->executeQuery()`; `PostModelTrait.php:32` `->execute()` → `->executeQuery()`. Verify DBAL 3 Result API is correct at each site (`fetchOne`/`fetchFirstColumn`/`fetchAllAssociative`/`fetchAssociative`). Finish by confirming zero `->execute(` remain on any QueryBuilder (`rg '->execute\(' app/ packages/ --type php | grep -v 'executeQuery\|executeStatement\|Connection\|PDO'`).
  2. **Normalize the JSON DBAL type `json_array` → `json`.** `JsonArrayType::getName()` returns `'json'` (§5.1) and refresh its class docblock (remove the 2.1.7 TODO; keep the array-safety description); `DataModelTrait.php:12` `#[ORM\Column(type: 'json_array')]` → `type: 'json'` (§5.2 — attribute only, **not** the `$data` property type); `ORM/ModelTrait.php:162` `case 'json_array':` → `case 'json':` (§5.3); `database/index.php` remove the redundant `Type::addType('json_array', …)` block (lines ~102–105) (§5.4); `Connection::registerCustomTypeMappings()` remove `registerDoctrineTypeMapping('json','json_array')` and `('json_array','json_array')` and update/remove the stale class-docblock TODO (lines ~116–139) (§5.5); keep the `simple_array` mapping unless PHPUnit proves it redundant; `SimpleArrayType` docblock note it is a JSON-fallback parser, not a compat layer (§5.6). Existing DB columns are unaffected (SQL type stays TEXT/JSON).
  3. **DBAL 3 audit-fix — Connection / Utility / Installer.** Delete `Connection::exec()` (Connection.php ~246–251; zero callers). `Utility.php`: `$this->connection->getSchemaManager()` → `createSchemaManager()` (line 30); `$this->manager->createSchema()` → `introspectSchema()` (line 31); **delete the dead `migrate()` method** (lines ~241–249) and drop the now-unused `use …\Comparator;` import (fallback if kept: `createComparator()` + `introspectSchema()` + DDL via `executeStatement()`); optionally rename Utility's own `getSchemaManager()` accessor (line 39) for consistency (no external callers — keep if churn is undesirable, update its docblock either way). `Installer.php:240`: `$db->getSchemaManager()` → `createSchemaManager()`.
  4. **DBAL 3 rewrite — `DbUtil` test-helper `else` branch.** In `app/modules/application/src/Tests/DbUtil.php::getConnection()`: `getSchemaManager()` → `createSchemaManager()` (lines 70, 71, 76); `$sm->createSchema()` → `introspectSchema()` (line 79); replace the removed `Schema::toDropSql()` (line 80) with DBAL 3 drop SQL (iterate introspected tables → `$platform->getDropTableSQL(...)`, or schema-manager drops); `$realConn->exec()` → `executeStatement()` (line 84); replace the silent empty `catch` (lines 83–91) with an **explicit** teardown strategy — FK-ordered drop **or** log-and-continue (no silent swallow), removing the `// good idea?` TODO. DbUtil/DbTestCase are dormant (no `extends DbTestCase` in the repo) so runtime impact is nil, but the branch must be PHPStan-clean.
  5. **Tests — cover the new API and JSON mapping (§6).** Re-verify no test calls `->execute()` on a QueryBuilder (currently none). Add/extend tests asserting `executeQuery()` returns `Doctrine\DBAL\Result` and `executeStatement()` returns `int` affected-rows (UPDATE + DELETE), plus a smoke test that a `json`-typed column round-trips an array via `JsonArrayType`. Follow existing `ConnectionTest` style; keep additions minimal.
  6. **Generic collection return types (§7; deferred from 2.1.6 — #153 / PR #212).** Add `@template T of object` / `@return array<int, T>` (or `array<int|string, T>`) annotations to ORM `QueryBuilder::get()`/`first()` and related model/repository fetch methods where entity types flow, extending the existing `@template T of object` pattern in `Connection::find()` / `ORM/Loader/AttributeLoader.php`. Developer-facing type accuracy only — **no runtime change** and **no new PHPStan Level-8 baseline entries**. If full class-level generics on `ORM\QueryBuilder` risk baseline churn, scope to the safe, clean subset.

## EXECUTION STATE
<!-- Machine-readable progress index for the Orchestrator/Conductor. Mirrors the Checklist 1:1
     (same numbers + short titles). Size hint per step: S = small/atomic, M = medium,
     L = large or loop-risk. A step orchestrator flips its box to [x] in the SAME commit as that
     step's code (after Tester PASS) — no self-referential SHA. -->
- [x] Step 1 (L) — Standardize QueryBuilder execution API (executeQuery/executeStatement, delete execute, migrate call sites)
- [x] Step 2 (M) — Normalize JSON DBAL type json_array → json
- [ ] Step 3 (M) — DBAL 3 audit-fix: Connection::exec + Utility + Installer
- [ ] Step 4 (M) — DBAL 3 rewrite of DbUtil test-helper else branch
- [ ] Step 5 (S) — Tests for executeQuery/executeStatement + JSON mapping
- [ ] Step 6 (L) — @template / typed-array generics on QueryBuilder & repository fetch returns

## TESTING STRATEGY
- **Per step (Tester subagent):** PHPUnit + PHPStan (mandatory after every checklist step)
- **Final run (after Early Push, Tester subagent):** wait on the four PHP Quality CI jobs (`phpunit (8.2)`, `phpunit (8.3)`, `phpstan`, `cs-fixer`, `security-audit`) via `gh run watch` and run the 3 Playwright E2E specs locally **in parallel**; both must pass. See `.cursor/agents/tester.md` § End-of-ticket tests for the exact commands and `.cursor/rules/orchestrator-subagent-workflow.mdc` § Final Test for the workflow position.
