## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.1.9 — Test Coverage Expansion (GitHub Issue #156, sub-issue of #147 / Step 2.1). Branch `feature/test-coverage-expansion` (base `develop`). **No Phase 1 audit cell closes in this PR.** The 1.10.5 E2E rework is genuinely large/orthogonal and has been re-homed to the new **Step 3.6.1 (E2E Test Suite Rework)** — see Deferred.
- **Plan-review reconciliation (companion doc edits made during this revision — commit them WITH this ticket):** resolving the plan-reviewer FAIL required three cross-file fixes that are already applied to the working tree and MUST land in this PR's plan commit:
  1. `.cursor/ROADMAP.md` — added the missing **2.8** (PHP Version Upgrade) and **2.9** (Phase 2 Closeout) rows, and a new **3.6.1** (E2E Test Suite Rework) row, so every deferral below points at a tracked ROADMAP ID.
  2. `migration-docs/TODO/PHASE_2_MODERNISING.md` §2.1.9 — removed the `Closes Phase 1 audit: Step 1.10.5` designation (it no longer contains that trigger string) so `push.mdc` Finalize will **not** falsely flip 1.10.5 when this PR closes; updated the Phase-1-audit E2E bullet to point at 3.6.1.
  3. `migration-docs/TODO/PHASE_3_MODERNISING.md` — added the **Step 3.6.1** section that now carries the `Closes Phase 1 audit: Step 1.10.5 → 🛡️` line (1.10.5 flips when 3.6.1 lands). GitHub issue for 3.6.1 is **to be created** via the `github-issue-creator` skill (Cloud-Agent architect cannot create issues under the read-only-`gh` constraint) — flagged in that section.
- **Fresh coverage baseline (regenerated at planning time — binding, NOT the stale #156 anchor):**
  - Measured with `./app/vendor/bin/phpunit --coverage-text --coverage-clover` on PHP 8.3.6 + PCOV 1.0.11, PHPUnit 11.5.55 (2026-07-09).
  - **Overall: Lines 3.21 % (1524/47527) · Methods 15.65 % (238/1521) · Classes 9.76 % (28/287).** (Historical #156 anchor was ~2.29 % lines on 30 Apr 2026 — coverage has grown; the CI gate in Step 12 is pinned to the fresh number, never the anchor.)
  - Suite health: 445 tests, 981 assertions, **0 Warnings, 0 Risky**, Deprecations 4, PHPUnit Deprecations 2, Skipped 5. The "4 PHP deprecations" are the `StreamWrapper::$context` dynamic-property source (Step 1); the 2 PHPUnit deprecations are framework-internal metadata (owned by 2.1.4/2.1.5, NOT this step).
  - Per-target snapshot (from per-class report): **auth** strong (`Auth` 72.4 %, `NativePasswordEncoder` 100 %, `DatabaseHandler` 95.7 %); **user** core strong (`User` 85.9 %, `Role` 100 %, listeners 100 %, `UserProvider` 81.3 %; gaps: `AccessModelTrait` 14.3 %, `UserListener` 66.7 %); **filter** ~100 % (only `JsonFilter` 66.7 %); **filesystem** strong (`Filesystem` 93 %, `Locator`/`Path` ~92-96 %, `StreamWrapper` 46.3 %); **database** LOW (`Connection` 87.9 % but `EntityManager` 26.3 %, ORM/Query `QueryBuilder` ~48 %, Relations 12-27 %, `Utility` 9.7 %); **site / widget** 0 % (no `Tests/` dir); **intl** mixed (`IntlServiceLocator`/`ArrayLoader` 100 %, `PhpFileLoader` 66.7 %, rest 0 %). NOTE: `phpunit.xml.dist <source>` covers `app/modules`, `app/system`, `app/console` only — **`packages/` is NOT in the measured scope** (blog/theme-one targets are out of the gate; ongoing effort).
- **Scope:** This ticket is the **concrete carried-over-debt + CI-infrastructure + edge-case consolidation** of the ongoing 2.1.9 effort — NOT a one-shot "raise every module to target %". Files/areas touched:
  - **Production (small, behaviour-preserving except the 2 hardening fixes):** `app/modules/filesystem/src/StreamWrapper.php`; `app/system/modules/mail/src/Message.php`; `app/modules/kernel/src/Event/ExceptionListener.php`; `app/modules/filter/src/AddRelNofollowFilter.php` (harden); `app/modules/auth/src/Handler/DatabaseHandler.php` + `app/modules/auth/index.php` (clock); `app/system/modules/user/src/Event/LoginAttemptListener.php` + `app/system/modules/user/index.php` (clock); `app/system/modules/site/src/Controller/MenuApiController.php` (validation).
  - **Tests (the bulk):** filter, auth, user, database (ORM cache), site (NEW `Tests/` dir), plus integration tests under `tests/Unit/` (Package, Console) and kernel/module DI-wiring. Existing test call-site touch-ups (`RouterTest`, `QueryBuilderCacheTest`, `MessageTest`, `DatabaseHandlerTest`, `LoginAttemptListenerTest`, `UserAccessTest`).
  - **Config / CI:** `phpunit.xml.dist` (`failOn*` flip), `infection.json.dist` (drop resolved ignores), `.github/workflows/php-quality.yml` (coverage gate + Codecov upload), `README.md` (coverage badge), `composer.json`/`composer.lock` (add `psr/clock` prod + `symfony/clock` dev if used).
  - **Test placement rule:** `app/installer/` is NOT in the `phpunit.xml.dist` testsuite paths — `PackageManager`/`MigrationCommand` integration tests go under `tests/Unit/` (e.g. `tests/Unit/Package/`, `tests/Unit/Console/`) reusing the in-memory-SQLite pattern from `tests/Unit/Migration/MigrationServiceTest.php` (`DriverManager::getConnection(['driver'=>'pdo_sqlite','memory'=>true])`).
  - **Cross-cutting policy (no dedicated step):** in every test file created/edited here, prefer `assertSame` over `assertEquals` for scalar/identity assertions (addresses the "~200+ assertEquals" audit finding incrementally, as files are touched).
- **Deferred (ongoing 2.1.9 backdrop + Step 2.9 closeout — explicitly OUT of this PR; all IDs below are now tracked in `.cursor/ROADMAP.md`):**
  - **Breadth coverage to the §1 target table** (core 80 %+, system 75 %+, packages 60 %+ per module) — the "grows with every feature branch" work; not achievable/appropriate in one PR. Highest-value gaps for later: `database` module (`EntityManager`, ORM `QueryBuilder`, Relations, `Utility`), `site`/`widget` modules (0 %), `intl` breadth, `StreamWrapper` methods, `AccessModelTrait`.
  - **Full E2E rework (Phase 1 audit 1.10.5)** — 11 specs exist, only the first ~3 are sound; a full Playwright rework is a large orthogonal effort. Re-homed to the newly-added **Step 3.6.1 (E2E Test Suite Rework)** (Phase 3, sequenced after the 3.6 `data-testid` strategy; GitHub issue to be created via the github-issue-creator skill). **This PR does NOT flip the 1.10.5 audit cell** (⚠️ stays; it flips when 3.6.1 lands) — `PHASE_2_MODERNISING.md` §2.1.9 was updated so 2.1.9 no longer claims that closure. Tester still runs the 3 sound specs at Final Test.
  - **`packages/` coverage** (blog 60 %+, theme-one 30 %+) — not in the measured `<source>` scope; ongoing.
  - **DB/kernel-bound unit gaps flagged by 2.1.8** (`UserProvider` happy-path lookups, `UserListener` handlers, `User::hasPermission` uncached branch) — these need a kernel/DB; consolidated in Step 2.9. Their `Deferred`-tagged test-file notes stay until then (they do NOT carry `infection.json.dist` ignores, so nothing to remove here).
  - **Ratcheting the Infection `minMsi`/`minCoveredMsi` gates** and widening Infection scope beyond auth+user → Step 2.9 closeout.
- **Bridges:** None. All changes are direct modernizations. Nuances:
  - Clock injection (Step 5) changes two constructors — this is an allowed internal breaking change (Rule 3), NOT a bridge. To bound churn, add the clock as the **last constructor parameter defaulting to a real `Symfony\Component\Clock\Clock`** (idiomatic Symfony pattern); only the new boundary tests inject a `MockClock`, so the ~20 existing `DatabaseHandlerTest` call sites need no signature change. Wire the real clock explicitly at the two factory sites (`auth/index.php:23`, `user/index.php:160`).
  - Codecov (Step 13): the CI upload step + README badge are code; **full activation is external** (install the Codecov GitHub App on the repo + add a `CODECOV_TOKEN` Cursor/Actions secret for reliable + fork uploads and per-PR delta comments). Make the action non-blocking (`fail_ci_if_error: false`) and document the human prerequisite in the branch doc — do NOT block the CI gate on it.
- **Current-state verification of §3.4 / audit tasks (all confirmed at planning time):**
  - RouterTest `blog.permalink` — **OPEN** (`RouterTest.php:215/218/224`).
  - `setAccessible()` — **OPEN**, all documented sites present: `RouterTest.php:213,252`, `QueryBuilderCacheTest.php:77,92,219`, `MessageTest.php:207`, `Message.php:391,403,438`, `ExceptionListener.php:67`.
  - `StreamWrapper::$context` — **OPEN** (no `$context` property declared).
  - Injectable clock — **OPEN** (`psr/clock` present transitively; no `ClockInterface` use in prod; `DatabaseHandler`/`LoginAttemptListener` carry `Deferred → 2.1.9 (clock injection)` notes; both `LessThan` ignores present in `infection.json.dist`).
  - `failOn*` flip — **OPEN** (`failOnWarning`/`failOnPhpunitWarning`/`failOnRisky` = "false"; normal suite already 0 warnings/0 risky, so the flip is safe for the plain run — the ignores it unmasks are Infection-only).
  - AddRelNofollow — **OPEN** (`AddRelNofollowTest` has ONE method + a 3-case TODO comment, not 3 written tests; filter regex `|<a (.+?)>|i` is naive).
  - MenuApiController — **OPEN** (manual `trim`/`BadRequestHttpException`/bare `\Exception`; no `#[Assert]`/`ValidatesRequestTrait`; site module has NO `Tests/` dir).
  - ORM cache invalidation test — **OPEN** (`EntityManagerTest` exists but no cache/save/delete/invalidate assertions).
  - DI-wiring tests — **PARTIAL**: `ControllerResolverTest` now covers constructor-injection resolution (6 tests) → do NOT duplicate; remaining audit items open (module `$app ?? App::getInstance()` fallback, factory-service freshness e.g. `finder`, `PackageManager` with/without container).
  - PackageManager enable/uninstall integration — **OPEN** (no tests). MigrationCommand CLI — **OPEN** (no tests; source at `app/console/src/Commands/MigrationCommand.php`).
- **Checklist:**
  1. **Declare `StreamWrapper::$context` (TD-16).** Add `public $context;` (untyped on purpose — PHP assigns `null` or a stream-context resource) to `app/modules/filesystem/src/StreamWrapper.php`. Kills the 4× "Creation of dynamic property … $context is deprecated" notices → suite deprecation-clean except the 2 PHPUnit-internal ones. Verify with `./app/vendor/bin/phpunit` (Deprecations should drop 4→0 PHP-level).
  2. **Remove all `setAccessible()` calls (PHP 8.5 forward-compat).** No-op since 8.1, `#[\Deprecated]` in 8.5. Delete at: `app/modules/routing/src/Tests/RouterTest.php:213,252`; `app/modules/database/src/Tests/ORM/QueryBuilderCacheTest.php:77,92,219`; `app/system/modules/mail/src/Tests/MessageTest.php:207`; `app/system/modules/mail/src/Message.php:391,403,438`; `app/modules/kernel/src/Event/ExceptionListener.php:67`. Behaviour-preserving on 8.2+. `rg -n 'setAccessible' app/` must return nothing after.
  3. **Decouple `RouterTest` from extension config.** In `RouterTest::testCacheKeyReflectsRouteAffectingOptions()` replace the `blog.permalink` option (blog-package-specific) with a generic name (e.g. `test.route_option`); update the docblock/assertion message. Behaviour under test (route-affecting options participate in the router cache key) is generic core logic — core tests must not depend on extension config.
  4. **Harden `AddRelNofollowFilter` + add the 3 XSS edge-case tests.** Fix the regex/logic so it (a) matches slash-obfuscated `<a/href=...>`, (b) is safe against null-byte `<\0a\0 href=...>` obfuscation, (c) replaces an existing `rel="follow"` with `rel="nofollow"` (current `str_replace` only strips `rel="nofollow"`). Then add the 3 edge-case assertions (currently only a TODO comment in `app/modules/filter/src/Tests/AddRelNofollowTest.php`) and keep the 2 existing ones green. Production hardening + tests together.
  5. **Injectable clock → make the two time boundaries deterministically testable + drop the 2 clock `LessThan` ignores (loop-risk).** Add `psr/clock` as a direct prod dependency; use `Symfony\Component\Clock\Clock` (add `symfony/clock`, `MockClock` for tests) as the default. Refactor `DatabaseHandler` (`read():53` boundary `strtotime($access)+timeout < time()`) and `LoginAttemptListener` (`onPreAuthenticate():43` boundary `(time()-$last) < DELAY`) to read time via an injected `ClockInterface` (last constructor param, default real `Clock`); wire the real clock at `app/modules/auth/index.php:23` and `app/system/modules/user/index.php:160`. Add exact-equality boundary tests (using `MockClock`) that kill both mutants, then remove the two `mutators.LessThan` ignores `Pagekit\Auth\Handler\DatabaseHandler::read::53` and `Pagekit\User\Event\LoginAttemptListener::onPreAuthenticate::43` from `infection.json.dist`. Re-run scoped Infection (PCOV/Xdebug required) to confirm both are killed and MSI gates hold. Update the `Deferred → 2.1.9 (clock injection)` docblocks in `DatabaseHandlerTest.php`/`LoginAttemptListenerTest.php`.
  6. **Flip `failOnWarning`/`failOnPhpunitWarning`/`failOnRisky` → "true" + drop the `UserAccessTest`-masked Infection ignores.** In `phpunit.xml.dist` set the three 2.1.9-owned gates to `"true"` (update the header comment; leave the 2.1.4/2.1.5-owned deprecation gates untouched) and confirm the plain suite stays green (already 0 warnings/0 risky). Then re-run scoped Infection: the boolean-parser mutants that were masked by the `failOnWarning="false"` + "Uninitialized string offset" warning are now killed → remove the corresponding `User::parseOrExpr/parseAndExpr/parseNotExpr/parseAtom` ignores documented in `app/system/modules/user/src/Tests/UserAccessTest.php:18-33` (groups: `LessThan`, `LessThanNegotiation`, `GreaterThanOrEqualTo`, `LogicalOr`, `LogicalAnd` — the user-model parser entries only; keep the LoginAttemptListener/AccessListener/2.1.8 entries). If any genuinely equivalent mutant still escapes after the flip, re-add ONLY that entry with an `Equivalent` (not `Deferred`) justification. Update the `UserAccessTest` docblock to reflect the resolution.
  7. **ORM query-cache invalidation regression test (data-integrity — not optional).** Add a test (extend `app/modules/database/src/Tests/ORM/EntityManagerTest.php` or a focused new test) proving a cached query result is actually evicted after a write: `EntityManager::save()` and `::delete()` call `invalidateCache()` → `$cache->clear()` (`app/modules/database/src/ORM/EntityManager.php`). Assert eviction via a spy/mock `CacheItemPoolInterface` (expect `clear()` on save + on delete) and/or a cache-hit-then-miss round trip with in-memory SQLite. Doubles as the safety net for the planned Step 4.3 tag-based-invalidation switch.
  8. **`MenuApiController` validation refactor + tests.** Replace manual validation (`trim`, `BadRequestHttpException`, bare `\Exception('Menu ID is required')`, ad-hoc `json_decode`) with `#[Assert\...]` constraints + `ValidatesRequestTrait` (match the module's established pattern; see `Pagekit\System\Controller\ValidatesRequestTrait`). Create the site module's first `Tests/` dir (`app/system/modules/site/src/Tests/MenuApiControllerTest.php`) covering valid save, invalid/empty id → structured error, duplicate-id conflict, and delete-without-id. Keep the standardized JSON error shape.
  9. **DI-wiring integration tests (remaining audit items; ControllerResolver already covered).** Add tests for: the module `$app ?? App::getInstance()` fallback resolving correctly; factory-service freshness (e.g. `finder` returns a fresh instance per resolution vs. a shared singleton service); and container resolution for a couple of the Stage-3-migrated module classes. Place under `tests/Unit/` (e.g. `tests/Unit/Container/` or the kernel module `Tests/`). Do NOT re-test `ControllerResolver` constructor injection (already 6 tests).
  10. **`PackageManager::enable()`/`uninstall()` migration integration tests (loop-risk — container/Composer coupling).** Under `tests/Unit/Package/`, cover: auto-migrate on `enable()`, auto-rollback on `uninstall()`, and partial rollback to the pre-migration version, plus `PackageManager` behaviour with/without container availability (the open DI-wiring audit item). Reuse the in-memory-SQLite pattern; stub/mac Composer + package configs where a full kernel is infeasible. If a sub-path genuinely needs a booted kernel, cover the feasible orchestration surface and flag the remainder for the ongoing effort (test-file note) — do NOT weaken assertions.
  11. **`MigrationCommand` CLI integration test.** Under `tests/Unit/Console/`, test the unified Doctrine-migrations + scripts pipeline with the version-bump guard, using Symfony `CommandTester` + in-memory SQLite. Cover the happy path and the version-bump guard branch.
  12. **CI minimum line-coverage gate (§4.2).** In `.github/workflows/php-quality.yml` (the `phpunit` 8.3 leg, which already emits `coverage.xml`), add a step that parses the Clover `<metrics>` line rate and **fails the job below the documented baseline**. Pin the threshold to the branch's line coverage measured after Steps 1-11 land (it will be ≥ the 3.21 % pre-ticket baseline; round DOWN to avoid float flakiness) and record the exact number in the branch doc. Ratchets upward over time, never below. Keep it a plain script (no new tool needed).
  13. **Codecov integration — badge + per-PR delta (§4.3).** Add a `codecov/codecov-action` upload step (pinned SHA) consuming `coverage.xml` in the `phpunit` 8.3 leg with `fail_ci_if_error: false` (non-blocking), and add a Codecov coverage badge to `README.md` (alongside the existing PHP/Symfony/Vue badges). Document in the branch doc that per-PR delta comments + a reliable badge require a human to install the Codecov GitHub App on `Shadesman5/pagekit` and add a `CODECOV_TOKEN` secret (Coveralls is an acceptable equivalent). This is the codeable half; external activation is flagged, not blocking.

## EXECUTION STATE
<!-- Machine-readable progress index for the Orchestrator/Conductor. Mirrors the Checklist 1:1
     (same numbers + short titles). Size hint per step: S = small/atomic, M = medium,
     L = large or loop-risk. A step orchestrator flips its box to [x] in the SAME commit as that
     step's code (after Tester PASS) — no self-referential SHA. -->
- [x] Step 1 (S) — Declare StreamWrapper::$context (TD-16, kills 4 deprecations)
- [x] Step 2 (S) — Remove all setAccessible() calls (tests + production)
- [x] Step 3 (S) — Decouple RouterTest from blog.permalink → generic option
- [x] Step 4 (M) — Harden AddRelNofollowFilter + 3 XSS edge-case tests
- [x] Step 5 (L) — Injectable clock (DatabaseHandler + LoginAttemptListener) + boundary tests + drop 2 LessThan ignores
- [x] Step 6 (M) — Flip failOnWarning/PhpunitWarning/Risky + re-run Infection + drop UserAccessTest-masked ignores
- [x] Step 7 (M) — ORM query-cache invalidation regression test (save/delete → clear)
- [x] Step 8 (M) — MenuApiController #[Assert]/ValidatesRequestTrait refactor + tests (new site Tests/ dir)
- [x] Step 9 (M) — DI-wiring tests (module $app fallback, factory/finder freshness)
- [x] Step 10 (L) — PackageManager enable()/uninstall() migration integration + container tests
- [ ] Step 11 (M) — MigrationCommand CLI integration test (CommandTester + in-memory SQLite)
- [ ] Step 12 (M) — CI minimum line-coverage gate pinned to measured baseline
- [ ] Step 13 (M) — Codecov upload step + README badge (external activation flagged)

## TESTING STRATEGY
- **Per step (Tester subagent):** PHPUnit + PHPStan (mandatory after every checklist step). All test-adding/edit steps must keep the full suite green and PHPStan-clean. **Steps 5 and 6 additionally require an Infection run** (coverage driver required — install PCOV `sudo apt-get install -y php8.3-pcov` or Xdebug with `XDEBUG_MODE=coverage`; none ships in the VM by default) to confirm the targeted mutants are killed and `minMsi`/`minCoveredMsi` (80/80) still hold before the corresponding `infection.json.dist` ignores are removed. Step 1 must show PHP-level Deprecations drop from 4 to 0. Step 12 verifies the new CI gate parses `coverage.xml` and fails below the pinned threshold.
- **Final run (after Early Push):** per `.cursor/agents/tester.md` § End-of-ticket tests, the **Tester subagent runs the local Playwright E2E suite ONLY** — the 3 stable specs, sequentially (`01-setup/installation.spec.js`, `02-core/authentication.spec.js`, `02-core/dashboard.spec.js`), with the clean-state (`rm -f pagekit.db config.php`) + `php pagekit setup`/`php pagekit list` smoke prep; all 3 must pass. The Tester does **NOT** wait on CI. The **Orchestrator owns the CI wait** — `gh run watch` on the five PHP Quality CI checks (`phpunit (8.2)`, `phpunit (8.3)`, `phpstan`, `cs-fixer`, `security-audit`). Both the local E2E and all CI checks must pass. See `.cursor/rules/orchestrator-subagent-workflow.mdc` § Final Test for the workflow position.
