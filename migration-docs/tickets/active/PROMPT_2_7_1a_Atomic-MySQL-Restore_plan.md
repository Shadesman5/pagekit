# Ticket: Step 2.7.1a — Atomic MySQL Restore (Shadow Cut-over)

## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.7.1a — Atomic MySQL Restore (Shadow Cut-over). GitHub Issue #281. Base `develop`. Dependency verified on this tree: Step 2.7.1 landed (`app/installer/src/Package/Snapshot/` holds `SnapshotStore`, `DumpFormat`, `DatabaseDumper`, `DatabaseRestorer`, `PackageSnapshotter`; snapshots panel + `tests/Unit/Snapshot/` exist; `origin/develop` is an ancestor of this branch). Step 2.7.1b has NOT landed — the restorer is rewritten here in place and relocated there, never the other way round.
- **Scope:**
  - `app/modules/database/index.php` — `mysql` connection default `'prefix' => ''` becomes `'pk_'` (matches the `sqlite` default; the disagreement is the bug).
  - `app/installer/src/TablePrefix.php` (new, small final class next to `Installer.php`/`StorageLink.php`) — the single home of the prefix shape rule `^[A-Za-z][A-Za-z0-9_]*_$` (starts with a letter → the reserved `_`-namespace can never be installed into; ends with `_` → a delimiter, so `pk` cannot match both `pkusers` and `pk_users`; no `.`/`-` — invalid in unquoted MySQL identifiers). Returns an actionable refusal message or null.
  - `app/installer/src/Installer.php` — server-side enforcement: `check()` validates the submitted connections' `prefix` values through `TablePrefix` **only when `!$this->config`** (fresh install; an existing installation — empty prefix included — must keep booting and is never validated here). Refusal surfaces through the existing `BadRequestHttpException` → status/message path, so both the web installer and the CLI show it.
  - `app/console/src/Commands/SetupCommand.php` — `--db-prefix=` (empty string) passes through to the Installer and is refused with the validator's message (never silently coerced to the default); a valueless `--db-prefix` (null) counts as not given, so the connection carries no `prefix` key and the module default `pk_` applies. Option help names the shape.
  - `app/modules/application/src/Module/Loader/EnvConfigLoader.php` (+ `src/Tests/EnvConfigLoaderTest.php`) — blank `PAGEKIT_DB_PREFIX` reads as unset (same pattern as `PAGEKIT_DB_DRIVER`/`PAGEKIT_DB_PORT`), never overlays `''`. No shape validation at boot — a container overlaying an odd but non-empty prefix must not end every boot. Class docblock sentence updated.
  - `AGENTS.md` — the caveat sentence "except `PAGEKIT_DB_DRIVER` and `PAGEKIT_DB_PORT` … read as unset when blank" gains `PAGEKIT_DB_PREFIX`.
  - `app/installer/src/Package/Snapshot/DatabaseDumper.php` — `schema()` skips reserved shadow/backup names (`_r_`/`_b_`), which matters on an empty-prefix installation where `str_starts_with($name, '')` is every table; dumping stays allowed there so uninstall is not stranded.
  - `app/installer/src/Package/Snapshot/DatabaseRestorer.php` — the rewrite: MySQL preflight refusals (empty prefix; rewritten name over 64 chars; shadow/backup collision; inbound FKs from outside the dump; undroppable leftovers; dump naming a reserved table), shadow fill into `_r_` tables, single-`RENAME TABLE` cut-over with backup drop, `GET_LOCK` serialization, leftover cleanup, deletion of the MySQL reach into the in-place drop-and-recreate. SQLite transactional apply (`replace()` SQLite branch, `recreate()`, `apply()`) untouched. Class docblock rewritten — the "MySQL leaves it partly replaced" essay describes behaviour this step deletes.
  - `app/installer/src/Package/Snapshot/` (new, 1–2 small final classes named by the Refactorer) — reserved-name constants + shadow/backup naming (shared by dumper and restorer), and the token-aware DDL rewriter (the data-loss risk centre, unit-testable platform-free). Same directory only — the whole namespace relocates in 2.7.1b as one unit; nothing lands outside it.
  - `.github/workflows/php-tests.yml` — new **required-posture** job `phpunit-mysql-snapshot` (mysql:8.4 service, `pdo_mysql`, **no** `continue-on-error`) running `./app/vendor/bin/phpunit -c phpunit-mysql.xml.dist tests/Unit/Snapshot`. The advisory full-suite `phpunit-mysql` job and its Step 2.11 forward-debt tag stay exactly as they are; existing required job names unchanged. Stale "only ConfigManagerTest/EntityManagerTest honor these globals" sentences in `php-tests.yml` and `phpunit-mysql.xml.dist` corrected (the Snapshot suite honors them through `SnapshotDatabase`/`DbUtil`).
  - `README.md` — one short operator note next to the existing snapshots/backup passage (§ around line 256): a MySQL restore needs roughly 2× the dumped tables' disk until backups are dropped; the rename takes metadata locks (a brief stall, not zero downtime); foreign-key/CHECK constraint names are regenerated by a MySQL restore (MySQL cannot rename constraints in place).
  - Tests — `tests/Unit/Snapshot/` (restorer preflight/fill/cut-over/lock, dumper guard, DDL rewriter), `tests/Unit/Installer/` (prefix validation), `app/modules/application/src/Tests/EnvConfigLoaderTest.php` (blank prefix), console/installer refusal coverage for `--db-prefix=`.
  - **No changes:** `docker/entrypoint.sh` (its `-n` guard already skips a blank `PAGEKIT_DB_PREFIX` for auto-setup), `prod.env.example` (already `pk_`), SQLite restore behaviour (empty prefix included), `PackageSnapshotter` files-then-dump restore order, dump format version, the snapshots panel Vue code, `Connection::replacePrefix()`.
- **Deferred:** [all targets already scoped in `PHASE_2_MODERNISING.md` — no PHASE amendment required by this plan]
  - Step 2.7.1b — relocating the (now rewritten) restorer and the rest of the Snapshot/package machinery into a package module of its own. PHASE §2.7.1b already names it; the rewrite lands first so the file is not moved mid-flight.
  - Step 2.9 — updater orchestration and update-time rollback consuming the now fail-safe MySQL restore. PHASE §2.9 already scopes it.
  - Step 2.11 — flipping the advisory full-suite `phpunit-mysql` job to required once the whole suite is DB-portable. PHASE §2.11 already states that the Snapshot-only required job added here does **not** satisfy it; the existing `// TODO: Must be refactored in Step 2.11 (Phase 2 Closeout)` tag at the advisory job stays.
  - Non-goals (explicitly rejected, not deferred): a fallback in-place MySQL apply; hashed live table names; `AUTO_INCREMENT` values in dump DDL; a MariaDB CI leg; dump-format version bump / checksums / views-triggers-routines; container or host-level snapshots; changing `PackageSnapshotter` restore order; Vue/admin copy changes; a repo-wide comment-prose sweep of existing Snapshot/Extension Safety docblocks.
- **Bridges:** None. Checklist Step 4 builds the shadow-fill machinery as unit-tested internals that `restore()` does not yet reach; Step 5 wires it in and deletes the MySQL reach into the in-place apply in the same commit — at every commit exactly one MySQL path is reachable, so no bridge tag is needed and none may appear. No `Step 2.7.1a` references in code or tests.
- **⚠️ Architect decisions + verified discovery (Refactorer MUST heed):**
  1. **Prefix invariant home (Discovery Q1).** One validator, `TablePrefix` in `app/installer/src/` (wizard-side — it stays in `installer` when 2.7.1b moves the package machinery out). Enforcement point is `Installer::check()` under `!$this->config`, which both entries flow through (web installer posts to it; `SetupCommand` calls `install()` → `check()`), so the CLI refusal of `--db-prefix=` is the same code path surfaced by the existing error handling — one rule, zero drift. `EnvConfigLoader` deliberately does **not** consume the validator: its only change is blank-reads-as-unset, because a boot that dies on an ill-shaped overlay would break the "existing installations keep booting" promise. The shape's leading-letter requirement is what keeps the reserved `_r_`/`_b_` namespace uninstallable — no separate reserved-prefix list in the validator, but its docblock says why the leading letter is load-bearing.
  2. **Reserved names + ownership.** Constants for the `_r_` (shadow) and `_b_` (backup) markers live in one small class in `Snapshot/`, consumed by dumper (skip in `schema()`), restorer (naming, cleanup, refusals) and tests. Equal length, outside any legal install prefix. A reserved-marker table whose remainder starts with this installation's prefix is **owned** by this restorer (safe to drop at cleanup); any other collision with a needed `_r_`/`_b_` name is a refusal. Ownership matching and collision checks both follow the server's `lower_case_table_names` folding (read the variable; fold both sides when it says the server does) — never PHP's default case sensitivity (Discovery Q6).
  3. **Refusal wording (Discovery Q2).** Empty-prefix MySQL restore is refused in `restore()` before the dump is even opened (cheapest refusal; `inspect()` covers dump-shape refusals, this one is about the installation). The message must name the fact and the action, e.g.: "This installation's tables have no name prefix, so a restore cannot tell them apart from the rest of the database while it swaps them. Reinstall with a table prefix (the default is pk_) to make restores possible." — final copy is the Refactorer's, but it is operator-facing (it reaches the panel via the existing `SnapshotController::failed()` path) and must never be a platform lecture.
  4. **Constraint renaming (Discovery Q3).** MySQL 8 foreign-key and CHECK constraint names are schema-unique and capped at 64 chars. Regenerate every one in shadow DDL as a short token (e.g. `_r_` + hex from hash of table + original name, well under 64) — never prefix the dump's names (overflow + collision with live). Regenerated names cannot collide with live names, and when live tables become `_b_` they keep their original constraint names (RENAME TABLE does not rename constraints), so no collision appears at cut-over either. Index names are per-table in MySQL — inside a freshly created shadow table no collision is possible, so index names are **never** rewritten. Consequence to document (README note): constraint names on a MySQL-restored installation are regenerated tokens; MySQL has no RENAME CONSTRAINT, so they stay.
  5. **Shadow DDL derivation (Discovery Q4) — the corruption risk centre.** Shadow DDL is the dump's DDL with table identifiers and `REFERENCES` targets rewritten and constraint names regenerated. Never `CREATE TABLE … LIKE` live tables (live schema may have migrated since the snapshot). Verified: the dump's DDL comes from DBAL `getCreateTableSQL(...)` on `AbstractMySQLPlatform` and may contain **unquoted** identifiers and multiple statements per table (`CREATE TABLE`, `ALTER TABLE … ADD CONSTRAINT … REFERENCES`, `CREATE [UNIQUE] INDEX … ON`). The rewriter must therefore tokenize: honour backtick-quoted identifiers and `'…'` string literals (a `DEFAULT 'pk_users'` literal is data, never rewritten — this is the "column value contains a table name" test the prompt demands), and rewrite identifier tokens only in grammar positions (`CREATE TABLE <t>`, `ALTER TABLE <t>`, `REFERENCES <t>`, `CONSTRAINT <c>`). Rewritten output always emits backtick-quoted identifiers. `REFERENCES` targets are rewritten **only** for tables the dump names; an outbound FK to a table outside the dump keeps pointing at the live table. Row data is never rewritten at all — inserts go through the prepared `INSERT INTO <quoted shadow name>` with bound values, which is why values cannot be corrupted. A `str_replace` over DDL text is a rejected design, not a shortcut.
  6. **Connection placeholder trap.** `Connection::replacePrefix()` substitutes `@name` outside quoted strings on **every** `prepare`/`executeQuery`/`executeStatement`. All shadow SQL uses literal quoted identifiers (backticks) and never a `@`-placeholder spelling like `@_r_users`; `GET_LOCK('literal', …)` arguments are quoted strings and safe.
  7. **Preflight refusals (the feature).** All MySQL refusals run after `inspect()` has read the whole dump and before the first `CREATE`, while the installation is untouched: (a) empty prefix (decision 3, before the dump is opened); (b) a rewritten table name over 64 chars (`_r_`/`_b_` add 3 chars, so live names over 61 refuse; regenerated constraint tokens are short by construction, but the check stays general); (c) a needed shadow/backup name colliding with an existing non-owned table (folding per decision 2); (d) inbound FKs — any table **not** in the dump referencing a dumped table (`information_schema.REFERENTIAL_CONSTRAINTS` + `KEY_COLUMN_USAGE`, current schema, folding-aware), because InnoDB would remap that FK onto the `_b_` name at rename and the backup drop then fails or a neighbour hangs; the message names the offending tables/constraints; (e) owned leftovers that will not drop (Step 6 turns presence-refusal into drop-then-refuse-on-failure, after the lock is held); (f) a dump that names a reserved `_r_`/`_b_` table (both platforms — never legitimate, and unguarded it would create `_r__r_…`).
  8. **Fill and cut-over.** Fill: create shadow tables and insert rows in dump order under the existing `FOREIGN_KEY_CHECKS = 0` window (FKs among shadows may point at not-yet-created shadows; MySQL accepts that with checks off). Any fill failure drops all shadow tables in `finally` — disk-full and timeouts may not leave undeclared `_r_` names. Cut-over: **one** `RENAME TABLE` statement listing every dumped table — live → `_b_` and `_r_` → live; a dumped table with no live counterpart contributes only `_r_` → live; live tables outside the dump are untouched. MySQL executes a multi-pair RENAME atomically; splitting it lets InnoDB remap FKs among the dumped group onto backup names — if the plan or the code splits it, the plan/code is wrong. Lock-wait or `max_allowed_packet` failure on that statement is a failure **before** cut-over: drop shadows, live tables intact. After a successful rename, drop the `_b_` tables (still inside the FK-checks-off window); a failed backup drop is a logged warning on a **successful** restore — the site is already restored, and the next restore's leftover cleanup clears the names.
  9. **One MySQL path.** Step 5 deletes the MySQL reach into `apply()`/`recreate()` in the same commit that wires the shadow path in. `recreate()` and `apply()` themselves **stay** — they are the SQLite transactional apply, which already keeps the promise and is not unified into any new abstraction. `replace()` ends up: SQLite → transaction + `apply()` (unchanged); MySQL → preflight + shadow fill + cut-over; anything else already throws in `DumpFormat::platform()`.
  10. **Serialization (Discovery Q5).** MySQL `GET_LOCK` taken on the restore's own connection before leftover cleanup and held across preflight, fill, cut-over and backup drop; `RELEASE_LOCK` in `finally` (server also releases on disconnect). Lock name ≤ 64 chars, scoped to installation, e.g. `pagekit.restore.` + short hash of schema name + prefix — server-wide names must not collide across installations sharing a server. Timeout 0 (or a small constant): a second concurrent restore is **refused** with "another restore is already running", not queued — Vue `busy` is UI state, not a lock. Leftover cleanup runs strictly after the lock is held, so one restore can never drop another's in-flight shadows.
  11. **Existing installations keep working.** The mysql module default flip cannot strand an installed site: the installer writes `prefix` into `config.php` (`persistableDatabaseConfig()` fills it from resolved config when the form omits it), so installed sites carry their own value; a hand-written `config.php` that omits `prefix` relying on the implicit `''` is the one edge the default flip moves — acceptable per PHASE ("MySQL module default becomes pk_"), noted in the branch doc. An installation with an explicit empty prefix boots, dumps (with the reserved-name skip) and uninstalls exactly as before; only MySQL **restore** refuses it, with the decision-3 message. SQLite restore stays transactional, empty prefix included.
  12. **Test reality in this VM.** The cloud VM has no MySQL server (`pdo_mysql` loaded, passwordless sudo available). All MySQL-behaviour tests go through the existing `SnapshotDatabase`/`DbUtil` pattern: they run for real when the `db_*` globals name a server (`phpunit-mysql.xml.dist`) and skip cleanly on SQLite-only runs. Refusal-**ordering** tests that need only a MySQL-reporting platform (e.g. empty-prefix refusal thrown before any SQL) may use a test-double connection in the house style of `ConnectionOnAnUnsupportedDatabase`. The DDL rewriter is pure string logic and fully testable without any database. The authoritative MySQL gate is the new CI job on mysql:8.4; a step orchestrator MAY additionally provision a local server (`sudo apt-get install -y mysql-server`, create root/root@127.0.0.1:3306 to match `phpunit-mysql.xml.dist`) and run `./app/vendor/bin/phpunit -c phpunit-mysql.xml.dist tests/Unit/Snapshot` — never fake a MySQL result.
  13. **Discipline.** PHPStan level 8, no new baseline entries (`app/` is analysed). The flipped test assertion (decision in TESTING) replaces the `markTestSkipped` MySQL leg in `DatabaseRestorerTest` — renamed platform-neutrally, asserting live tables unchanged **and** no `_r_`/`_b_` leftovers after a failed apply. No completed-step narration in code, tests or docblocks; the only permitted step tags in touched files are the pre-existing 2.11 tag in `php-tests.yml`. Conventional Commits; one version bump at Finalize only.
- **Checklist:**
  1. **Safety gates + prefix invariant at install.** Gates first: branch up-to-date with `develop`; `composer install --no-interaction --prefer-dist --no-progress`; `./app/vendor/bin/phpunit`; `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M`; `pnpm install --frozen-lockfile && pnpm build` — any red → STOP, report the pre-existing failure. Then decisions 1 + 11: `TablePrefix` validator; `Installer::check()` enforcement (fresh installs only); mysql module default `pk_`; `SetupCommand` option handling (`''` refused via Installer, valueless flag = not given); `EnvConfigLoader` blank-prefix-as-unset + docblock; AGENTS.md caveat sentence. No restorer change. Tests: shape table (valid/invalid incl. leading `_`, missing trailing `_`, `.`/`-`, empty), installer refusal message reaches the result, `--db-prefix=` fails the command, blank env var overlays nothing (putenv + tearDown, `Request::setTrustedProxies()` untouched here), an existing empty-prefix connection still constructs and queries.
  2. **Reserved names + dumper guard.** Decision 2 constants class; `DatabaseDumper::schema()` skips reserved names. Tests: `_r_`/`_b_` tables never enter a dump — including on an empty-prefix installation whose dump still runs; prefixed dump unaffected.
  3. **Preflight refusals before the first CREATE.** Decision 7 (a)–(f) with (e) as presence-refusal for now; folding per decision 2; refusal messages name the offender. All refusals leave the installation whole (assert: tables untouched, no reserved names created). SQLite path untouched; dump-shape refusals stay in `inspect()`.
  4. **Shadow DDL rewriter + fill machinery.** Decisions 5 + 8 (fill half) + 6: token-aware rewriter class (identifier/literal-aware, grammar-position rewriting, constraint-name regeneration per decision 4); shadow create + prepared inserts into `_r_` names; failure drops shadows in `finally`. Internals + tests only — `restore()` still routes MySQL through the old in-place path at this commit (single reachable path, decision 9 lands next step). Rewriter tests are the priority: string-literal-containing-a-table-name case, quoted + unquoted inputs, multi-statement DDL, outbound-FK target outside the dump left alone, regenerated constraint names unique and ≤ 64.
  5. **Cut-over + delete the in-place MySQL apply.** Decisions 8 (cut-over half) + 9: wire preflight + fill + single `RENAME TABLE` + backup drop into `replace()`'s MySQL branch and delete the MySQL reach into `apply()` in the same commit; class docblock rewritten (decision 13); README operator note (Scope). Flip the skipped assertion: a failed MySQL apply leaves live tables byte-identical and zero `_r_`/`_b_` leftovers; retry-after-failure test stays green; success path asserts backups gone, dumped-only tables present, post-snapshot tables outside the dump untouched.
  6. **Serialization + leftover cleanup.** Decision 10: `GET_LOCK`/`RELEASE_LOCK` bracketing; owned-leftover drop after the lock (decision 2 ownership), turning step 3's presence-refusal into drop-then-refuse-on-drop-failure. Tests: two connections — second restore refused while the first holds the lock (MySQL leg, deterministic via timeout 0); leftovers from a simulated dead restore are cleared by the next restore; a foreign (non-owned) collision still refuses.
  7. **CI leg.** New `phpunit-mysql-snapshot` job in `php-tests.yml` (mysql:8.4 service, `pdo_mysql`, no `continue-on-error`, `-c phpunit-mysql.xml.dist tests/Unit/Snapshot`); advisory `phpunit-mysql` job untouched, its 2.11 tag intact; stale globals-consumer sentences corrected in `php-tests.yml` + `phpunit-mysql.xml.dist`. Workflow YAML lints clean (`actionlint` if available); job name chosen once — it becomes a protected-branch required check (Maintainer action).
  8. **Review (Bugbot + Security) + E2E.** Bugbot on the branch diff → fix-loop until clean; Security review (focus: DDL rewriting cannot be steered by dump contents into touching non-owned tables; refusals fail closed; no SQL built from unquoted/unvalidated dump-supplied names; lock name not attacker-influencable; leftover cleanup cannot drop neighbour tables; refusal messages leak no credentials) → fix-loop, restart from Bugbot until both clean in sequence; then final E2E run (3 Playwright `@ci` specs). No production refactor work here beyond fix-loops with their regression tests.

## EXECUTION STATE
<!-- Machine-readable progress index for the Orchestrator/Conductor. Mirrors the Checklist 1:1
     (same numbers + short titles). Size hint per step: S = small/atomic, M = medium,
     L = large or loop-risk, XL = Review (Bugbot + Security) + E2E (mandatory last step, weight 8).
     A step orchestrator flips its box to [x] in the SAME commit as that step's code + tests
     (after full step PASS incl. test-writer when applicable; for XL after reviews + E2E PASS) -->
- [x] Step 1 (M) — Safety gates + prefix invariant at install
- [ ] Step 2 (S) — Reserved names + dumper guard
- [ ] Step 3 (L) — Preflight refusals before the first CREATE
- [ ] Step 4 (L) — Shadow DDL rewriter + fill machinery
- [ ] Step 5 (L) — Cut-over + delete the in-place MySQL apply
- [ ] Step 6 (M) — Serialization + leftover cleanup
- [ ] Step 7 (S) — CI leg (required Snapshot job on MySQL 8.4)
- [ ] Step 8 (XL) — Review (Bugbot + Security) + E2E

## TESTING STRATEGY
- **Per step (production gate):** Refactorer → Verifier → Tester (PHPUnit + PHPStan) — production code must be green before any new tests are written. The default suite runs on SQLite; MySQL-leg tests must skip cleanly there (existing `SnapshotDatabase::namesARealDatabase()` pattern) and never fake a MySQL result.
- **Per step (coverage — inline light):** test-writer → Verifier (test files only) → Tester (PHPUnit + PHPStan). Steps 1–6 change production PHP under `app/` — test-writer applies. **Step 7 is `test-writer: skip`** (workflow YAML + config comments only; Step 8 is the XL review step, outside the pipeline).
- **Batching note:** Steps 4 and 5 carry the behaviour risk and belong in the same review batch even as separate commits (prompt sizing hint).
- **MySQL verification path:** per-step Tester runs are SQLite; MySQL-behaviour tests are enforced by the new `phpunit-mysql-snapshot` CI job at PR time (Finalize). Optional stronger local check for steps 3–6 (decision 12): provision `mysql-server` via sudo in the VM, match the `phpunit-mysql.xml.dist` credentials (root/root@127.0.0.1:3306, db `pagekit_test`), run `./app/vendor/bin/phpunit -c phpunit-mysql.xml.dist tests/Unit/Snapshot`. Expect a Finalize fix-loop if local MySQL was unavailable — plan for it, do not skip the CI gate.
- **Per step notes (minimum coverage, from the prompt):**
  - Step 1 — shape rule table (single home: `TablePrefixTest`); `--db-prefix=` refused, valueless flag falls to `pk_`; blank `PAGEKIT_DB_PREFIX` reads as unset (putenv + tearDown as `EnvConfigLoaderTest` already does); existing empty-prefix installation still boots (connection-level assertion, no validation on the boot path).
  - Step 2 — dump omits `_r_`/`_b_` leftovers on empty-prefix and prefixed installations; empty-prefix dump still succeeds.
  - Step 3 — empty-prefix MySQL restore refused before the dump is opened (test-double platform, runs on SQLite leg); 64-char table case; collision + folding cases; inbound-FK refusal naming the neighbour (MySQL leg); reserved-name-in-dump refusal (both platforms); every refusal leaves tables untouched.
  - Step 4 — rewriter: column value / DEFAULT literal containing a table name is never rewritten; quoted + unquoted DDL; `REFERENCES` inside vs. outside the dump; constraint tokens unique, ≤ 64, deterministic enough to test; fill failure drops all shadows (`finally`), live tables untouched (MySQL leg).
  - Step 5 — the skipped MySQL assertion becomes real: failed apply → live tables unchanged + no `_r_`/`_b_` leftovers; successful restore → backups dropped, dumped-only tables present, non-dumped tables intact; retry-after-failure still passes; SQLite behaviour byte-for-byte unchanged (existing tests stay green unmodified where behaviour did not change).
  - Step 6 — two concurrent restores serialize (second refused, timeout 0, two connections on the MySQL leg); leftover cleanup only touches owned names; drop-failure refusal.
- **Frontend:** no JS/Vue changes planned; if any step touches docs-adjacent frontend files anyway, `pnpm lint` + `pnpm exec prettier --check .` stay blocking (full-tree).
- **Review + E2E (Execute — mandatory last `(XL)` step):** Bugbot → Security (fix-loops until both clean in sequence; security focus list in Checklist Step 8) → Tester `"final E2E run"` (3 Playwright `@ci` specs, chromium-desktop — installer + admin still boot; snapshots panel still restores and purges on the SQLite dev install). Not gated on PR/CI. Details: `orchestrator-v2-step.mdc`.
- **Finalize:** `orchestrator-v2-finalize.mdc` (PR → CI incl. the new `phpunit-mysql-snapshot` job → optional coverage → PR Bugbot/Security → version bump: product runtime changed, so expect a bump unless the skill says NO BUMP).
- **Maintainer action (optional):** add `phpunit-mysql-snapshot` to the protected-branch required status checks (GitHub settings — the workflow file alone cannot mark a check required); one manual end-to-end MySQL restore on a real Docker host (dev stack) before relying on it in production.
- **Deferred / Out-of-Scope (optional):** see ARCHITECT OUTPUT → Deferred (2.7.1b relocation; 2.9 updater consumption; 2.11 full-suite MySQL flip — all already scoped in PHASE_2, no amendments; rejected non-goals listed there). Bridges: none.
