## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.1.2
- **Scope:** `.github/workflows/php-quality.yml` (new), `migration-docs/branches/PROMPT_2_1_2_CI-CD-Quality-Gates.md` (branch-protection documentation note — created at finalize step, not in checklist), `CHANGELOG-NEW.md` (finalize step). No application source changes — CI YAML and docs only.
- **Deferred:**
  - Step 2.2 (CI/CD Pipeline) — E2E (Playwright) jobs, matrix builds across DBs, release automation, Codecov upload integration.
  - Step 2.1.3 (`strict_types` Migration) — CS-Fixer rule `declare_strict_types` stays disabled; CI runs CS-Fixer in dry-run only and must not flip that rule.
  - Step 2.1.4–2.1.6 (PHPStan level raises) — CI uses current Level 5 + baseline; raising the level happens later.
  - Step 2.1.8 (Infection Mutation Testing) — not added to CI here.
  - Step 2.1.9 (Test Coverage Expansion) — 2.1.2 only generates and prints coverage; expanding coverage and enforcing thresholds is 2.1.9.
  - Branch protection rule enforcement — documented for the user to apply manually in GitHub Settings (admin-only); the workflow MUST NOT call any GitHub admin API.
- **Bridges:** None. CI configuration only — no runtime code paths, no temporary shims.
- **Checklist:**
  1. Create `.github/workflows/php-quality.yml` with `name: PHP Quality`, triggers `on: { push: { branches: [main, develop] }, pull_request: { branches: [main, develop] } }`, top-level `permissions: contents: read`, and a default `concurrency` group keyed by workflow + ref to cancel superseded runs on the same PR.
  2. Add **Job 1 `phpunit`**: `runs-on: ubuntu-latest`, matrix `php: ['8.2', '8.3']`. Steps: `actions/checkout@v4` → `shivammathur/setup-php@v2` (with `coverage: xdebug`, `tools: composer:v2`) → cache `app/vendor` via `actions/cache@v4` keyed by `${{ runner.os }}-php-${{ matrix.php }}-composer-${{ hashFiles('composer.lock') }}` with restore-key `${{ runner.os }}-php-${{ matrix.php }}-composer-` → `composer install --no-interaction --prefer-dist --no-progress` (respects `vendor-dir: app/vendor` from `composer.json`) → `mkdir -p tmp/logs tmp/cache tmp/temp tmp/packages storage` (writable dirs required by Pagekit, see AGENTS.md) → `./app/vendor/bin/phpunit --coverage-text --coverage-clover=coverage.xml` → `actions/upload-artifact@v4` uploading `coverage.xml` (only when `matrix.php == '8.3'` to avoid duplicate uploads).
  3. Add **Job 2 `phpstan`**: `runs-on: ubuntu-latest`, single PHP `8.3`. Steps: checkout → setup-php (no xdebug, `coverage: none`) → cache `app/vendor` (same key scheme) → `composer install ...` → `./app/vendor/bin/phpstan analyse --no-progress --error-format=github`. Must respect existing `phpstan.neon` (Level 5) and `phpstan-baseline.neon`; new errors above baseline = fail.
  4. Add **Job 3 `cs-fixer`**: `runs-on: ubuntu-latest`, single PHP `8.3`. Steps: checkout → setup-php (`coverage: none`) → cache `app/vendor` → `composer install ...` → `./app/vendor/bin/php-cs-fixer fix --dry-run --diff --no-interaction --show-progress=none`. Style violations = non-zero exit = fail.
  5. Add **Job 4 `security-audit`**: `runs-on: ubuntu-latest`, single PHP `8.3`. Steps: checkout → setup-php (`coverage: none`) → `composer audit --no-interaction --abandoned=ignore`. Known advisories in `composer.lock` = fail. (No `composer install` needed — `composer audit` operates on the lockfile.)
  6. Validate the workflow YAML locally with `php -r "yaml_parse_file('.github/workflows/php-quality.yml');"` if `ext-yaml` is available, otherwise inspect syntax via `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/php-quality.yml'))"` (Python is preinstalled in the cloud agent VM). Confirm all four jobs (`phpunit`, `phpstan`, `cs-fixer`, `security-audit`) parse and have unique `id`s.
  7. Document branch protection requirements in the branch documentation file (created at the finalize stage, not in this checklist) — required status checks: `phpunit (8.2)`, `phpunit (8.3)`, `phpstan`, `cs-fixer`, `security-audit`; "Require branches to be up to date before merging"; require 1 approval for external contributors. Include explicit note that this MUST be applied manually in GitHub repo settings (admin-only) and is intentionally NOT scripted via API. Also note the coverage baseline (to be captured from the first successful CI run) for Step 2.1.9 reference. **This is a finalize-stage doc step; the Refactorer does not need to act on it during the per-step loop.**

## TESTING STRATEGY
- **Per step:** PHPUnit + PHPStan (mandatory after every checklist step). For YAML-only changes the local Tester run still validates that no PHP regressions slipped in; the actual CI workflow itself can only be exercised once pushed, but local PHPUnit + PHPStan confirms the commands the workflow invokes still pass.
- **Final run (after all steps):** PHPUnit + PHPStan + `php pagekit setup` + `php pagekit list` + Playwright E2E (installation, login, dashboard). After merge the user observes the actual GitHub Actions run on the resulting PR — all 4 jobs (`phpunit` × 2 PHP versions, `phpstan`, `cs-fixer`, `security-audit`) must go green; if any fail, treat as a refactorer failure on Checklist Step 1–5 and loop back through the standard refactorer → verifier → tester cycle.
