## ARCHITECT OUTPUT

- **Current Step (ROADMAP):** 2.1.6 (PHPStan Level 7→8 — Strict Typing). GitHub Issue: #153. Predecessor Step 2.1.5 merged (PR #210). Closes Phase 1 audit Step 1.11 partial (remaining 1.11 findings — full 1.11 closure also requires Step 2.1.10 and Step 2.1.11 to land; the `User::hasAccess()` create_function() portion was closed by Step 2.0.8).
- **Scope:**
  - `phpstan.neon`: bump `level: 7` → `level: 8` (final Checklist Step only; intermediate steps run scoped L8 checks under the still-committed `level: 7` config).
  - `phpstan-baseline.neon`: surgical removals only — **4 stale entries** already unmatched at L7 HEAD (SetupCommand `boot()` method.notFound; AccessListener `hasAccess()` method.notFound; AuthorizationListener `$login` property.notFound; AuthorizationListener `isBlocked()` method.notFound). Never run `--generate-baseline`. Never add entries. Never increment `count: N → N+1`.
  - **Discovery snapshot (run at Step 1, L8):** ~481 L8 errors by identifier: `method.nonObject` 342, `argument.type` 97, `offsetAccess.notFound` 14, `property.nonObject` 10, `return.type` 9, `ignore.unmatched` 4 (stale), `clone.nonObject` 2, `assign.propertyType` 2, `throw.notThrowable` 1.
  - **Test-file errors (~229):** concentrated in `session/Tests/SessionTest.php` (50), `mail/Tests/MessageTest.php` (38), `filesystem/Tests/FilesystemTest.php` (36), `auth/Tests/AuthTest.php` (26), `filesystem/Tests/Adapter/StreamAdapterTest.php` (18), `config/Tests/ConfigTest.php` (12), `mail/Tests/MailerTest.php` (11), `cookie/Tests/CookieJarTest.php` (10), `filter/Tests/PregReplaceTest.php` (9), `mail/Tests/Integration/MailIntegrationTest.php` (5), `filesystem/Tests/LocatorTest.php` (5), `database/Tests/ConnectionTest.php` (4), `filter/Tests/AddRelNofollowTest.php` (2), `application/Tests/DbUtil.php` (2), `application/Tests/FtpTestCase.php` (1). Root cause: properties declared `protected ?Type $property = null;` — PHPStan Level 8 requires method calls on these to be guarded or the type narrowed to non-nullable.
  - **Production-file errors (~252):** by directory — `app/system/modules/` 59, `app/system/src/` 23, `app/modules/markdown/` 27, `app/modules/filesystem/` 22, `app/modules/application/` 22, `app/modules/auth/` 11, `app/console/` 33, `app/modules/database/` 12, `app/modules/kernel/` 11, `app/modules/view/` 5, `app/modules/routing/` 3, `app/modules/filter/` 3, `app/modules/migration/` 3, `app/installer/` 4, `packages/pagekit/blog/` 7. Plus `app/system/src/Controller/ValidatesRequestTrait.php` fires 16 cross-module `method.nonObject` errors in the context of 8 different controller classes (fix: narrow `ConstraintViolationList` iterator return type).
  - **Interface refactors** (from Step 2.1.1 review, deferred to this step): MailerInterface split; FileLocatorAsset static DI → constructor; ResponseListener::$url type-narrow.
  - **Audit findings (Phase 1 review, as listed in PHASE_2_MODERNISING.md §2.1.6):** IntlServiceLocator, ModelServiceLocator (type-narrow only), PackageController God-DI, `#[AllowDynamicProperties]` on Node+Widget, ORM Metadata/Relation/PropertyTrait typing gaps, NodeModelTrait static cache, UrlGeneratorInterface naming collision, GetResponseEvent misleading name.
  - **Audit findings (Step 2.0 closure review):** `DebugStack.php` 42-line deprecated shim (git rm).
  - **Audit findings (Step 2.1.3 review):** `AliasListener.php` dead inline query-string parser block.
  - **Audit findings (Step 2.1.5 review):** `requirements.php` dead PHP 5.x/7.x/APC blocks.
  - **Static service-locator / setter-DI cleanups (codebase TODO inventory §2):** Console Application setter injection (`Command::setContainer()`); blog/UrlResolver static locator (type-narrow + TODO bridge only — full removal blocked by routing factory).

- **Deferred:**
  - **Step 2.1.10 (Entity Presentation Layer / ModelServiceLocator removal, #204)** — `ModelServiceLocator::getUrl()/getUser()/getModule()` type-narrowed from `mixed` to concrete in this step; architectural removal + DTO/presenter refactor deferred.
  - **Step 2.1.11 (EntityManager DI / singleton removal, #205)** — `EntityManager` typing hardened in this step (strict-mode clean, no new `mixed`); `static $instance`, `getInstance()`, Active-Record removal deferred.
  - **blog/UrlResolver static locator full removal** — cannot remove until routing factory (`ParamsResolver` bootstrap) supports DI (Step 1.8 prerequisite). Type-narrow `mixed` properties and add `TEMPORARY BRIDGE` TODO.
  - **Level 9** — explicitly excluded per prompt (too strict for CMS dynamic extension APIs).
  - **Step 2.1.7 (QueryBuilder API standardization)** — no QueryBuilder call-site refactoring in this step.
  - **PSR-11 `Container::get()` / `ContainerInterface::get()`** — keep returning `mixed` per PSR-11 contract; do NOT narrow.
  - **`packages/composer/*`** (Composer-generated) — already excluded from PHPStan; no changes.
  - **PHPStan baseline regeneration** — explicitly forbidden.

- **Bridges:**
  - `// TODO: Must be refactored in Step 2.1.10 (Entity Presentation Layer)` on `ModelServiceLocator` accessor method bodies that return concrete types instead of delegating through a presenter.
  - `// TODO: Must be refactored in Step 2.1.11 (EntityManager DI)` on `EntityManager::$instance` static property and `::getInstance()` method.
  - `// TODO: TEMPORARY BRIDGE - To be removed after routing factory supports DI (prerequisite Step 1.8)` on `blog/UrlResolver` static `$cache` and `$module` properties.

- **Checklist:**

  1. **Discovery snapshot + stale baseline pre-clean.** Run `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M --level=8 --error-format=raw 2>&1 | tee tmp/phpstan_l8_discovery.txt` (gitignored under `tmp/`, never committed). Categorize the snapshot by identifier and by directory (see scope section above for the pre-computed breakdown — confirm it matches current HEAD). Pre-clean the 4 stale baseline entries that are already unmatched on `develop` HEAD (run `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` at configured Level 7 and surgically remove entries flagged `Ignored error pattern ... was not matched`): (a) SetupCommand `boot()` `method.notFound`; (b) AccessListener `hasAccess()` `method.notFound`; (c) AuthorizationListener `$login` `property.notFound`; (d) AuthorizationListener `isBlocked()` `method.notFound`. Do NOT bump the level. Do NOT edit source files. Commit (only if non-empty): `chore(phpstan): pre-clean stale baseline entries before L8 sweep`. If the L7 run shows no unmatched entries, this step ships an empty diff and the Orchestrator skips the commit — Refactorer emits `Step 1 done. Files: [].` in that case.

  2. **Install `phpstan/phpstan-phpunit` + test property declaration sweep.** Add `phpstan/phpstan-phpunit: ^2.0` to `require-dev` in `composer.json` and run `composer require --dev phpstan/phpstan-phpunit --no-interaction` (vendor-dir is `app/vendor/`). Add `- app/vendor/phpstan/phpstan-phpunit/extension.neon` to the `includes:` list in `phpstan.neon` (alongside the existing doctrine and symfony extensions). Then sweep all 15 test files with L8 errors (listed in the Scope section above) and change every `protected ?Type $property = null;` pattern to `private Type $property;` (no `= null` initializer) and remove or refactor any `$this->property = null;` assignments in `tearDown()` — replace with `unset($this->property)` if the tearDown cleanup is genuinely needed, or simply drop the null assignment if tearDown only cleans up for GC. For test files whose tearDown guards with `if ($this->property &&` before calling a method on it, drop the null guard (the property is no longer nullable). Per-step Tester runs scoped L8 on all Test directories listed in Scope and asserts 0 errors. Also runs PHPUnit (full suite must remain green — the property-type change is PHP-valid since setUp() always runs first). Commit: `refactor(types): add phpstan-phpunit extension + narrow nullable test properties to non-nullable`.

  3. **Dead code deletions — DebugStack shim + AliasListener dead parser + requirements.php cleanup.** Three independent deletions, one commit:
     - `git rm app/modules/database/src/Logging/DebugStack.php` — 42-line `@deprecated since DBAL 3.x migration` shim with zero consumers in `app/` / `packages/` source (verified via `rg -n "DebugStack" app/ packages/` which returns only migration-doc references). The replacement (`app/modules/debug/src/Middleware/DebugMiddleware.php`) is already wired and used. Per Rule 4 (Delete Over Wrap).
     - `app/modules/routing/src/Event/AliasListener.php`: delete the `if (false !== ($queryPos = strpos($aliasName, '?')))` block (lines ~50–57, tagged `// TODO: Must be refactored in Step 2.1.6`) and the dependent `$name == strtok($alias->getName(), '?')` clause in the `array_filter` (line ~39). Zero callers use `?param=value` suffix in alias names; the `$defaults` parameter of `Routes::alias()` fully replaced this API. Do NOT touch `Router::generate('route?foo=bar', [...])` — separate mechanism. Remove the `// TODO` tag.
     - `app/installer/requirements.php` (`PagekitRequirements`): physically delete the PHP 5.6/7.0 `display_startup_errors` / `always_populate_raw_post_data` block (~lines 456–462); delete the entire APC (not APCu) requirement block (`apc_store` / `apc.enabled`, referencing "PHP 5.4"); update or delete the `$accelerator` recommendation that names APC, eAccelerator, XCache — only **OPcache** is relevant on PHP 8.2+; remove abandoned `detect_unicode`, `magic_quotes_gpc`, `register_globals` ini-checks (removed in PHP 5.4, only pass today via `approveCfgAbsence`); update APCu minimum version text to reflect PHP 8.2 reality (APCu 5.1.x); drop or replace `utf8_decode()` recommendation (deprecated PHP 8.2). Per Rule 4. Commit: `refactor(cleanup): delete DebugStack shim, AliasListener dead parser, requirements.php dead PHP 5.x/7.x branches`.

  4. **MailerInterface split + ImpersonatePlugin update.** Split `app/system/modules/mail/src/MailerInterface.php` into two focused interfaces:
     - **`MailerInterface`** (in same file): `send(Email $message): bool`, `create(): Message`, `registerPlugin(MailPluginInterface $plugin): void` — the Mailer's own contract.
     - **New file `app/system/modules/mail/src/MailPluginInterface.php`**: `beforeSend(Email $email): void`, `afterSend(Email $email): void` — the plugin hook contract.
     Update `app/system/modules/mail/src/Mailer.php`:
     - Implements `MailerInterface` only (no longer needs `beforeSend()`/`afterSend()` no-op stubs — remove them).
     - `registerPlugin(MailPluginInterface $plugin): void` — update parameter type.
     Update `app/system/modules/mail/src/ImpersonatePlugin.php`: implements `MailPluginInterface` instead of `MailerInterface`.
     Verify `MessageInterface.php` and `Message.php` are unaffected.
     Remove the `// TODO: Must be refactored in Step 2.1.6` comment that was placed in Step 2.1.4 on the no-op stubs.
     Scoped L8 check: `./app/vendor/bin/phpstan analyse --level=8 --no-progress --memory-limit=512M app/system/modules/mail` must exit 0 for the mail module source (mail Tests are already 0 from Step 2). Commit: `refactor(mail): split MailerInterface + MailPluginInterface (remove no-op beforeSend/afterSend from Mailer)`.

  5. **FileLocatorAsset constructor injection + ResponseListener::$url type-narrow.** Two targeted DI fixes:
     - `app/modules/view/src/Asset/FileLocatorAsset.php`: replace `static mixed $file` and `static mixed $locator` set via `setServices()` with constructor injection. Type `$file` as `\Pagekit\Filesystem\Filesystem`, `$locator` as `\Pagekit\Filesystem\ResourceLocator`. Update `app/modules/view/index.php` (asset factory / `'asset'` service registration) to pass the dependencies at construction time instead of calling `setServices()`. Verify scoped L8 on `app/modules/view` is 0.
     - `app/modules/application/src/Event/ResponseListener.php`: narrow `mixed $url` property. Inspect the actual value injected at service registration (it is callable — a closure or the URL generator invokable) and type it as `callable` or the correct interface. Remove the `// TODO: Must be refactored in Step 2.1.6` comment. Scoped L8 on `app/modules/application/src/Event`.
     Commit: `refactor(di): FileLocatorAsset constructor injection + ResponseListener callable type`.

  6. **IntlServiceLocator → proper DI chain.** `app/system/modules/intl/src/IntlServiceLocator.php` currently holds `static ContainerInterface $container` set via a static `setServices()` call. Replace with proper DI:
     - Remove the static `$container` and `setServices()` method.
     - Inject the required services (the `intl` module translator / locale lookup) directly via constructor or factory.
     - Update the consumer(s) of `IntlServiceLocator` in `app/system/modules/intl/index.php` and any call sites.
     Scoped L8 check on `app/system/modules/intl`. Commit: `refactor(di): replace IntlServiceLocator static container with constructor injection`.

  7. **ModelServiceLocator type-narrow only (NOT architectural removal).** `app/system/modules/*/src/ModelServiceLocator.php` (or wherever the locator lives — verify path via `rg -rn "ModelServiceLocator" app/`): narrow the return types of `getUrl()`, `getUser()`, `getModule()` from `mixed` to their concrete types (e.g., `UrlGeneratorInterface`, `UserInterface`, `ModuleInterface` or the concrete Pagekit classes). Do NOT remove the static service locator pattern, do NOT change its injection mechanism — that is Step 2.1.10's job. Add `// TODO: Must be refactored in Step 2.1.10 (Entity Presentation Layer)` above the class or on the accessor methods. Scoped L8 check on all consumers. Commit: `refactor(types): ModelServiceLocator mixed → concrete return types (type-narrow only, Step 2.1.10 deferred)`.

  8. **`#[AllowDynamicProperties]` removal on Node + Widget + NodeModelTrait static cache.** Audit finding from Phase 1:
     - `app/system/modules/site/src/Model/Node.php`: remove `#[AllowDynamicProperties]` attribute. Identify every dynamic property usage (properties assigned outside declared class properties) and add explicit typed property declarations for each. Run `rg -n "\\\$node->" app/system/ packages/` to find consumers — confirm no consumer sets a non-declared property dynamically.
     - `app/system/modules/widget/src/Model/Widget.php`: same treatment.
     - `app/system/modules/site/src/Model/NodeModelTrait.php` (`static ?array $nodes`): the static request-scoped cache array is a global mutable state anti-pattern. Replace with a PSR-6 `CacheItemPoolInterface` passed via constructor or via the container — use a simple in-memory array-based approach wrapping the injected cache pool with a `getItem()`/`save()` call. If injecting the pool is not feasible in the current architecture, use `static ?array $nodes` with a `// TODO: BACKWARD COMPATIBILITY - Must be refactored later` tag documenting the dependency on static state. Scoped L8 check on `app/system/modules/site` and `app/system/modules/widget`. Commit: `refactor(types): remove AllowDynamicProperties from Node/Widget + NodeModelTrait cache annotation`.

  9. **UrlGeneratorInterface naming collision rename + GetResponseEvent rename.** Two interface renames to eliminate namespace collisions:
     - `app/modules/routing/src/UrlGeneratorInterface.php` — rename to `LinkReferenceType` (or whichever name the Architect selects that clearly distinguishes it from Symfony's `Symfony\Component\Routing\Generator\UrlGeneratorInterface`). Move the `LINK_URL` constant to the renamed interface. Update all consumers via `rg -rn "UrlGeneratorInterface" app/ packages/` (excluding Symfony vendor); update any `use` statements, `implements` clauses, `@param`/`@return` docblocks. Do NOT rename Symfony's interface.
     - `app/modules/auth/src/Event/GetResponseEvent.php` (or wherever this lives — verify via `rg -rn "GetResponseEvent" app/`) — rename to `AuthResponseEvent`. Update all consumers.
     Scoped L8 check on `app/modules/routing` and `app/modules/auth`. Commit: `refactor(names): rename UrlGeneratorInterface→LinkReferenceType and GetResponseEvent→AuthResponseEvent`.

  10. **EntityManager singleton typing hardening + ORM Metadata/Relation/PropertyTrait typing gaps.** Scope in 2.1.6: harden typing only, do NOT remove the singleton pattern (Step 2.1.11). Files: `app/modules/database/src/ORM/EntityManager.php`, `app/modules/database/src/ORM/Metadata.php` (and sub-files), `app/modules/database/src/ORM/Relation/*.php`, `app/modules/database/src/ORM/PropertyTrait.php` (if not already done in Step 2.1.5). Tasks:
      - `EntityManager.php`: for every method with `mixed` parameter or return type that can be narrowed, narrow it. Add `// TODO: Must be refactored in Step 2.1.11 (EntityManager DI)` above `static ?self $instance` and `getInstance()`. The singleton itself (the `static $instance` set in `__construct()` and the `getInstance()` static method) must NOT be removed — only typed correctly (`private static ?self $instance = null`).
      - `ORM/Metadata.php` + sub-files: add missing parameter types and return types throughout; use `array<string, mixed>` where the structure is unknown but the key is always a string.
      - `ORM/Relation/*.php` (BelongsTo, HasMany, HasOne, ManyToMany): add missing parameter types and return types.
      - `ORM/PropertyTrait.php`: if any missing types remain from Step 2.1.5, complete them here.
      Scoped L8 check on `app/modules/database`. Commit: `refactor(types): EntityManager/ORM typing hardening + Metadata/Relation missing types (Step 2.1.11 deferred)`.

  11. **Console Application setter-DI → constructor DI / command factory.** `app/modules/application/src/Application/Console/Application.php` (~line 51) currently calls `$command->setContainer($this->container)` inside `add()` (setter injection). Steps:
      - Introduce a `ConsoleCommandFactory` or use constructor injection: define a `ContainerAwareCommand` interface (or reuse an existing one) with a constructor parameter for the container or specific services.
      - Update the `add()` override to NOT call `setContainer()` — instead, rely on the command receiving its services via constructor DI.
      - Update `app/modules/application/src/Application/Console/Command.php` (the base command class): the 12 `method.nonObject` errors on `$this->input`, `$this->output`, `$this->helperSet` are because these nullable Symfony Console properties are called without null guards in convenience methods. Since Symfony's parent class types these as `?InputInterface` / `?OutputInterface` / `?HelperSet`, the fix is to add early-return guards (`if ($this->input === null) { throw new \LogicException('...'); }`) at the start of each affected convenience method, OR to use Symfony's `$this->getHelper()` / `$this->getDefinition()` methods which already guard internally. Do NOT override the parent's property types to non-nullable (breaks Symfony contract).
      - Update all console commands under `app/console/src/Commands/` that currently use `$this->container` via setter injection to instead receive their dependencies via constructor.
      Scoped L8 check on `app/modules/application/src/Application/Console` and `app/console/src/Commands`. Commit: `refactor(di): console commands setter DI → constructor DI + Command null-guard convenience methods`.

  12. **PackageController God-DI → inject specific services.** `app/installer/src/Controller/PackageController.php` uses `ContainerInterface $app` as a God-DI object. Identify every `$this->app->get('service')` call and replace with a specific injected constructor parameter. Derive the service types from `app/installer/index.php`. Scoped L8 check on `app/installer/src`. Commit: `refactor(di): PackageController inject specific services (remove God-DI ContainerInterface $app)`.

  13. **blog/UrlResolver static setter type-narrow + TODO bridge.** `packages/pagekit/blog/src/UrlResolver.php` (~line 25) uses `private static` cache/module setters (`setCache()`, `setModule()` or similar). These cannot be fully removed in this step because the `ParamsResolver` bootstrap instantiates resolvers via `new $class` without DI. Tasks:
      - Narrow all `private static mixed $cache` / `private static mixed $module` to their concrete types (`\Psr\Cache\CacheItemPoolInterface`, `\Pagekit\Module\Module` or equivalent). Remove any `mixed` from property declarations.
      - Fix the 6 production L8 errors in `UrlResolver.php` (all `method.nonObject` on `DateTime|null` — add null guard on the `$date` variable before calling `format()`, as `getDate()` on a post can legitimately return null for undated posts).
      - Fix the 1 L8 error in `packages/pagekit/blog/src/Model/PostModelTrait.php` (`preg_replace` with `string|null` subject — narrow the subject to `string` with a null-coalesce or early return).
      - Add `// TODO: TEMPORARY BRIDGE - To be removed after routing factory supports DI (prerequisite Step 1.8)` above the static setter methods.
      Scoped L8 check on `packages/pagekit/blog`. Commit: `refactor(types): blog UrlResolver type-narrow + null guards + TEMPORARY BRIDGE TODO`.

  14. **Production null-safety sweep — `app/modules/*`.** For every remaining L8 error in `app/modules/` not already addressed by Steps 3–11:
      - `app/modules/markdown/src/Lexer/BlockLexer.php` (11 errors): `argument.type` passing `string|null` to string-only functions (`preg_replace`, `strlen`, `strpos`). Add null-coalesce guards (`$str ?? ''`) at each call site where the value can legitimately be null, OR narrow the property/variable declaration to `string` if it's always set. Follow the same L7 decision tree: if the value can actually be null at runtime → add null guard; if the type declaration is wrong → narrow the type.
      - `app/modules/markdown/src/Parser.php` (10 errors): same pattern.
      - `app/modules/markdown/src/Lexer/InlineLexer.php` (6 errors): same pattern.
      - `app/modules/filesystem/src/StreamWrapper.php` (17 errors): `$this->filesystem` typed as `?Filesystem` — calling `getPath()` on it. Since `StreamWrapper` is registered with a `Filesystem` instance via `setFilesystem()` before use (PHP stream-wrapper protocol), narrow to non-nullable and assert/throw if null at the start of each `stream_*` method.
      - `app/modules/application/src/Application/UrlProvider.php` (9 errors): `$this->request` typed `?Request` — calling methods on it. The request is a nullable dependency (before the first HTTP request in CLI mode). Add an early-return `?string` guard pattern: if `$this->request === null` return a sensible default (empty string or throw).
      - `app/modules/auth/src/Handler/DatabaseHandler.php` (11 errors): `$this->config` typed `array<string, mixed>|null` — accessing keys. Add null-check on config at the top of each method. `$this->request` nullable — same pattern as UrlProvider.
      - `app/modules/kernel/src/Event/ExceptionListener.php` (6 errors): `$this->exception` is typed `?Exception` — narrow to `Exception` (non-nullable) since the exception listener always receives an exception; update constructor/setter accordingly.
      - `app/modules/view/src/Asset/AssetManager.php` (5 errors): narrow nullable properties.
      - `app/modules/routing/src/Router.php` (3 errors): narrow nullable or add guards.
      - `app/modules/filter/src/SlugifyFilter.php` (2 errors) + `FilterManager.php` (1 error): narrow.
      - `app/modules/database/src/ORM/MetadataManager.php` (3 errors): narrow or guard.
      - `app/modules/migration/src/` (3 errors): narrow or guard.
      - `app/modules/debug/src/` (1 error): narrow or guard.
      After all fixes, run scoped L8 on the full `app/modules/` tree: `./app/vendor/bin/phpstan analyse --level=8 --no-progress --memory-limit=512M app/modules` must exit 0 (modulo the 2 `trait.unused` standing exemption from Steps 2.1.4/2.1.5 if still applicable). Commit: `refactor(types): null-safety sweep for app/modules production code (L8 strict)`.

  15. **Production null-safety sweep — `app/system/*` + `app/installer/` + `app/console/`.** For every remaining L8 error not addressed by Steps 4–12:
      - `app/system/src/Controller/ValidatesRequestTrait.php` (16 cross-module `method.nonObject`): the `ConstraintViolationList` iterator returns `ConstraintViolationInterface|null` per the `\Iterator` contract but `ConstraintViolationList::offsetGet()` always returns `ConstraintViolationInterface`. Fix: add `assert($violation instanceof ConstraintViolationInterface)` or use a typed foreach. Since the prompt bans `assert()` as a type override, instead iterate with an explicit `foreach ($violations as $violation)` and verify the loop variable's type annotation is `ConstraintViolationInterface` — PHPStan should infer this from the `ConstraintViolationList` iteration.
      - `app/system/src/SystemModule.php` (4 errors): `$this->app` typed `?Application` — narrow or add null guard.
      - `app/system/src/Controller/AdminController.php` (2 errors) + `MigrationController.php` (1 error): narrow nullable dependencies.
      - `app/system/modules/mail/src/Mailer.php` (4 errors): fix the remaining L8 errors (likely `$this->transport` or similar nullable property).
      - `app/system/modules/user/src/Controller/ProfileController.php` (8 errors): fix method.nonObject.
      - `app/system/modules/site/src/Event/NodesListener.php` (6 errors): fix.
      - `app/system/modules/cache/src/CacheModule.php` (5 errors): fix.
      - `app/system/modules/user/src/Event/AuthorizationListener.php` (4 errors), `AccessListener.php` (3 errors): fix (note: the stale baseline entries for these are removed in Step 1; the 4+3 real errors are separate production issues).
      - `app/system/modules/site/src/SiteModule.php` (3 errors), `comment/CommentPlugin.php` (3 errors): fix.
      - `app/system/modules/widget/src/PositionHelper.php` (2 errors), `user/UserModule.php` (2 errors), `user/Controller/RegistrationController.php` (2 errors), `settings/Controller/SettingsController.php` (2 errors), `dashboard/DashboardModule.php` (2 errors): fix.
      - `app/system/modules/user/src/Model/User.php` (1 error), `site/MenuHelper.php` (1 error), `site/Controller/PageController.php` (1 error), `user/Controller/ResetPasswordController.php` (1 error): fix.
      - `app/console/src/Commands/ExtensionTranslateCommand.php` (9 errors): all likely `argument.type` / `method.nonObject` on optional command inputs. Fix by narrowing or guarding.
      - `app/console/src/Commands/SetupCommand.php` (5 real errors), `MigrationCommand.php` (5), `BuildCommand.php` (3), `TranslationFetchCommand.php` (2), `ArchiveCommand.php` (2), `StartCommand.php` (1), `SelfupdateCommand.php` (1), `Migration/StatusCommand.php` (1), `Migration/RollbackCommand.php` (1), `Migration/MigrateRunCommand.php` (1), `Migration/GenerateCommand.php` (1), `ClearCacheCommand.php` (1): fix all — primarily `argument.type` on nullable option/argument values that are declared required in `configure()` (safe to narrow with `(string) $value` or `$input->getArgument('foo') ?? throw new ...`).
      - `app/installer/src/` (4 errors): fix.
      After all fixes, run scoped L8 on `app/system`, `app/installer`, and `app/console`: each must exit 0. Commit: `refactor(types): null-safety sweep for app/system, app/installer, app/console (L8 strict)`.

  16. **Bump PHPStan to Level 8 + final baseline cleanup + `mixed` usage audit.**
      - Edit `phpstan.neon`: `level: 7` → `level: 8`. No other config edits.
      - Run `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M`. For every `Ignored error pattern ... was not matched in reported errors` warning, surgically remove that single entry from `phpstan-baseline.neon` (the `-` block including `message`/`identifier`/`count`/`path` keys). Do NOT run `--generate-baseline`. Final state: `git diff phpstan-baseline.neon` shows removals only (or zero changes); `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` exits 0 at Level 8.
      - **`mixed` usage audit (Verifier-led):** Run `rg -rn ": mixed|mixed \$|@return mixed|@param mixed" app/ packages/ | grep -v "vendor\|node_modules\|composer" | grep -v "// TODO\|// Justified"` and review every remaining `mixed` usage. For each match confirm it falls into one of: (a) PSR-11 `Container::get()` / `ContainerInterface::get()` — kept per spec; (b) event/config/plugin dynamic data — documented with `/** @return mixed Genuinely unknown type — ... */` PHPDoc; (c) template rendering view variables — documented. Any `mixed` that does NOT fall into a documented category must be narrowed before this step can be committed.
      - **Final sanity check:** `rg -rn "#\[AllowDynamicProperties\]" app/ packages/ | grep -v vendor | wc -l` returns `0`.
      Commit: `chore(phpstan): raise level to 8 (strict typing) + final baseline cleanup`.

## TESTING STRATEGY

- **Per step (Tester subagent):** PHPUnit + PHPStan (mandatory after every Checklist Step).
  - **Steps 1–15** run PHPStan at the **config-defined level (7)** — exit 0 required, plus `git diff --quiet phpstan-baseline.neon` (no baseline additions and no `count: N → N+1` increments). Surgical removals are permitted only in: Step 1 (4 stale entries), and on any step where a source fix directly renders a pre-existing baseline entry unmatched (same surgical-removal precedent as all prior steps). The Tester must confirm: `git diff phpstan-baseline.neon` shows only removals (or is empty) after each step.
  - **Steps 2–15** additionally run a **scoped Level-8 check** on the step's target directory(s): `./app/vendor/bin/phpstan analyse --level=8 --no-progress --memory-limit=512M <step's directory(s)>` and assert 0 errors in those directories. The Tester must NOT run a global L8 sweep on intermediate steps (that fails until Step 16). Document the scoped-paths command in the Tester's per-step run log.
  - **Step 2 `trait.unused` exemption (standing convention from Steps 2.1.4/2.1.5)** — if any cross-module trait files in the scoped directories produce `trait.unused` scoping artifacts, treat them per the standing convention: exclude from the per-step gate, rely on Step 16's global L8 check. No `phpstan.neon` changes to suppress them.
  - **Step 16** runs the **global Level-8 check**: `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M` (now under `level: 8`) and asserts exit 0. Tester also runs the `mixed` usage audit and `AllowDynamicProperties` sanity check described in Checklist Step 16.
  - **PHPUnit** runs unconditionally on every step; the suite must stay green throughout.
- **Final run (after Early Push, Tester subagent):** wait on the four PHP Quality CI jobs (`phpunit (8.2)`, `phpunit (8.3)`, `phpstan`, `cs-fixer`, `security-audit`) via `gh run watch` and run the 3 Playwright E2E specs locally **in parallel**; both must pass. See `.cursor/agents/tester.md` § End-of-ticket tests for the exact commands and `.cursor/rules/orchestrator-subagent-workflow.mdc` § Final Test for the workflow position.
