## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.0 (Foundation Consolidation — Closure & Gap Audit, parent of 2.0.0–2.0.8; gates Step 2.1.2)
- **Scope:**
  - **Audit machinery only** — no PHP, JS, LESS, Vue, or composer changes. The only code touched is what `php pagekit setup` / cache regeneration produces.
  - Read-only verification of merged sub-steps **2.0.0, 2.0.1 (incl. 2.0.1a–e), 2.0.2, 2.0.3, 2.0.4, 2.0.5, 2.0.6, 2.0.7, 2.0.8** against `develop` HEAD.
  - **New audit report:** `migration-docs/audits/2026/04/AUDIT_REPORT_STEP_2.0_FOUNDATION_CLOSURE_2026-04-28.md` (sections per §4 of the prompt).
  - **Possible new ROADMAP rows / PHASE_2 sections / agent-prompt skeletons** for any newly identified Foundation-Consolidation gaps that warrant their own sub-step (numbered ≥ 2.0.9; never reuse).
  - **Possible PHASE_2 updates to existing future steps** (most likely 2.1.6, 2.1.9, 2.5) when a gap is best routed there — append to that step's `**Audit findings (Phase 1 review):**` block.
  - **GitHub issues** for each new sub-step, linked as sub-issues of `#181` via the `github-issue-creator` skill.
  - **Branch doc:** `migration-docs/branches/step-2-0-foundation-closure.md` (verdict + new sub-step list).
  - **CHANGELOG-NEW.md:** add `## Pagekit X.Y.Z - Foundation Consolidation Closure (2026-04-28)` section (orchestrator finalize step bumps the patch version).
  - **ROADMAP closure:** if no must-fix-before-2.1 gaps → flip 2.0 to `✅ / 🛡️` and advance `Current Step` pointer to `2.1.2` (2.1.1 is already done). If blocking gaps exist → keep 2.0 `⏳ / ⏳`, point `Current Step` at the first new sub-step (e.g. `2.0.9`).
  - Files allowed to change in this PR: `.cursor/ROADMAP.md`, `migration-docs/TODO/PHASE_2_MODERNISING.md`, `migration-docs/TODO/agent_prompts/Step-2_0-Foundation-Consolidation/PROMPT_2_0_*.md` (new skeletons only), `migration-docs/audits/2026/04/AUDIT_REPORT_STEP_2.0_FOUNDATION_CLOSURE_2026-04-28.md` (new), `migration-docs/branches/step-2-0-foundation-closure.md` (new), `CHANGELOG-NEW.md`, `app/system/config.php` (version bump in finalize), and PHPStan/PHPUnit/Pagekit cache artefacts regenerated by `php pagekit setup`.
- **Deferred:**
  - **Step 2.1.x (Static Analysis & Code Quality)** — strict_types, PHPStan level 6/7/8, return-type / null-safety / strict-typing fixes. Out of scope per §10.
  - **Step 2.1.6 (PHPStan Level 7→8)** — `EntityManager` singleton, `ModelServiceLocator`, `IntlServiceLocator`, ORM `Metadata`/`Relation`/`PropertyTrait` typing, `#[AllowDynamicProperties]`, `GetResponseEvent` rename. The audit only **routes** these into 2.1.6's PHASE_2 "Audit findings" block; it does not fix them.
  - **Step 2.1.9 (Test Coverage Expansion)** — `MenuApiController` manual validation. Routed only.
  - **Step 2.2 (CI/CD)** — pipeline / GitHub Actions / quality gates.
  - **Step 2.3 (Docker prod)**, **2.4 (Build tools)**, **3.x (Vue 3)**, **4.x (security/API v2)** — out of scope.
  - **Step 2.5 (Extension Safety System)** — `evaluateBooleanExpression()` extraction into `PermissionExpressionEvaluator` (deferred from 2.0.8). Routed only — confirm 2.5's PHASE_2 section mentions it, otherwise add it there.
  - **Re-execution of any 2.0.x sub-step** — they are merged. If a sub-step delivered something incorrect, the fix is a *new* sub-step, never an amend.
  - **Code refactors of any kind** — even if a gap is "trivial". Code-level fixes for newly identified gaps belong in their own future PRs.
- **Bridges:** None.
  - Audit-only work; no PHP / JS / LESS source files are modified, so no `// TODO: TEMPORARY BRIDGE - To be removed in Step X.Y` markers are created.
  - If, while auditing, the Refactorer encounters a stale `// TODO: Refactor in Phase 3 (Vue Migration)` or other Rule-5 marker whose target code path has already been deleted, it MUST be recorded as a Gap-List row (Rule-5 violation: debt without owner) and routed — never silently rewritten in this PR.
  - Any newly identified must-fix-now hotfix (e.g. another fatal-on-PHP-8 surprise like 2.0.8) **aborts** this closure PR; it gets its own hotfix sub-step PR first, then the audit resumes. This is documented in §9 of the task prompt.
- **Checklist:**
  1. **Pre-flight baseline.** On a clean `develop` HEAD, run and capture the output of `./app/vendor/bin/phpunit`, `./app/vendor/bin/phpstan analyse --no-progress --memory-limit=512M`, `php pagekit list`, `php pagekit setup`. All four MUST be green; any red → STOP and escalate to Architect (it's itself a 2.0 closure blocker). Save outputs as evidence for §4.7 of the audit report. Prepare the empty audit-report file at `migration-docs/audits/2026/04/AUDIT_REPORT_STEP_2.0_FOUNDATION_CLOSURE_2026-04-28.md` using the §4 section skeleton (Executive Summary placeholder, Closure Verdict table with all 10 rows: 2.0.0, 2.0.1, 2.0.1a, 2.0.1b, 2.0.1c, 2.0.1d, 2.0.1e, 2.0.2 … 2.0.8 — collapse 2.0.1a–e into one row labelled "2.0.1a–e" if desired, but list them individually in the per-sub-step blocks).
  2. **Audit Sub-step 2.0.0 (Controller Attributes).** Read its ROADMAP row, PHASE_2 section, agent prompt, branch doc. Verify on `develop`: zero remaining Doctrine `@Route` annotations on controllers (`rg -n "@Route\(" app/ packages/ --glob "*.php" --glob "!*Test.php"`); attribute-based routing alive (`rg -n "#\[Route\(" app/ packages/ --glob "*.php"`). Verify Phase 1 audit closures claimed (none directly per ROADMAP — 2.0.0 has no `Closes Phase 1 audit:` line). No-Mercy spot-check: zero `@deprecated` markers introduced in controller layer by this step. Record evidence block (≤ 20 lines) in audit report.
  3. **Audit Sub-step 2.0.1 + 2.0.1a–e (PSR-11 Container Modernization).** Verify on `develop`: zero references to `Psr11Adapter` / `StaticTrait` / `ArrayAccess` on `Container` (`rg -n "Psr11Adapter|StaticTrait|class_alias.*Container" app/ packages/ --glob "*.php"`); `Container` natively `implements ContainerInterface` from `Psr\Container`. Verify `Closes Phase 1 audit: Step 1.6` claim — confirm ROADMAP row 1.6 = 🛡️ and prior audit `migration-docs/audits/2026/03/AUDIT_REPORT_STEP_2.0-2.0.2_2026-03-27.md` corroborates. Record evidence block.
  4. **Audit Sub-step 2.0.2 (Validator-Translator Integration).** Verify on `develop`: `validators.php` exists per locale (system + blog), `validation.php` deleted; `ValidatorServiceProvider` calls `setTranslator()` + `setTranslationDomain('validators')`. Confirm prior audit cell flipped 1.13 → 🛡️. Confirm `MenuApiController` manual-validation finding is **already** listed under 2.1.9 PHASE_2 (route check, no action). Record evidence block.
  5. **Audit Sub-step 2.0.3 (Cache API Full Modernization).** Verify on `develop`: zero `CacheInterface` (Pagekit's own) / `Psr6Adapter` references (`rg -n "Pagekit\\\\Cache\\\\CacheInterface|Psr6Adapter" app/ packages/ --glob "*.php"`); zero `fetch()` / `flushAll()` calls on cache pools; consumers (`LoginAttemptListener`, `UrlResolver`, `RouteListener`, `blog/scripts.php`) use PSR-6 API. Verify `composer.lock` + `yarn.lock` committed (lockfile-versioning sub-task that shipped early in 2.0.3 PR #187). Confirm 1.10 stays 🛡️. Record evidence block.
  6. **Audit Sub-step 2.0.4 (Package/Migration System Redesign).** Verify on `develop`: `DatabaseHandler::createTable()` deleted (`rg -n "function createTable" app/modules/auth/`); blog migration renamed to timestamp format (`rg -n "Version001_CreateBlogTables|Version[0-9]{14}.*Blog" app/`); `MigrationServiceTest` un-skipped (no `markTestSkipped` in that file); `MigrationService::getConfigPath()` deleted; login check + update wizard execute Doctrine Migrations before `scripts->update()`. Confirm 1.12 stays 🛡️. Record evidence block — flag any audit-findings sub-bullet still untouched as a Gap.
  7. **Audit Sub-step 2.0.5 (Composer & Autoload Hygiene).** Verify `composer.json` on `develop`: dead PSR-4 mappings `Pagekit\Theme\` and `Pagekit\Package\` removed; unused deps removed (`symfony/framework-bundle`, `symfony/twig-bridge`, `symfony/yaml`, `symfony/process`, `paragonie/sodium_compat`, `doctrine/data-fixtures`); `symfony/validator` aligned to `^6.4`; `paragonie/random-lib` resolved (replaced or loosened). Verify `composer validate` clean and `composer.lock` consistent. Confirm 1.4 → 🛡️. Record evidence block.
  8. **Audit Sub-step 2.0.6 (Test Infrastructure Cleanup).** Verify on `develop`: zero module-level `phpunit.xml.dist` files (`rg --files app/modules/ -g "phpunit.xml.dist"`); `tests/Unit` casing fixed; `@dataProvider` / `@group` migrated to `#[DataProvider]` / `#[Group]`; `Doctrine\Common\Cache\ArrayCache` import removed from `ConfigManagerTest`; `RoutesLoader::addController()` no longer silently swallows `InvalidArgumentException`. Confirm 1.2 + 1.8 → 🛡️. Record evidence block.
  9. **Audit Sub-step 2.0.7 (Event Dispatcher Bridge Removal).** Verify on `develop`: `rg -n "SymfonyEventDispatcherBridge|symfony\\.event_dispatcher|EventDispatcherCompatibilityTest" app/ packages/` returns **zero** hits; PHPStan baseline does not list any of those identifiers. Confirm 1.7 → 🛡️ (1.9 already 🛡️). Cross-check `GetResponseEvent` rename deferral is tracked in 2.1.6 PHASE_2 — if not, add it there as a routed gap. Record evidence block.
  10. **Audit Sub-step 2.0.8 (`User::hasAccess()` Hotfix).** Verify on `develop`: zero `create_function` references in `app/` and `packages/` (`rg -n "create_function" app/ packages/ --glob "*.php"`); PHPStan baseline entry `function.notFound: create_function` removed. Verify the parser handles `&&` / `||` / `!` AND single-character `&` / `|`. Confirm 1.11 still ⚠️ (only partial closure — `EntityManager` singleton etc. carry to 2.1.6). Cross-check the `evaluateBooleanExpression` → `PermissionExpressionEvaluator` deferral is referenced in 2.5's PHASE_2 section; if missing, add it there as a routed gap. Record evidence block.
  11. **Cross-cutting ripgrep sweep.** Run the 4 ripgrep sweeps from §3.3 of the prompt verbatim:
      - `rg -n "Bridge|Adapter|Compat|Shim|Legacy|Wrapper" app/ packages/ --glob "*.php" --glob "!*Test.php"`
      - `rg -n "@deprecated" app/ packages/ --glob "*.php"`
      - `rg -n "class_alias" app/ packages/ --glob "*.php"`
      - `rg -n "TEMPORARY BRIDGE|AUDIT FIX|BACKWARD COMPATIBILITY|Must be refactored later" app/ packages/ --glob "*.php" --glob "*.js" --glob "*.vue" --glob "*.less"`
      Plus targeted sweeps from §3.5: stale `// TODO: Refactor in Phase 3 (Vue Migration)` markers; module `phpunit.xml.dist`; module `composer.json` autoload entries; `CacheInterface` / `Psr6Adapter`; `SymfonyEventDispatcherBridge` / `symfony.event_dispatcher`; `create_function`. Classify each hit as **OK / by design**, **OK / tagged with valid future ROADMAP ID**, or **GAP**. Record full tables in §4.3 of audit report.
  12. **Documentation drift sweep.** Verify per §3.6:
      - `.cursor/ROADMAP.md` rows 2.0.0–2.0.8 statuses, audit cells, issue + PR linked.
      - `migration-docs/TODO/PHASE_2_MODERNISING.md` — every 2.0.x section has `Closes Phase 1 audit:`, `Agent Prompt:` path, and (if applicable) `**Audit findings (Phase 1 review):**` subsection.
      - `migration-docs/branches/` — branch doc per sub-step (`step-2-0-X-*.md` or older `*_MIGRATION.md` naming). Missing branch docs = documentation-debt gap.
      - `README.md` and `AGENTS.md` — find references to cache / migrations / event-dispatcher / PHPUnit module configs that 2.0.x changed and that still describe the old way.
      - `CHANGELOG-NEW.md` — every 2.0.x entry has a coherent section, version chain `1.2.5 → 1.2.13` complete (no gaps).
      Record findings in §4.6 of audit report.
  13. **Gap List assembly + disposition decisions.** Collate every "GAP" classification from steps 2–12 into the §4.4 Gap List table:
      | # | Gap (one line) | Disposition | Target |
      Disposition column ∈ {`new sub-step 2.0.X`, `route to existing step`, `informational only`}. For routed gaps, name the existing step (most likely `2.1.6`, `2.1.9`, `2.5`). For new sub-step gaps, assign the next free integer ≥ 9. **Architect's call** in this checklist step which gaps are must-fix-before-2.1 (block 2.0 closure) vs. non-blocking (allow ✅ / 🛡️ closure with deferred sub-steps). Document the decision rationale in audit-report Conclusion.
  14. **For each new sub-step gap (zero or more): create paper deliverables.** For each new 2.0.X (X ≥ 9) decided in step 13:
      - Insert ROADMAP row immediately after the last existing 2.0.X row, with `⏳ / ⏳` and a placeholder Issue ID (replaced after step 16).
      - Insert PHASE_2 section in `migration-docs/TODO/PHASE_2_MODERNISING.md` immediately after the existing **Step 2.0.8** subsection and before the **## Step 2.1** header. Use the verbatim labels from §5.3 of the prompt: Goal, Prerequisite, Priority, Context, Closes Phase 1 audit, Tasks, Result, Risk, Affected Files, Agent Prompt.
      - Create agent-prompt skeleton at `migration-docs/TODO/agent_prompts/Step-2_0-Foundation-Consolidation/PROMPT_2_0_X_{Slug}.md` using `PROMPT_2_0_7_Event-Bridge-Removal.md` or `PROMPT_2_0_8_User-hasAccess-Hotfix.md` as a template, including the 11 mandatory sections from §5.4 (Title, Header block, Closes Phase 1 audit, Also read, §1 CONTEXT, §2 SAFETY & VERIFICATION, §3 IMPLEMENTATION, §4 TESTS, §5 SUCCESS CRITERIA, §6 OUT OF SCOPE, `**End of prompt.**`).
  15. **For each "route to existing step" gap: update target PHASE_2 subsection.** Append the gap as a bullet under that step's `**Audit findings (Phase 1 review):**` (or `**Audit findings (Step 2.0.X review):**`) block in `migration-docs/TODO/PHASE_2_MODERNISING.md`. Do **NOT** create a new agent prompt and do **NOT** open a new GitHub issue — the existing step already owns it. Add a one-liner pointer in the audit report's Gap List "Disposition" column referencing the diff.
  16. **GitHub issues for each new sub-step.** Use `.cursor/skills/github-issue-creator/SKILL.md` to open one issue per new sub-step on `Shadesman5/pagekit` with: Title `Step 2.0.X: {Concise Task Name}`; Labels `phase-2` + relevant area label per `.cursor/rules/github-labels.mdc`; Milestone `Phase 2: Developer Experience`; Body linking to PHASE_2 section + agent prompt + audit report; Parent issue **#181** (sub-issue relationship). After creation, write the real issue number into the new ROADMAP row's Issue column AND into the new PHASE_2 section's `Agent Prompt` line preamble (replace placeholder `#{ISSUE}`).
  17. **Audit-report finalisation.** Write Executive Summary (one paragraph + Closure Verdict table for all 10 sub-steps); fill in §4.5 New Sub-Step Proposals (full skeletons cross-linked from step 14); §4.6 PHASE_2/ROADMAP/Issue Updates (exact diffs from steps 14–16); §4.7 Final Test Summary (paste step 1 outputs); §4.8 Closure Verdict — one of `✅ Step 2.0 can close in this PR. New sub-steps (if any) non-blocking.` OR `⚠️ Step 2.0 stays ⏳ until {list of must-fix new sub-steps} land.`
  18. **Branch doc.** Create `migration-docs/branches/step-2-0-foundation-closure.md` summarising audit verdict, list of new sub-steps (with IDs + titles + issue numbers), and list of routed gaps. Format / style per existing files in `migration-docs/branches/` (e.g. `step-2-0-7-event-bridge-removal.md`, `step-2-0-8-user-hasaccess-hotfix.md`).
  19. **CHANGELOG-NEW.md.** Add `## Pagekit X.Y.Z - Foundation Consolidation Closure (2026-04-28)` section grouping: audit report file, new sub-step skeletons (if any), ROADMAP closure verdict, routed-gap PHASE_2 updates. The version `X.Y.Z` is the patch bump produced by `version-bump` in step 21; insert a placeholder `${NEW_VERSION}` here and resolve it in step 21.
  20. **ROADMAP final state update** per §7 of the prompt:
      - If audit verdict ✅ and zero must-fix-before-2.1 gaps: flip the `2.0` row Status → `✅`, Audit → `🛡️`, PR cell → `#{THIS_PR}` (orchestrator fills after push); set `Current Step` header pointer to `2.1.2` (2.1.1 already done); insert any new 2.0.X rows below 2.0.8 with `⏳ / ⏳`.
      - If verdict ⚠️ (must-fix-before-2.1 gaps exist): leave `2.0` row at `⏳ / ⏳`, point `Current Step` header at the first new sub-step (e.g. `2.0.9`), document blocking dependency in PHASE_2 prose under the new sub-step's `Prerequisite` line ("must land before Step 2.1.x").
  21. **Version bump.** Run `.cursor/skills/version-bump/SKILL.md` (third-digit patch bump — this PR is `docs`/`chore` heavy, no production code changes). Updates `app/system/config.php` only (composer.json no longer carries the version per `version-bump` SKILL). Replace `${NEW_VERSION}` placeholder in CHANGELOG-NEW.md with the resolved version.
  22. **Final test gate.** Run **PHPUnit + PHPStan + `php pagekit setup` + `php pagekit list` + Playwright E2E (chromium-only: `installation`, `authentication`, `dashboard` specs)** on the closure branch. All MUST be green. Any red → fix in a follow-up Refactorer pass on the *audit machinery only* (e.g. malformed YAML/Markdown breaking parsers); never patch PHP / JS / LESS in this PR.

## TESTING STRATEGY
- **Per step:** PHPUnit + PHPStan (mandatory after every checklist step). Because this PR touches no PHP/JS/LESS source, both gates are **regression detectors** — they MUST stay green from baseline (step 1) to final (step 22). After Markdown-only steps (2–20) Verifier may run a lightweight `rg`/`composer validate` cross-check instead of waiting on full PHPUnit, but Tester always runs PHPUnit + PHPStan before commit.
- **Final run (after all steps):** PHPUnit + PHPStan + `php pagekit setup` + `php pagekit list` + Playwright E2E (installation, login, dashboard).
