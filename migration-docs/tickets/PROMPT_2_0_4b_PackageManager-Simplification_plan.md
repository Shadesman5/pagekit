## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.0.4 (sub-step 2.0.4b)
- **Scope:** `app/installer/src/Package/PackageManager.php`, `tests/Unit/Installer/PackageManagerNamespaceTest.php`, `packages/pagekit/blog/scripts.php`, `.cursor/BUGBOT.md`
- **Deferred:**
  - Step 5.6 (Marketplace & Extensions) — marketplace API, discovery, ZIP upload
  - Step 2.5 (Extension Safety) — sandboxed loading, fault isolation, auto-disable
  - Step 2.1.9 (Test Coverage) — `MigrationCommand` integration test remains relevant; `PackageManager::enable()`/`uninstall()` migration integration tests are NO LONGER NEEDED (auto-migration removed)
- **Bridges:** None. Clean deletion per Rules 1, 4 (No Compatibility Layers, Delete Over Wrap).
- **Checklist:**
  1. **Remove auto-migration detection from `enable()`** — `app/installer/src/Package/PackageManager.php`: (a) Delete the `$appliedMigration` variable declaration and `@var` annotation (~lines 159-160). (b) Delete the entire auto-migration detection block (~lines 193-226) that checks `is_dir($packagePath . '/src/Migrations')`, calls `resolveExtensionMigrationNamespace()`, `getExtensionCurrentVersion()`, and `migrateExtension()`. After deletion, `enable()` flows: event → `doInstall()` if new → `scripts->update()` → `scripts->enable()` → set version → register extension/theme.
  2. **Remove migration rollback from `enable()` catch block** — `app/installer/src/Package/PackageManager.php`: Delete the entire `$appliedMigration !== null` block in the `catch (\Throwable $e)` handler (~lines 246-270). Keep the `rollbackEnable()` call for config state restoration — that is correct. The catch block should flow: `rollbackEnable()` → log error → re-throw.
  3. **Remove auto-rollback from `uninstall()`** — `app/installer/src/Package/PackageManager.php`: Delete the entire auto-rollback block (~lines 94-129) that checks `is_dir($packagePath . '/src/Migrations')`, calls `resolveExtensionMigrationNamespace()`, and `rollbackExtension('0')`. Extensions that want table cleanup do so in their `scripts.php` `uninstall` hook per original Pagekit philosophy.
  4. **Delete namespace resolution methods** — `app/installer/src/Package/PackageManager.php`: Delete these 5 methods entirely (~160 lines): `resolveExtensionMigrationNamespace()` (~lines 379-425), `parseAutoloadFromIndexFile()` (~lines 436-462), `extractBracketBody()` (~lines 470-506), `unescapePhpString()` (~lines 514-517), `stripPhpComments()` (~lines 525-537). These only existed to support auto-detection.
  5. **Delete `PackageManagerNamespaceTest`** — Delete `tests/Unit/Installer/PackageManagerNamespaceTest.php` entirely. All 21 tests cover deleted methods (`resolveExtensionMigrationNamespace`, `parseAutoloadFromIndexFile`, `extractBracketBody`, `unescapePhpString`).
  6. **Update Blog `scripts.php` to explicit pattern** — `packages/pagekit/blog/scripts.php`: (a) Add `enable` hook with explicit `migrateExtension()` call (idempotent — only runs NEW migrations). (b) Simplify `install` hook — remove error wrapping, let exceptions propagate naturally. (c) Simplify `uninstall` hook — keep as explicit developer choice with commented-out rollback example (per original Pagekit philosophy: extensions choose whether to clean up). (d) Remove verbose error handling. (e) Add `updates` array documentation noting schema changes go in `src/Migrations/`, `updates` is only for DATA migrations.
  7. **Update `BUGBOT.md`** — `.cursor/BUGBOT.md`: (a) In "Already resolved" table, add entry: `Auto-migration/rollback removed from PackageManager | PR #XXX (Step 2.0.4b) | Extensions use explicit scripts.php hooks per original Pagekit design`. (b) In "Known deferred patterns" table, remove the `PackageManager::enable()`/`uninstall()` migration integration tests entry (Issue #156) — these tests are no longer needed since auto-migration was removed. The `MigrationCommand` integration test entry remains.
  8. **Final verification** — (a) Confirm `MigrationService` public API is unchanged (all methods intact). (b) Confirm core update pipeline is unchanged: login check (`app/system/index.php`), wizard (`MigrationController`), CLI (`MigrationCommand`). (c) Confirm `MigrationServiceTest` still passes (12 tests). (d) Confirm no references to deleted methods remain via `rg 'resolveExtensionMigrationNamespace|parseAutoloadFromIndexFile|extractBracketBody|unescapePhpString|stripPhpComments' --type php`. (e) Confirm `PackageManager::enable()` responsibilities are exactly: event, install, script updates, script enable, version set, config register.

## TESTING STRATEGY
- **Per step:** PHPUnit + PHPStan (mandatory after every checklist step)
- **Final run (after all steps):** PHPUnit + PHPStan + `php pagekit setup` + `php pagekit list` + Playwright E2E (installation, login, dashboard)
