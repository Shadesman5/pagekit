version: '3.8'

services:
  # PHP + Apache Web Server
  web:
    build: .
    container_name: pagekit_web
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/php.ini
    depends_on:
      - mysql
    env_file:
      - docker.env
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html
    networks:
      - pagekit_network

  # MySQL Database (default)
  mysql:
    image: mysql:8.4
    container_name: pagekit_mysql
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init:/docker-entrypoint-initdb.d
    env_file:
      - docker.env
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    command: --default-authentication-plugin=mysql_native_password
    networks:
      - pagekit_network
    profiles:
      - mysql
      - default

  # phpMyAdmin (only with MySQL)
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: pagekit_phpmyadmin
    ports:
      - "8081:80"
    env_file:
      - docker.env
    environment:
      PMA_HOST: ${DB_HOST}
      PMA_PORT: ${DB_PORT}
      PMA_USER: root
      PMA_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    depends_on:
      - mysql
    networks:
      - pagekit_network
    profiles:
      - mysql
      - default

  # Node.js for Frontend Development
  node:
    image: node:18-alpine
    container_name: pagekit_node
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install -g yarn@1.22.22 && yarn install && yarn watch-all"
    networks:
      - pagekit_network

volumes:
  mysql_data:

networks:
  pagekit_network:
    driver: bridge
