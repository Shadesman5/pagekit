## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.1.3
- **Scope:** Add `declare(strict_types=1);` to every PHP file under `app/modules/`, `app/system/`, `app/installer/`, `app/console/`, and `packages/`, fixing any resulting `TypeError`s in production and test code, then enable `declare_strict_types => true` in `.php-cs-fixer.php`. Files affected (current count, excluding files that already have the declaration): `app/modules/` ≈ 241, `app/system/modules/` ≈ 89, `app/system/src/` 2, `app/installer/` 18, `app/console/` 15, `packages/pagekit/blog/` 92, `packages/pagekit/theme-one/` 94 — total ≈ 551 files. The ~28 untyped test files mentioned in the audit are spread across Filter, Filesystem, Cookie, Auth, Database, Config, Session, Routing and are migrated inside the same group as their module sources.
- **Deferred:**
  - Step 2.1.4 (PHPStan Level 5→6) — return type sweep stays out of this ticket; do NOT add return types beyond what is strictly required to silence a `TypeError` introduced by `strict_types`.
  - Step 2.1.5 (PHPStan Level 6→7) — null-safety sweep stays out of this ticket.
  - Step 2.1.6 (PHPStan Level 7→8) — strict typing sweep stays out of this ticket.
  - Step 2.1.7 (QueryBuilder API standardization) — out of scope.
  - Step 2.1.8 / 2.1.9 (Infection / Coverage) — out of scope.
  - **PHPStan baseline regeneration** — explicitly forbidden during this step. Errors that disappear because `strict_types` resolved them are a positive signal; new errors are real regressions and must be fixed in code, never absorbed into the baseline.
- **Bridges:** None. `strict_types` is a forward-only change with no parallel old/new behavior; if a call site cannot be made strict-safe, the call site is fixed (cast, signature change, or null-safe variant) — never wrapped in a shim.
- **Checklist:**
  1. **Group 1 — Small Core Modules.** Add `declare(strict_types=1);` to every PHP file (after `<?php`, before `namespace ...;`) in `app/modules/filter/` (29 files), `app/modules/filesystem/` (16 files), `app/modules/cookie/` (3 files), including their `src/Tests/*` test files (audit-flagged). Skip files that already declare it. Fix any `TypeError`s surfaced by PHPUnit by adding the minimal cast (`(int)`, `(string)`, `(float)`, `(bool)`) at the call site or by tightening the parameter type (`?int $x` for nullable inputs). Single conventional commit: `refactor(types): add strict_types to filter, filesystem, cookie`.
  2. **Group 2 — Auth & Security.** Add `declare(strict_types=1);` to every remaining PHP file in `app/modules/auth/` (16 files), including `src/Tests/AuthTest.php` and `src/Tests/DatabaseHandlerTest.php` (audit-flagged). Fix `TypeError`s as in Step 1. Pay special attention to `Auth`, `UserProvider`, password hashers, and session bridges — these are exercised by the login E2E later. Commit: `refactor(types): add strict_types to auth`.
  3. **Group 3 — Database Core.** Add `declare(strict_types=1);` to every remaining PHP file in `app/modules/database/` (10 files), including any uncovered `src/Tests/*` files (audit-flagged). Fix `TypeError`s — Doctrine DBAL parameter binding and ORM hydration are the highest-risk surfaces. Prefer adjusting the call sites that pass numeric strings over loosening the receiving method signature. Commit: `refactor(types): add strict_types to database`.
  4. **Group 4a — Remaining Core Modules (low-volume).** Add `declare(strict_types=1);` to every remaining PHP file in `app/modules/routing/` (21), `app/modules/session/` (9), `app/modules/config/` (5), `app/modules/markdown/` (6), `app/modules/migration/` (1), `app/modules/log/` (3) — ~45 files. Include any audit-flagged Config / Session / Routing test files. Fix `TypeError`s. Commit: `refactor(types): add strict_types to routing, session, config, markdown, migration, log`.
  5. **Group 4b — Remaining Core Modules (high-volume).** Add `declare(strict_types=1);` to every remaining PHP file in `app/modules/kernel/` (23), `app/modules/application/` (34), `app/modules/view/` (35), `app/modules/debug/` (17), `app/modules/feed/` (13) — ~122 files. Fix `TypeError`s — Twig integration in `view`, error handlers in `debug`, and `Kernel`/`Application` boot path are the most likely failure points. Commit: `refactor(types): add strict_types to kernel, application, view, debug, feed`.
  6. **Group 5a — Critical System Modules.** Add `declare(strict_types=1);` to every remaining PHP file in `app/system/modules/user/` (22), `app/system/modules/site/` (17), `app/system/modules/widget/` (7) — ~46 files. Fix `TypeError`s — User/Role hydration and Site node tree manipulation are the highest-risk areas, both are exercised by the dashboard E2E. Commit: `refactor(types): add strict_types to system user, site, widget`.
  7. **Group 5b — Remaining System Modules.** Add `declare(strict_types=1);` to every remaining PHP file in the rest of `app/system/modules/*` (cache, captcha, comment, content, dashboard, editor, finder, info, intl, settings, theme, view — ~43 files) and `app/system/src/` (`SystemModule.php`, `SystemMenu.php`, `Controller/*`, `Model/*`, `Validator/*`, `ValidatorServiceProvider.php` — 2 files still missing it). `app/system/modules/mail/` is already complete and must be skipped. Fix `TypeError`s. Commit: `refactor(types): add strict_types to remaining system modules`.
  8. **Group 6 — Installer & Console.** Add `declare(strict_types=1);` to every remaining PHP file in `app/installer/` (18 files — includes `app/installer/index.php`, `app/installer/install.php`, `app/installer/install-demo.php`, `app/installer/requirements.php`, `app/installer/config.php`, plus `app/installer/src/*` and `app/installer/views/*.php`) and `app/console/` (15 files — includes `app/console/index.php`, `app/console/app.php`, `app/console/src/*`). For non-namespaced root files (`index.php`, `install.php`, `app.php`, `config.php`), insert `declare(strict_types=1);` immediately after `<?php` even if there is no `namespace`. Fix `TypeError`s. Commit: `refactor(types): add strict_types to installer and console`.
  9. **Group 7a — Blog Package.** Add `declare(strict_types=1);` to every remaining PHP file in `packages/pagekit/blog/` (~92 files: `index.php`, `src/*`, `app/*`, `views/*.php`). Fix `TypeError`s in Blog `Post`/`Comment` controllers and views. Commit: `refactor(types): add strict_types to blog package`.
  10. **Group 7b — Theme-One Package.** Add `declare(strict_types=1);` to every remaining PHP file in `packages/pagekit/theme-one/` (~94 files: `index.php`, `src/*`, `app/*`, `views/*.php`, `views/widgets/*.php`). Fix `TypeError`s. Commit: `refactor(types): add strict_types to theme-one package`.
  11. **Pre-toggle dry-run sweep (§ 4.1).** Run `find app/modules app/system app/installer app/console packages -type f -name "*.php" -exec grep -L 'declare(strict_types' {} \; | wc -l` (must output `0`) and `./app/vendor/bin/php-cs-fixer fix --dry-run --diff --no-interaction --show-progress=none --allow-risky=yes` (must exit 0 and report no `declare_strict_types` violations even though the rule is still off — the run also catches any other style drift introduced by the migration). If the count is non-zero, the Refactorer adds `declare(strict_types=1);` to the offending files and folds them into a single commit `refactor(types): sweep up missed strict_types files`. If the count is already zero, this checklist step produces no commit (Orchestrator detects clean tree → re-delegates per the failure rule, but in this case the Refactorer must explicitly emit `Step 11 done. Files: [] (sweep clean, no commit needed).` and the Orchestrator skips the commit and proceeds).
  12. **Enable `declare_strict_types` in PHP-CS-Fixer (§ 4.2).** Edit `.php-cs-fixer.php`: replace the line `// TODO: Must be refactored in Step 2.1.3 (strict_types Migration)` with `'declare_strict_types' => true,` (preserving the surrounding rules array structure and trailing comma). Run `./app/vendor/bin/php-cs-fixer fix --dry-run --diff --no-interaction --show-progress=none --allow-risky=yes` and confirm it still exits 0 (no violations because Step 11 already brought every file into compliance). Commit: `chore(cs-fixer): enable declare_strict_types rule`.

## TESTING STRATEGY
- **Per step:** PHPUnit + PHPStan (mandatory after every checklist step). On Steps 1–10, the Tester MUST also confirm that `phpstan-baseline.neon` was NOT regenerated (`git diff --quiet phpstan-baseline.neon` exits 0). On Step 11, the Tester additionally runs the `find ... grep -L` sweep above and asserts the count is `0`. On Step 12, the Tester additionally runs `./app/vendor/bin/php-cs-fixer fix --dry-run --diff --no-interaction --show-progress=none --allow-risky=yes` and asserts exit 0.
- **Final run (after all steps):** PHPUnit + PHPStan + `php pagekit setup` + `php pagekit list` + Playwright E2E (installation, login, dashboard).
