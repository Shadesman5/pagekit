## ARCHITECT OUTPUT
- **Current Step (ROADMAP):** 2.0.2
- **Scope:** `app/system/src/ValidatorServiceProvider.php`, `app/system/languages/en_US/validation.php` → `validators.php`, `packages/pagekit/blog/languages/en_US/validation.php` → `validators.php`, `app/system/index.php` (docblock only), `app/system/src/Controller/ValidatesRequestTrait.php` (no code change needed — already returns `getMessage()`), new test file `tests/Unit/Validator/ValidatorTranslatorIntegrationTest.php`
- **Deferred:** Step 2.1 (Static Analysis) — `ExtensionTranslateCommand` AST extraction for `#[Assert\...]` message keys is optional/low-priority; defer with TODO tag. No other deferrals.
- **Bridges:** None required. The rename `validation.php` → `validators.php` is a clean cut (no compatibility layer). `IntlModule::loadLocale()` uses `glob('*.php')` so the domain auto-derives from the new filename. No bridge needed.
- **Checklist:**
  1. **Rename translation files** — `git mv` `app/system/languages/en_US/validation.php` → `validators.php` and `packages/pagekit/blog/languages/en_US/validation.php` → `validators.php`. Verify no other locale dirs contain `validation.php`. Verify no PHP code explicitly references `validation.php` by name.
  2. **Connect Translator to Validator** — In `ValidatorServiceProvider::register()`, add `$builder->setTranslator($app->get('translator'))` and `$builder->setTranslationDomain('validators')`. Update the class docblock to remove "hybrid mode" / Step 1.13-era comments and reflect Translator integration.
  3. **Update boot comment in `app/system/index.php`** — Replace the Step 1.13 "Hybrid Mode" comment on the `ValidatorServiceProvider::register()` call with a current note (Step 2.0.2, Translator-integrated). Verify boot order: `translator` registered in `IntlModule::main()` (container phase, before boot), `validator` factory resolves `translator` lazily — no ordering issue.
  4. **Verify translation key consistency** — Cross-reference all `message: 'validation.*'` keys in model `#[Assert\...]` attributes against entries in the renamed `validators.php` files (system + blog). Add any missing keys. Confirm constraints without explicit `message:` use Symfony built-in English defaults (acceptable).
  5. **Write tests** — Create `tests/Unit/Validator/ValidatorTranslatorIntegrationTest.php`: (a) test validator returns translated message (not raw key) for invalid entity, (b) test `validationErrorResponse()` contains human-readable messages, (c) test locale fallback to `en_US`.
  6. **Add deferred TODO for ExtensionTranslateCommand** — Add `// TODO: Step 2.0.2 - Extract #[Assert\...] message keys from PHP 8 Attributes (optional, low priority)` in `app/console/src/NodeVisitor/PhpNodeVisitor.php`.
  7. **Run full PHPUnit suite** — `./app/vendor/bin/phpunit` must pass with 0 failures.
